FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install --no-cache-dir pandas numpy plotly

COPY . .

ENV PORT=8050
CMD ["python", "watch_chart.py"]
