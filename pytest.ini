[pytest]
# Added 2026-09-10. ASCII ONLY in this file: pytest reads it with the system
# locale codec (cp950 on this machine), so non-ASCII comments raise
# UnicodeDecodeError before any test runs.
markers =
    slow: takes minutes (permutation tests, full-history rebuilds). CI runs
        them; for local iteration use -m "not slow".
