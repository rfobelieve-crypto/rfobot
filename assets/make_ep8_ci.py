# -*- coding: utf-8 -*-
"""產生 LinkedIn ep8：信賴區間是做什麼用的。

2026-09-07 第二版（使用者：「CI 的文章太複雜了，簡化白話一點，主要是介紹
CI 信賴區間是做什麼用、有什麼功能」）。

第一版是「同一個工具上的五種錯法」，密度太高、預設讀者已經懂 CI。
這一版改成入門：它在回答什麼問題、三個實際功能、一件它做不到的事。
例子縮到兩個，而且每個只講結論不講推導。
"""
from pathlib import Path

from docx import Document
from docx.oxml.ns import qn
from docx.shared import Pt

OUT = Path.home() / "Desktop" / "linkedin_posts" / "quant_ep8_confidence-intervals.docx"

BODY = """信賴區間到底在告訴你什麼

大部分人只用了它十分之一的功能。

先講它在回答哪個問題。

你做了一個實驗，量到平均值是 +5%。這是「這一批樣本」的平均。

那換一批樣本呢？會不會變 +1%？會不會變 −3%？

信賴區間就是這個問題的答案。它說的是：如果同樣的實驗重做很多次，大約 95 次裡的區間會蓋住真正的值。

所以它量的是「這個數字有多穩」，不是「這個數字有多好」。

這兩件事常常被混在一起，而混在一起就會用錯。

功能一：判斷有沒有結論

這是大家最熟的用法：區間有沒有跨過零。

但這裡有個很重要的分別。跨過零的意思是「沒有結論」，不是「沒有效果」。

沒有效果，是說這件事真的不存在。沒有結論，是說你目前的資料還分不出來——可能真的不存在，也可能存在但你樣本太少。

這兩個的下一步完全不同。一個是收手，一個是繼續收資料。把它們講成同一件事，你會提早放棄本來會成立的東西。

功能二：決定要收集多少資料

這個功能大部分人沒有用到，但它其實最省時間。

正常用法是：收完資料，算出區間，看結論。

反過來用是：在開始之前，先問「我想偵測多大的效果」，然後算「那需要多少樣本」。

我犯過一次錯。我設了一個門檻：兩組的差要達到 8 個百分點才算通過。跑了一個月，差 5.8 個百分點，看起來像差一點沒過。

檢討時把標準誤算出來，發現在那個樣本數下，雜訊本身就有 11.6 個百分點。

門檻 8，雜訊 11.6。

也就是說，不管資料怎麼走，這個實驗都分不出過或沒過。它不是快要過了，它是從第一天起就不可能有答案。

而這件事在開始之前就算得出來，只要五分鐘。算完可以省下幾個月。

所以現在我凍結任何判準的時候，一定會多做一步：把它在我打算收集的樣本數下的標準誤算出來。如果雜訊比門檻還大，這個設計就要重做，不能開跑。

功能三：知道該看哪一端

區間有兩端，而你該看哪一端取決於你在主張什麼。

主張「A 比 B 好」，你要看 A 的下緣有沒有超過 B。因為下緣是 A 最差的情況——連最差都比 B 好，才算數。

主張「A 比 B 差」，反過來，要看 A 的上緣有沒有低於 B。

看錯邊會發生什麼？我寫過一個判準：「這一組的下緣要低於全體」。代入數字之後才發現，任何夠寬的區間都自動滿足它。那不是門檻，是一句永遠成立的話。

判準要壓在對自己不利的那一端。壓錯邊，門檻就失效了。

一件它做不到的事

信賴區間只處理一種不確定性：抽樣。

它假設你的資料是對的、你量的是你以為在量的東西。這兩個假設它不會幫你檢查，也沒有能力檢查。

我有一條策略，統計上非常乾淨：每筆報酬的區間下緣穩穩大於零、九個標的全部同向、t 值超過 3、兩年半的資料。

後來我去驗一件統計看不到的事——回測假設的成交價，市場當時給不給。

結果是超過半數的情況下沒有給。用真實可成交的價格重算，同一批交易的區間上緣變成小於零。

信賴區間從頭到尾都算對。它量的是「這個平均有多穩」，而它確實很穩。它不會、也不可能告訴我：我餵給它的那個平均，本身是用一個拿不到的價格算出來的。

統計顯著能處理抽樣誤差。它處理不了資料污染、版本混合、或一個不存在的成交價。

所以它是最後一關，不是第一關。在算它之前，要先確定你量的是對的東西。

一句話收尾

信賴區間告訴你「這個數字有多穩」，不告訴你「這個數字是不是真的」。

前者是統計問題。後者是你的問題。
"""


def main():
    doc = Document()
    st = doc.styles["Normal"]
    st.font.name = "Microsoft JhengHei"
    st.font.size = Pt(11)
    st._element.rPr.rFonts.set(qn("w:eastAsia"), "Microsoft JhengHei")
    for line in BODY.strip().split("\n"):
        doc.add_paragraph(line.strip())
    OUT.parent.mkdir(parents=True, exist_ok=True)
    doc.save(OUT)
    n = len([x for x in BODY.strip().split("\n") if x.strip()])
    print(f"written -> {OUT}")
    print(f"段落 {n}、字數約 {len(BODY.replace(chr(10), ''))}")


if __name__ == "__main__":
    main()
