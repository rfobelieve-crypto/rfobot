# TODO — 待處理計劃

> **跨 session 任務真相源**。Claude Code 的 session 任務清單不進 git、不跨機器——
> 凡是要「本機 / 雲端 / 下一次對話」都看得到的任務，寫這裡並 push。
> 每次開工：先讀「當前任務」區。

## 當前任務（2026-07-16 更新）

### 0.80 「每個 Strong 都進、每筆縮小、一起出」（2026-08-30 使用者提案）——訊號層量測：不建議，未預註冊

**提案**：Strong 訊號常成串出現，但單倉制只吃第一根；改成每筆金額縮小、
每根訊號都進、出場訊號一出全平。

**先驗證觀察是對的**（`tracked_signals` Strong，dual v7 起 2026-04-03，
n=122，同向且相隔 ≤6h 視為同串）：**69% 的 Strong 落在 ≥2 根的串裡**，
但 **60% 的串（38/63）只有一根**。全歷史（n=819）串更長：平均 3.34 根，
6+ 根的串佔 20%。

**串內名次（訊號層 4h TWAP 方向報酬）**：

| 名次 | n | WR | bps |（全歷史 WR / bps）|
|---|---|---|---|---|
| 第 1 根 | 63 | **63.5%** | **+20.7** | 64.5% / +25.7 |
| 第 2 根 | 25 | 40.0% | +1.8 | 54.5% / +15.6 |
| 第 3 根 | 13 | 46.2% | +20.0 | 60.8% / +23.6 |
| 第 4+ 根 | 21 | 61.9% | +15.3 | 57.0% / +13.2 |

**第一根在每個切法上都是最好的一根**——現行「只吃第一根」剛好吃到最強
的那根。把資金從它挪到後面幾根，是從最好的桶挪到較差的桶。

**四個量測結果，沒有一個支持提案**：
1. **均分 vs 首根**（每串總額固定、成員平均）：6h 切 +11.0 bps/串，
   CI95 **[−19.5, +43.0]**；3h 切 +3.1 [−25.6, +34.4] 兩半反號；解碼修法後
   +0.7 兩半反號；全歷史 +2.7 [−11.0, +17.6] 兩半反號。→ 無證據。
2. **照提案的實際規則**（每筆固定縮小、串越長曝險越大）：把每筆設為
   0.52×S 使平均曝險與現制相同，每串損益 **首根 +20.7 vs 提案 +15.8 bps**
   （全歷史 +25.7 vs +18.7）——**更差**。原因：曝險自動集中到最長的串，
   而 6+ 根的串（04-03 後 n=4）每根 −5.7 bps、首根 −98.7——正是 §0.51b
   說的「模型堅持、市場不同意」那種虧損，加碼加在它身上。
3. **曝險上限**：最長串 9 根 → **4.6×S**，21% 的串超過 1.5×S。有效槓桿
   2x × 4.6 = 9x，直接撞 `guard` 上限 3.0；這是 2026-06-09
   [[project_tier_entry_crowdout]] 「開每個 Strong ＝ 峰值同向疊倉 4-8 ＝
   爆倉向量」的同一件事，縮小每筆只是把倍數從 9 變 4.6。
4. **不是分散**：串內相鄰兩根的 4h 報酬相關 **0.58**（全歷史；近期 0.17，
   n=59）——4h 窗口重疊 3h，這是同一個賭注分批下，不是九個賭注。

**唯一有一點形狀的子假設**（誠實記下，不推進）：第 3 根以後的進場價
平均已經逆訊號方向走了 −14 ~ −67 bps（回歸交易的「更便宜的進場」），
之後 4h +15 ~ +20 bps。這是「總額固定、逆向漂移時分批攤入」——與提案
不同（提案是每根都加、曝險隨串長）。它的死穴同樣在長串尾巴。若要驗，
必須用交易層 harness（`exit_attribution.py`，忠實出場語意）而非本節的
訊號層 4h 代理，且先預註冊；**現階段不開**。

**處置**：不改 jarvis V7Bot 單倉制；不預註冊。腳本在 session scratchpad
（`cluster.py` / `cluster2.py`），未入 repo——結論已全記於此。

**追問（同日）：「平均成本」與「訊號多＝可信度高」兩個直覺分開量**
（`cluster3.py`；同一出場點＝最後一根之後 4h、曝險相同，只比進場價）：
- **平均成本**：平均進場價比首根便宜的串只佔 **44%**（04-03 後）／**38%**
  （全歷史）——回歸訊號開火時價格已經拉開，第二根常常已經開始回頭，
  後面的進場反而更貴。走到同一出場的報酬差：+7.4 bps CI [−18.5, +34.1]
  （近期）／**−5.5** [−16.8, +7.1]、兩半 +0.4/−11.3（全歷史）。→ 價格上是
  擲硬幣，報酬上是零。攤入期間從首根算起的最大逆向走勢平均 −45~−52 bps、
  最差一成 **−120~−128 bps**——攤平的代價是全程抱著這段。
- **可信度**：已出現 k 根同向訊號之後，**下一根**的勝率：0 根 63.5% →
  1 根 **40.0%** → 2 根 46.2% → 3+ 根 61.9%（全歷史 64.5 → 54.5 → 60.8 →
  57.0）。重複開火沒有讓下一根更準；串長 vs 整串賭注結果也無單調關係
  （近期 6+ 根 −20.9 bps、全歷史 3 根 −37.7 / 6+ 根 +53.2，純雜訊）。
  機制：連續小時的特徵幾乎相同、4h 窗口重疊 3h——**是同一個投票人重複
  投票，不是多個獨立投票**；與 08-24「極端預測不是更好的預測」同一件事。

### 0.81 V7 圖表補真實進出場（2026-08-31，使用者指出圖上沒標）

**病因**：兩張 V7 圖的 live 疊層讀 `v7_okx_positions`，該表凍在 08-11
（執行面遷 Bitget，CLAUDE.md 08-21）——之後的真實進出場只存在 jarvis 的
`{userDir}/v7_trades.jsonl`（08-25 起，`v7bot._trade`），圖上自然是空的。
另註：**08-11→08-25 有真實的記錄空窗**（產品端當時還沒有逐筆帳本），
這段圖上就是沒有，不補不假造。

**修法（照 §0.78 磨坊管道的形，兩端分工同款）**：
- 產品端：請開 `/api/u/export/v7`（鏡像 `/export/fills`，同一顆
  RESEARCH_EXPORT_TOKEN，只讀原樣回傳）——規格請求
  `../jarvis/研究端請求_V7逐筆匯出_20260831.md`
- 研究端（已落地）：`research/v7_product_trades_publish.py` 每小時拉
  （掛 shadow_engine.bat）→ 事件配對成回合（已知答案自測 PASS）→
  upsert `v7_product_trades` → `indicator/v7_product_trades.py` 供圖
- 兩張圖（`chart_renderer.py`／`chart_interactive.py`，圖表同步規則）
  改為 **OKX 時代（21 筆）＋ Bitget 時代聯集**疊層；產品端表不存在或
  拉取失敗一律回空清單，圖照畫（overlay 永不弄壞圖）
- 網站與 TG 走同一個 render 端點，部署即同步，無需另改

**✅ 端點已上（2026-08-31 08:51，產品端 V1.54.0 `93f29a8`，含 7 項測試）**：
研究端實測 HTTP 200、`fetched 0 events for Rfo`——**管道全鏈通了**。
0 筆是事實不是故障：本家帳戶的 V7Bot 自 08-25 有逐筆帳本以來尚無成交。
第一筆真實成交出現後，下一班車（每小時）拉回 → 表 → 圖，自動長出標記。
公共圖顯示層記得標「本家實盤」（§0.81b）。

**§0.81b 多用戶修正（2026-08-31，使用者：「每個用戶的進出場都是獨立的」）**：
上面整條管道只解**一張圖的一個帳戶**——jarvis 是多租戶，每個用戶的
V7Bot 獨立進出場，記在各自的 `v7_trades.jsonl`。所以拆成兩層，不可混：
1. **用戶自己的進出場 → 標在 App 自己的圖上**（產品端功能，資料在
   他們本地，不經研究端）——**產品端自行處理中（2026-08-31 使用者
   確認，研究端不動作）**——規格請求
   `../jarvis/研究端請求_V7用戶自身進出場上圖_20260831.md`：登入態
   端點讀自己的 jsonl、視覺沿用獵取圖的 ▲▼●、**措辭與獵取圖相反**
   （獵取標「規則的理想價位」，V7 標「你自己的實際成交」）。
2. **網站公共圖 → 只標本家帳戶**（`MILL_EXPORT_UID` 那一個 uid，
   即 §0.81 管道），顯示上必須標示「本家實盤」——公共圖在結構上
   代表不了任何用戶的個人進出場，別讓頁面暗示它可以。

### 0.82 V7 預算台階式重錨（2026-08-31 使用者裁定，規格已凍結交產品端）

**使用者**：「台階式重錨那就做成這個，不然不可能資金使用率都不增加」。
規格凍結於 `../jarvis/研究端規格_V7預算台階式重錨_20260831.md`（sizing
是策略規則 → 規則單向流，產品端照做不調數字）。核心：

- 策略權益 = 預算 + Σ(已平回合 pnl − 費)，**只算 V7 自己的回合**——
  對「權益被手動操作污染」（07-27）結構性免疫
- **升錨**：≥20 筆 ∧ ≥30 天 ∧ 權益 ≥1.2×預算 → 新預算 = min(權益,
  1.5×預算)；**降錨**：權益 ≤0.8×預算 → 立即（≈4-5 連虧深度，
  自動降階的產品版）；地板 100U 停機留人工
- `NOTIONAL_MULT=2` 不動、槓桿仍只管保證金；`autoRatchet` 預設 off、
  錨定歷史用戶可見；首次自動重錨帶告警（decay 首觸發先例）

**誠實界定**：這解「賺了不長大」（複利），不解「沒訊號時錢閒著」
（使用率）——後者 = 多策略共用資本（§0.4 統一風控框架），前置是
第二條策略過自己的 Gate（變體 B 已判 FAIL，Gate F 正式軌道 A 292/1400，§0.92）。兩題分開，不得互相冒充。

**狀態**：等產品端實作與驗收測試；連同 §0.81b（用戶自身進出場上圖）、
decay 催辦（08-30）共三件在產品端佇列。

### 0.84 英文口說面試準備（2026-09-01 列入；接 §6 面試就緒度）

**背景**：Web3 求職方向已析（適配序：交易所策略產品 > 風險機制 >
做市商執行研究 > AI×Crypto；前者中文友善可先投）。口說是英文面試
格子的瓶頸——但面試口說＝「把自己的系統講清楚」的英文，範圍可控。

**待辦**：
- [ ] 五段核心敘事英文口說腳本（材料全在 repo，Claude 產出）：
  ① 60 秒自介 ② 5 分鐘系統走讀 ③ 三個失敗故事（滑價符號／解碼
  鎖死三個月／uniMMR 拆解）④ 一個判決故事（Gate F 凍結開獎流程）
  ⑤ Q&A 20 題（overfit／預註冊／風控類）
- [ ] 每天 20 分鐘三步練法：影子跟讀（腳本餵 TTS）→ 語音 AI 對練
  （面試官角色，追問＋糾正表達）→ 每週錄 60 秒自介存檔（進步可量測）
- [ ] 8–12 週後評估：英文面試格子（風險機制／做市商）開投；
  中文格子（交易所策略產品）不必等
**與面試資產的關係**：口說腳本與 case study 是同一份內容（凍結記錄／
滑價帳／公開失敗三件事做齊後，敘事=「運營中系統＋50 真實用戶＋
十個月 prospective log＋主動展示失敗」）——一魚兩吃。

### 0.85 上游斷供降級守衛（2026-09-01 上線）——V7 最大單點依賴的靜默失效

**使用者問「Coinglass 停掉會怎樣／斷幾天再恢復會怎樣」，追程式碼的答案
不好看**：方向模型 136 個特徵有 **91 個是 `cg_*`（67%）**、幅度模型
60/76（79%）。斷供**不會拋錯**——`fetch_coinglass` 回退本機快取、
`merge_asof(backward)` **無時間上限**把最後一筆平推到每根新 bar。

**兩階段病灶（已用合成資料重現）**：
- 斷線中：17 個差分特徵變成**恰好 0**（「資金費率完全沒動」——現實不存在
  的狀態）；20 個 z 分數因滾動 std 塌陷而**漂向極端**；54 個水平純凍結。
- **恢復瞬間更糟**：跨數天的變化壓進一根 bar，z 的參考 std 是常數期算的
  → 爆表；兩個 z 相乘的衍生特徵更甚 → XGB 落到訓練never見過的葉 →
  極端 pred → **滾動分位數解碼下必然是 Strong** → 產品端拿去下真錢。
  且極端值進 200 根 buffer 會撐開切點，污染後續約一週（同 08-11 機制）。

**同族第三次**：08-08（模型凍結市場漂）、08-11（buffer 是種子）、本次
（資料凍結→跳變）——都是「解碼的參考分佈不是它要判斷的那個分佈」。

**三道守衛（`indicator/data_degradation.py`，16 個測試綠）**：
1. **陳舊上限**：CG 的 merge_asof 加 `tolerance`（`CG_MERGE_TOLERANCE`，
   預設 6h），超過給 NaN 不平推——**XGB 對 NaN 有訓練時學到的預設分支，
   對「凍結三天的常數」沒有**。
2. **降級旗標＋告警**：失敗端點 ≥3 → DEGRADED、≥50% → OUTAGE，
   狀態變化推 Telegram，狀態存 MySQL（`data_degradation_state`；
   **不存容器檔案**——08-11 教訓）。
3. **恢復靜默 24 根**：從 DEGRADED/OUTAGE 回來先進 RECOVERING，
   期間**不發訊號**，讓 z 窗口與解碼 buffer 輪替乾淨（抄 08-11 重訓
   暖機靜默的成功先例）；靜默期間再斷則計時重來。
   圖表/落庫/出場/kill 全部照跑，**只擋訊號發布**（同 ENTRY_PAUSED 語意）。

**儀器紀律應驗**：第一版測試的合成資料用線性斜坡，導致「正常期」的 z
也爆掉——**測試抓到的是我的 fixture 不真實，不是結論錯**；改隨機漫步
＋因果式 z（std 窗不含當根）後重現病灶成功。

**§0.85b 影響面補完（2026-09-01 同日，使用者：「我覺得還是會有很多影響」
——上一版只盤點 V7 訊號線，漏了四條）**：
1. **降級標記進訊號表**：`tracked_signals.data_state`（OK/DEGRADED/
   OUTAGE/RECOVERING，開火當下寫入，additive migration 已跑）——
   **判決時才能排除污染樣本**。沒有這個戳記，斷供在事後完全隱形，而
   Gate F／地形扳機／§0.59 都可能橫跨斷供窗口。影響的是研究結論的
   正確性，所以排第一。
2. **端點重要性加權**：`funding`/`oi`/`oi_agg`/`taker` 各餵約 20 個衍生
   特徵——**單一核心端點掛掉即 DEGRADED**（不必湊滿 3 個）、兩個即
   OUTAGE。原本純計數是錯的度量。
3. **feed 加 `data_state`**：`/public/signal-feed` 與 `latest_signal()`
   帶上狀態，產品端從此能區分「安靜的市場」與「上游降級、訊號被刻意
   withhold」（agent 側只讀狀態表，未 import 任何熱路徑）。
4. **freshness 加內容新鮮度**：新的 `parquet_content` 模式讀 CG parquet
   **最後一根資料的時間**而非 mtime——斷供時 collector 照跑、檔案照寫、
   mtime 永遠新鮮而內容凍結（08-01 同構）。

**又一次「儀器先驗」**：新守衛第一次跑就兩條紅燈，查下去**兩條都是我的
儀器錯**——(a) pandas import 被我前一個修補誤刪成函式內；(b) 掃描器用
`glob`（取最舊）判斷，但它**每日輪替**，昨天的檔案本來就舊 → 該行會
永遠紅 → 訓練操作者忽略告警（正是本看板存在要防的事）。已加
`glob_newest` 模式。修完 **0 red / 23 tracked**。

### 0.86 網站文章管線的斷點（2026-09-01 修，使用者：「我看網站沒有更新」）

**病灶**：`/writeups` 停在 07-27（zero-fires），但 **ep5/ep6/ep7 三篇 docx
早就寫完**（成本符號、地形判決、雙模型），躺在 `Desktop/linkedin_posts/`
六週沒上站。原因是管線有個手動步驟：docx 寫完後要**手動加進
`assets/extract_for_site.py` 的 ARTICLES 清單**、重跑、把 JSON 複製到
product-site。那個步驟沒人做，而**沒有任何東西會提醒**——網站看起來
正常，只是內容永遠停在六週前。

**已修**：三篇加進清單並抽取（零殘留鷹架）、封面圖搬進
`public/writeups/`、build 綠（12 條 writeup 路徑）、push master
（product-site `4a6cc13`）。網站現有 8 篇。

**結構性缺口（未修，登記）**：這是「產出與發佈之間的手動斷點」，跟
[[freshness board]] 抓的「排程死了」不同類——**這裡沒有排程，只有一個
沒人負責的步驟**。可能的修法：(a) 抽取腳本改成掃描目錄而非硬編清單，
docx 命名規則即註冊；(b) freshness board 加一條「linkedin_posts 的 docx
數 vs 網站文章數」對照，落差就紅。**(a) 較好但要先凍結命名規則**，
擇日再做。

**順帶產出**：`writeups/01_解碼鎖死三個月.md`（flow_system 內，技術版）
——那是給求職/case study 用的長版，與網站的敘事版是不同受眾，兩者並存
不互斥。若要上站需另寫成 docx 走同一條管線。

### 0.87 套利看板的三個缺口（2026-09-01，使用者：「套利的數據網站也沒更新」）

網站的進度條其實有在動（14:06 更新），但查下去有三個真缺口，前兩個是
§0.86 的同族——**東西在本機是對的，只是從來沒到達外面**：

1. **判決檔沒有任何排程**。`premium_verdict.py` 只有人跑才更新，
   `arb_premium_verdict.json` 因此**停在 08-30**（兩天前），而它正是
   09-04 判決要用的那個檔案。已掛進每小時班車（bat 末行已驗）。
2. **看板只數 SNDK**。08-30 登記的家族（NBIS/ANTH/BTC/ZEC/NEAR）在
   `_n_arb` 裡不存在 → 從網站看，那五個錄價器等於沒發生。已改：閘門
   仍以 SNDK 為準（它起算最早、09-04 判決），家族數與 BTC 對照組寫進
   note，看板不再低報這條線。
3. **「可捕獲美元」指標把結構性偏移算成錢——對照組抓到的**。
   BTC（兩邊深簿零費率、帶應≈0）報出全家族最高的 **$97.8/天**。資料
   是真的：HL 的 BTC 持續比 Robinhood 鏈貴 3-5 bps、**100% 的分鐘為正**。
   但它的收斂欄是 `episodes: 0`——溢價從不偏離自己的中線，**因為它就是
   中線**：結構性偏移，不是會回來的價差。鎖進去永遠拿不到錢。
   **判決閘門是對的**（收斂關過不了就不過閘）；**錯的是我加的報告欄**，
   band × fires × depth 沒有考慮收斂。已改成以收斂為前提，不收斂記 0
   並印出理由。修正後同時把 NBIS buy 與 NEAR sell 兩個假數字也歸零。

**這是對照組第一次真正發揮作用，而它抓到的是「我自己的儀器」**——
與 mistake.md 2026-07-29「新診斷先在答案已知的資料上跑」同一條規矩，
只是這次那個已知答案是「BTC 不該有肉」。若當初沒有把 BTC 放進家族，
NBIS/NEAR 的假數字會一路帶到判決日。

### 0.88 套利第二條收益軸：資金費率 carry（2026-09-01 開錄，使用者定調「不嚕空投、要靠套利獲利」）

**觸發**：使用者轉來 MOB (mob.exchange) 的空投教學。讀完後判斷：教學本身
是**刷積分**（MOB＋Ondo 對鎖賺三家積分，收益＝未來空投，EV 不可算），
與本線無關；但平台的 Delta-Neutral 策略卡自己寫著 **「The funding spread
is the return」**，兩腿是 **Hyperliquid ↔ Lighter RH**——**正是我們已在錄
的那兩條腿**。使用者定調不做空投、要靠套利獲利，於是只取這一條情報。

**兩種收益要分開記**：價差要**收斂**才付錢（現有四關管的就是它）；
資金費率是**持有**就付錢。前者已錄兩天到四天，後者連資料都沒有。

**已上線**：
- `entropy_arb/funding.py`（本機補丁）：每 5 分鐘拉兩邊費率。
  **單位是這裡唯一不能錯的東西**——HL 是**每小時**（142/233 幣停在
  1.25e-5 基準）、Lighter 是**每 8 小時**（錨定：同端點 `exchange:
  "binance"` 的列群聚在 1e-4 ＝幣安 0.01%/8h）。全部正規化成 **bps/8h**，
  `diff = hedge − entropy`（正＝多 entropy／空 hedge 收錢）。
- 錄價每分鐘多寫四欄（entropy/hedge/diff/age）；poller 掛掉留空白，
  **永不為 funding 掉一分鐘**。
- **HYPE 加入家族（第七條）**：09-01 截面費率差最大（+10.95%/年）。
- `premium_verdict` 加 `funding_stats`（中位／p10-p90／同號比例／年化）
  ——**報告用不進判準**。08-28 凍結的四關是關於價差的，不因為出現第二種
  收益就事後擴充（那正是預註冊要擋的事）。

**修掉一個會吃資料的 bug**：schema 輪替原本是 `os.replace(path,
path+".old")`——**第二次輪替會覆蓋第一次**，08-28 那 257 分鐘會被靜靜
吃掉。已改時間戳輪替，判決端改 glob 所有 `*.old`。驗證：輪替後分鐘數
連續（SNDK 6031、其餘 2909，與輪替前對得上）。這是 mistake.md 08-29
「輪替要枚舉每個消費者」的延伸——**這次的消費者是未來的自己**。

**截面 vs 序列（為什麼要錄）**：09-01 兩次量測相隔一小時就不同
（HYPE 的 Lighter 側 0 → 0.32、BTC 5.6e-5 → 4.8e-5）。**截面不能下注**，
一週後才知道那個差是穩定 carry 還是每天翻。判決仍照 09-04 / 09-06 的
價差四關；carry 是獨立的第二張表，屆時並陳。

### 0.89 訊號噪聲比（SNR）成為常設儀表（2026-09-02，外部文章觸發）

**概念採用、實務做法否決。** SNR = Var(μ)/Var(ε)；因為 Var(μ)/Var(r)=R²，
**有預測值時可直接算 SNR = R²/(1−R²)**，不需要文章建議的潛在變數估計。

**兩個必須擋掉的錯**：
1. **文章的滾動均值估計式有系統性偏誤 ≈ 1/(k+1)**。實測純噪聲、k=20：
   **4.72% ± 0.38%**（理論 4.76%）——**比本系統的真實 SNR 還大**。用它量
   V7 會量到估計式自己的偽訊號。故一律用封閉式，且**每次報 null 底線**。
2. **定義的分子是「期望報酬的時間變異」**：μ 恆定為正的策略 SNR=0 卻有
   年化 Sharpe 3.95。所以它是**擇時診斷不是獲利診斷**，放在 IC/AUC 旁邊，
   **永不進 sizing**（估計量調倉位已三次全滅：實測 Kelly 18 筆算出 20x、
   confidence 加權、calibrated-edge；2x 是 ruin 數學不是最佳化目標）。

**首次讀數（乾淨 WF OOS n=3,696，`research/snr_monitor.py`）**：
| | 值 | 洗牌 null |
|---|---|---|
| SNR(Pearson) | **0.008%** | 均值 0.026% / p95 0.101% / **p99 0.166%** |
| SNR(Spearman) | **0.218%** | 同上 |

**誠實判讀：線性關聯（Pearson）低於洗牌 null 的 p99——它自己不構成證據；
只有排序關聯（Spearman）勉強越過。** 這與既有事實完全自洽（乾淨 AUC
0.5412、Gate A 乾淨版 FAIL、+7bps/筆 vs ~100bps 波動），也解釋了為什麼
這個專案的儀表一直是 rank 類（IC/AUC）而不是 R²——**edge 在排序上，不在
線性幅度上**。

**接線**：`snr_monitor.py`（獨立可跑，含純噪聲對照）＋月度復驗新增
§1b（SNR 與洗牌 null 並列，低於 p99 就上 flag）。

### 0.90 排程與複驗紀錄上牆（2026-09-02，使用者：「方便管理紀錄」）

**病灶**：排程散在**四個互不相通的地方**——Windows 工作排程器、一支
內含十幾個 publisher 的 .bat、新鮮度看板、以及一個月度報告資料夾。
「東西有沒有在跑、它判了什麼」只有在操作者自己的機器上才回答得出來。

**已上線**：`research/ops_board_publish.py` → `ops_board` 表 →
`/public/ops-board` → 網站 `/system` 三張表：
1. **排程作業**（6 項）：頻率、內容、**產物年齡**、以及「停掉會怎樣」。
   健康一律由**產物年齡**推導，不看排程面板的燈——面板顯示的是「有沒有
   被觸發」，那正是 96 天與 29 小時兩次事故的形狀。
2. **產物新鮮度**（28 條）：離過期最近的排最前面。
3. **月度複驗史**：每次的判決＋AUC/IC/SNR，**含拒判（STALE-DATA）與推送
   失敗的標記**——只顯示 PASS 的紀錄不是紀錄。

**順手補掉兩個真缺口**：
- **新鮮度看板從來不監控自己**。它若停跑，所有列會凍在最後一次好值、
  面板全綠——正是這支程式存在要抓的病，套用在它自己身上。已加自我監控。
- **月度複驗報告沒被監控**（35 天上限）。加進去時第一次就紅，查出是
  `glob` 取**最舊**檔案，而報告是**累積**的（六月那份永遠最舊）——與
  09-01 掃描器每日輪替同一個坑，改 `glob_newest` 後恢復。

**結構性守衛又抓到我一次**：新端點的 docstring 寫在 `rate_gate` 之前，
`test_every_public_route_calls_rate_gate` 立刻紅並點名該路由。守衛不是
擺設。

### 0.91 站內資金費收租（2026-09-02 登記，產品端請求；**判決 09-30**）

> 產品端請求書 `../jarvis/產品端請求_站內資金費收租_20260902.md`；
> 研究端回覆 `../jarvis/研究端回覆_站內資金費收租登記_20260902.md`。
> 產品端建議 §0.80，**該號已被 08-30「每根 Strong 都進」佔用**，改 §0.91。

**問題**：Bitget 站內「現貨多＋永續空」收資金費，BTC/ETH 的年化分佈長什麼
樣——中位多少、翻負佔比多少。原本只有 08-20 一次抽樣（BTC +10.9%）。

**凍結判準（一字未改產品端提案）**：窗口 09-02 → **09-30**；BTC 與 ETH
**各自**滿足「結算年化中位 ≥8%」∧「翻負 <25%」，且**前後兩半各自成立**。
任一不過 → 關線，產品端不寫現貨 adapter。8% 的推導（對投入資金約 6.7%、
扣一輪 6–12bp 與翻負期）站得住；**在登記當下把門檻挪到感覺比較好的數字，
正是預註冊要擋的事**。

**⚠ 先驗（判準凍結後才拉的 180 天回填，每幣 270 次結算）**：

| | 中位年化 | 翻負 | p10/p90 |
|---|---|---|---|
| BTC | **+4.05%** | 21.5% | −3.9 / **+10.9** |
| ETH | **+3.67%** | 21.5% | −2.4 / +10.9 |

**中位只有門檻的一半，而且 08-20 那次促成提案的 +10.9% 正好是分佈的
p90**——動機來自一次尾部抽樣。**不改判準、不提前關線**（回填是背景不是
判決），但期望值已經講清楚：**這條線目前看起來會 FAIL**。

**已落地（與登記同 commit —— §0.44 的教訓：只寫在文件裡的預註冊等於沒有）**：
`basis_recorder.py`（10 分鐘一輪，BTC/ETH 判決＋8 個山寨背景；每列含
fundInterval **不寫死 8**、mark/index → 溢價 bp）、`basis_verdict.py`
（期限前拒判；結算次數以 (symbol, funding 週期) 去重，用取樣次數當分母
會讓安靜期灌爆分佈）、班車、看板 0.91、freshness（1 小時上限）。
**不擴充 `funding_collector`**：`funding_rates` 沒有 mark/index，而溢價
是這條線的另一半肉。

**兩個產品端問題的裁示**：
1. **對沖 vs 獨立**：兩者都是，**分開判、同一批資料**。獨立照 8%；對沖的
   門檻低得多（任何正 carry 都抵銷網格庫存成本）但**規模被網格庫存綁死**。
   對沖列為附錄讀數，**永遠不能拿來救一個 FAIL 的獨立判決**。
2. **hedge 觸發單四條規格**採為底線，但**判決前不實作**。

**同日自踩一坑並修**：09-01 把錄價輪替改成時間戳之後，`prereg_publish`
的 `_n_arb` 只認字面 `.old`，看板數字從 ~6,000 掉到 **914**——
**mistake.md 08-29 一字不差的重演，而且發生在修好輪替的同一個 session**。
已改 glob 所有 `*.old`（並補上 HYPE）。教訓再寫一次：**輪替一份檔案時，
枚舉每一個讀它的人；計數類下游的錯法不是壞掉，是從零重數。**

### 0.92 Gate F 判決（2026-09-02 使用者：「Gate F 判決好了可以開跑了」）——變體 B **FAIL**，C/D 連坐作廢，A 正式軌道繼續

**跑的是什麼**：兩支預註冊計分器，不改一行、從 repo 根目錄用排程的方式呼叫。
變體 B 走 `shadow_engine.py --gate`（每小時 B 列＝權威值，mistake.md 08-26）；
變體 A 走 `sweep_forward.py`（先 `fetch_klines.py` 補到 09-02 12:05，22,319 根/幣）。
判準是 07-28 凍結、同日收緊為日聚類 CI 的那三條：**n ≥ 1400 ∧ 日聚類
bootstrap CI95 下緣 > 0 ∧ ≥ 6/9 幣正**。這一節只記結果，不動判準。

**變體 B（29 幣·淺穿越 ≤0.25 ATR·四種池，凍結 07-29）：FAIL**

| 判準 | 凍結門檻 | 實測 | 結果 |
|---|---|---|---|
| 樣本 | n ≥ 1400 | **1428** 筆已平倉 | ✅ 達標 |
| 日聚類 CI95 下緣 | > 0 | **−0.0942**（meanR −0.0098，WR 55.8%）| ❌ |
| 幣別正數 | ≥ 6/9（B 用 29 幣：15/29 正）| core9 子集 6/9 | 一項勉強、一項不過 |

計分器印的是「accumulating」——那是因為它的狀態列舉只有 PASS / accumulating，
**沒有 FAIL 這個字**；樣本門檻已過而 CI 下緣仍為負，依凍結判準就是 FAIL。
穩健性：剔掉 07-28 那 37 筆早於 B 凍結日的（均值 +0.088）B 反而更差，判決不變。

**連坐（§0.43 層級規則 A ⊃ B ⊃ C ⊃ D）**：B FAIL → **C（767/400，CI 下緣
−0.128）與 D（371/400，CI 下緣 −0.149）一律作廢**，各自的 400 筆時鐘停止，
不得因為「C 自己看起來如何」而復活。E（BTC only，n=31，+0.36）樣本過少不判，維持。

**變體 A（Gate F 正式軌道，core9·無濾網，凍結 07-28）：未判，繼續累積**
forward pool **n=292/1400**、meanR +0.0467、WR 60%、日聚類 CI95
**[−0.0870, +0.1748]**（含零）、**5/9 幣正**（BTC +0.14、ETH +0.15、DOGE +0.05、
LINK +0.00、AVAX +0.35；SOL/BNB/XRP/ADA 負）。兩個質性條件現在都不成立
（08-08 曾兩條都達標，樣本一多就退回——CLAUDE.md 早有註記）。以實測
~8 筆/天，1400 筆約在 **2027-01 中**。

**提前止損條款（§0.5：quasi-forward + forward 合計 meanR 顯著為負 → 收攤）：
未觸發。** quasi-forward（07-11 起）n=418 +0.0297 CI [−0.069, +0.126] 7/9；
forward +0.0467。兩者皆正、CI 含零，「顯著為負」不成立，A 線照走。

**揭露但不採用的切片——「真前瞻」子集**：CLAUDE.md 有一句「只有
`first_seen < exit` 的才是真前瞻，判 Gate 只能用前者」。照它切：n=1169、
meanR **+0.0769**、CI [+0.024, +0.127]、WR 61.2%、23/29 幣正——看起來像 PASS。
**不採用，理由兩條**：(a) 被濾掉的 259 筆不是「回填的歷史」，是兩種東西：
排程斷線後的補記塊（均值與同日前瞻列一致，無偏）＋**同一小時內成交即停損的
輸家**（meanR ≈ −1.0，first_seen 與 exit 間距中位 9.3h、最小 5 分鐘）——
`first_seen < exit` 這條濾網剛好把最快死的那批交易濾掉，是存活偏誤，不是
前瞻性；(b) 註冊的計分器（`gate_stats` 與 `sweep_forward.py` 的
`fill_ts >= FREEZE`）本來就用**全部**已平倉列，事後換一個讓結果變好的子集
＝ C/D 陷阱（§0.43 註記過「不得因此改用 C/D」，同一條紀律）。CLAUDE.md 那句
話本節一併改正。

**附件（§0.54 規定：附件不是判準，只供解讀）**
- B 池型：pdh_pdl +0.004 (n=348)、session +0.005 (725)、swing −0.019 (299)、
  **pwh_pwl −0.232 CI [−0.386, −0.039]** (56)——唯一離零的格是負的
- B regime：RANGING +0.039、TREND_DOWN +0.080、NEUTRAL +0.026、
  **TREND_UP −0.165（WR 46.9%，5/29 幣）**——與 §0.54b「只有上漲趨勢是逆風」
  一致，但這是解讀，不是把 B 救回來的理由
- B 籃子：core9 n=437 +0.027 CI [−0.067, +0.121]；added20 n=991 **−0.026**
  CI 下緣 −0.112、9/20 幣正——擴籃那 20 幣拖垮整體
- B 時序：前半 +0.054 / 後半 −0.073；07 月 +0.026 (156)、08 月 −0.015 (1202)、
  09 月 +0.007 (70)；停損佔 13.2%
- **§0.57 進場時機成本**：以上全是「在價位成交」的帳，可實現版本每筆再少
  ~0.13 R——A 線就算將來 CI 過零，也要先把這條扣掉再談 live

**判決後的後果**
1. **策略 #2 上線候選只剩 A**（無濾網原始版）。B 那條「淺穿越濾網有價值」的
   敘事到此為止；§0.43 註記的「不加濾網比加濾網穩」被 1428 筆證實。
2. 產品端（jarvis RaidBot）吃的 `raid_signals_live` 是變體 B 的訊號——
   **對外顯示狀態要改成「FAIL / 已陣亡」**（CLAUDE.md 公開面規則：研究結論上牆
   必須標狀態），是否停供由使用者裁定，本節不動供給。
3. 不得做的事：不得放寬 B 的門檻、不得改用真前瞻子集、不得拿 C/D 的數字
   單獨談、不得用 regime/池型的好格重組一個「B′」再開新時鐘——要開新變體
   一律先寫進 §0.43 再計分，且從零起算。
4. 看板／網站：`prereg_publish` 的 B 進度應顯示 FAIL 而非 1428/1400 累積中；
   `shadow_engine.gate_progress` 狀態列舉補 FAIL（顯示層修正，不碰算術）。

- [x] 變體 B 判決：**FAIL**（2026-09-02，n=1428，CI 下緣 −0.094）
- [x] C/D 連坐作廢（2026-09-02）
- [x] 提前止損條款檢查：未觸發（quasi +0.030 / forward +0.047，皆含零）
- [ ] 變體 A 繼續：292/1400，~2027-01 中到期，中途 checkpoint 禁提前放行
- [x] 顯示層（2026-09-02/03 完成）：`gate_stats` 狀態列舉補 FAIL（C/D 標籤標
      「作廢·連坐」）、`sweep_forward.py` 輸出 A 的判決產物 JSON（該數字的唯一
      擁有者）、prereg 看板 B 轉 settled「已陣亡」＋A 新開一列（292/1400）、
      網站 product-site `e733b5e`（FAIL/作廢 chip、B 列改用凍結判決數字、
      判決全文上牆、zh/en 文案）。注意 `/public/sweep-status` 是在 flow_system
      image 內重算的，**要 flow_system 部署才會跟上**（現停 08-29 的 1270/1400）
      ——網站因此自己寫死已定案的判決數字（已定案的判決不會再動）。
- [ ] 產品端通知：RaidBot 訊號源（變體 B）判決 FAIL，供給去留由使用者裁定

### 0.93 雙層嵌套馬丁格爾 LP 梯子（2026-09-03 使用者丟規格來研究）——分配形狀不是重點，場館與區間寬度才是

**規格**（使用者提供）：總資金 T、比例 r、m 個大區間 × n 個小格，第 j 大區間
第 k 格的資金 c(j,k) = Y1 · r^(j+k−2)；例子 T=100k、r=1.5、m=5、n=10、
100,000→50,000。研究程式：`research/lp_ladder/nested_martingale.py`
（代數 + BTC 1h 路徑重播，未擬合任何參數）。

**一、代數（完全確定，不靠樣本）**
- 公式自洽（加總 = T），價格網格是 **log 等分**（五個大區間的 log 寬度都是
  0.1386 = ln2/5）——這部分寫得對。
- **錯齒不是特性，是兩個幾何級數相乘的副作用**：c(j,k) 可分離成
  （外層 r^j）×（內層 r^k），所以每個大區間交界處資金**除以 r^(n−2)**
  （r=1.5、n=10 時是 26 倍）：第 10 格 2,572 → 第 11 格 100.4，而兩格的價格
  只差 **1.4%**。規格第 1 節聲稱「隨價格下跌資金分配增加」——**實際上不是
  單調遞增**，有 4 次下跳。
- 錯齒的淨效果是把錢從深尾**拉回來**（最深 20% 區間佔 38.4%，而同端點的
  單層梯子是 66.2%）——方向是好的，**但用一個市場不存在的斷點達成**；同樣的
  「軟化」只要把單一 r 調小就能平滑做到（對照組 single_mf：同總金、同區間、
  **同資金加權平均進場價 62,373**，等效比例只要 1.056）。
- 內外兩層用**同一個 r** 是不對稱的：區間內 13% 的跌幅要求單格放大 38 倍，
  而整段 50% 跌幅只讓首格放大 5 倍——市場沒有任何理由長成這個形狀。

**二、路徑重播**（BTC 1h，2024-02→2026-09，108 個滾動起點，180 天窗，
區間 = 當下價 → −50%）：
- **資金閒置 78.6%**。窗內最大回撤中位數只有 **24.1%**（p95 47.2%），而規格
  把 **38.4% 的錢放在 −33%～−50%**——那些格兩年半裡幾乎沒被碰過。真正的
  成本不是尾部風險，是**機會成本**。
- 周轉（資金加權的格子來回次數）1.92 T/180d；底存 MTM 平均 −1.15% T。
- **打平所需費率 = 60 bps / 每次格子來回**。這個數字才是判決點：
  - 當 **AMM LP**（v3-style，來回存貨損益約零，只賺 fee tier 5～30 bps）→ **不過**。
  - 當 **交易所網格**（下邊限價買、上邊限價賣，每次來回賺一個格寬
    ≈ **139 bps** 減手續費）→ **過，而且有兩倍以上餘裕**。
  → **同一個分配形狀，在兩種場館的結論相反。先回答「哪裡執行」，再談 r/m/n。**
- 把區間從 −50% 收到 −25%（90 天窗）：閒置 78.6% → 51.7%、周轉 1.92 → 8.61、
  打平費率 60 → **18 bps**。**區間寬度是一階旋鈕，r/m/n 是二階**。規格第 6 節
  的「波動大就調 r」是在調二階旋鈕，而且那正是 threshold-sweep 陷阱
  （mistake.md 2026-06-20）。

**三、規格缺了一個非有不可的東西**：沒有出場／重平衡規則（第 5 節只寫
「重平衡機制」四個字）。沒有它，PnL 本身沒有定義——上面所有數字都是
「固定梯子 + 期末 MTM」這個暫定規則下算的。要評判真實績效，出場規則必須
先凍結再計分（同 Gate F 的預註冊紀律）。

**四、馬丁格爾本身的紀律線**：這條策略與 jarvis 產品端的網格／雙錨 DCA
同屬一個家族（高勝率、肥左尾）。專案既有的硬規則適用：未過自己的 Gate
之前不碰真錢；不得因為回測好看就上線；區間下緣的尾部（價格跌出區間）
是這類策略唯一的死法，必須先寫死「跌出區間怎麼辦」才能談部署。

**五、場館問答已結（2026-09-03，使用者：「掛哪個才有肉吃」）——網格，AMM 是負的**

同一個梯子、同一批路徑，只是把「一次來回」用兩種場館的方式計價
（`nested_martingale.py` 現在同時輸出兩欄）：

| 區間／窗 | 格寬 | AMM 淨值/年 | **網格淨值/年** | 網格 p5 | 網格最差 | 跌出區間 |
|---|---|---|---|---|---|---|
| −50%／180d | 1.40% | −2.15% | **+3.03%** | −0.43% | −8.07% | 1.9% |
| −25%／90d | 0.58% | −4.71% | **+12.50%** | −1.49% | −40.6% | 29.8% |
| −15%／30d | 0.33% | −13.83% | +2.74% | −87.4% | −243% | 23.3% |

（均為總資金的年化；最差欄是單一視窗年化後的數字，−40.6%/年 = 該 90 天
視窗實際 −10%。）

**結構性理由，不是這個樣本的偶然**：網格每次來回賺的是**自己的買賣價差
（一個格寬 58～140 bps）減兩次 maker 費**；AMM 把那個價差交給套利者，只
收池子的 fee tier（5～30 bps）。**存貨風險完全一樣，收入差 5～25 倍。**
打平所需費率 60 bps（−50%）／18 bps（−25%）就是這條線畫在哪裡。

AMM 欄是**下界**（只數完整格子來回，沒算格內來回的手續費），但下界不影響
結論的方向：AMM 永遠收不到格寬本身。要讓 AMM 翻正，需要在已部署資金上
拿到 >10～13% 的 fee APR，那要求你在高量池子裡佔到大份額——而那正是競爭
把 APR 壓下去的地方。唯一能翻盤的是**外生補貼**（流動性挖礦／積分），那
是農場決策不是這條策略。

**嵌套形狀在網格模式下的真實貢獻是尾部不是報酬**：−25% 那組，nested
+12.50% vs 均分 +11.67% vs 單層 +12.97%——**平均值分不出來**，但 p5 是
−1.49% vs −24.24%。機制很乾淨：馬丁形狀把大部分錢留成現金直到價格真的
跌到那裡，所以下跌途中曝險小。這是這份規格最站得住的地方，值得記著。

**還沒解決的殺法**：−25% 區間有 **29.8% 的視窗價格跌出下緣**——出了區間
兩種場館都停止賺錢，只剩存貨尾巴。這就是為什麼「跌出區間怎麼辦」必須先
寫死才能談部署。

**歸屬**：這條線屬於 jarvis 產品端既有的網格家族，不是新產品——正確的下一
步是把馬丁分配當成現有網格的一種 sizing profile 去比，不是另外造一台。

**待辦**（依序，不跳階）：
- [x] 場館與訂單型態：**交易所網格**（2026-09-03 判定，見上表）
- [ ] 出場／重平衡規則凍結（寫進本節，先寫再計分）
- [ ] 若走 AMM：需池子的逐區間成交量與競爭流動性（subgraph），否則 fee 端
      只能給下界；若走網格：直接用本 repo 的 kline 快取就能算到底
- [ ] 多標的與多時期穩健性：現在只跑 BTC 且視窗重疊，不能當統計證據

### 0.83 產品資安規劃（2026-08-31 使用者轉入威脅模型，「等等來規劃」——先答前置五問）

使用者轉來的威脅模型排序（訊號竄改 > 訊號竊取 > 用戶交易所 key > 帳號
接管 > 一般 Web 漏洞）方向正確。規劃前的五個前置問題，答案從系統現況：
1. **技術棧**：全 PaaS——flow_system 5 個 Railway 服務（Python/Flask）、
   agent-mcp（Railway）、product-site（Next.js/Vercel）、jarvis（Railway，
   Node）。無自架 VPS。
2. **訊號推送管道**：product-site 經 agent-mcp `/public/*`（無簽章）、
   兩個 Telegram bot、jarvis 經 `FLOW_SIGNAL_URL`（signal-feed，無簽章）。
3. **用戶交易所 key**：**有**——jarvis 存用戶 Bitget key（`keys.enc`，
   vault 加密，見 tenants.js）。依威脅模型這是最高優先項：加密方式、
   密鑰隔離、只交易不提幣權限、永不回傳前端、存取日誌——待審計。
4. **付費機制**：jarvis 有 `src/payments/`（細節待盤點）。
5. **訊號生成端與 Web 端**：不同服務但同一個 Railway 帳號／同一 MySQL
   ——「分離」只到 process 級，帳號級未分離。
規劃時的既有資產：agent-boundary（唯讀邊界、CI 強制）、公開面規則
（不出金額/內部）、admin token guard（2026-07-06）。

**實作完成（2026-08-31，flow_system 半邊）**：
1. **訊號完整性**：(a) 四條可執行 feed（signal-feed／raid-signals／
   raid-pending／raid-outcomes）加 `X-Signal-Signature: v1=<HMAC-SHA256>`
   回應簽章（`indicator/agent/security.py`，金鑰 env `SIGNAL_SIGNING_KEY`
   ≥16 字，未設則無標頭不破壞消費者）；(b) **tracked_signals 雜湊鏈**
   （`research/signal_audit_chain.py`→`signal_audit_chain` 表，2,248 節
   已建、`--verify` 全鏈完整；每小時班車自動延鏈）——歷史被改會斷鏈，
   「這不是我發的」可證。誠實界定：鏈只蓋發布時不變欄位（correct/
   actual_return 是 4h 後回填，設計如此）。
2. **防批量竊取／撞庫**：全部 21 條 `/public/*` 路由掛每 IP 滑動窗
   速率限制（120/分，login/register/waitlist 收緊到 10/分），
   **結構性測試強制**（`tests/test_agent_security.py`：新增 public 路由
   漏掛守衛即紅，反向證明過——facade 守衛同款）；歷史窗口既有上限
   200 筆維持。
3. **jarvis 半邊**：審計請求 11 問已交
   （`../jarvis/研究端資安審計請求_20260831.md`），**#A 用戶 key 五問
   最優先**；含簽章驗證對接規格（降級告警：見過簽章後消失=攻擊形狀）。
   **09-01 進度**：產品端自主出了 **V1.57.0 實盤安全批#1**（模式互斥
   守衛四處補齊、/admin/stop-all kill switch 補全 raid/v7/surf、admin
   XSS 雙層修、admin/RESEARCH_EXPORT_TOKEN 改 timing-safe 比對、cookie
   Secure、註冊節流 env 化）——方向與審計請求一致。**尚欠**：11 問
   正式回覆（尤其 #A key 五問）、feed 簽章驗證端（src 尚無
   X-Signal-Signature 引用）。順帶：decay 已補回（V1.56.0 照規格逐條、
   雙模式、快照續數）、用戶自身進出場已上圖（V1.55.0）。
4. **使用者動作項（2026-08-31 列入待辦，未完成前簽章不生效）**：
   - [ ] **設金鑰**：Railway 的 agent-mcp 服務與 jarvis 服務各設**同值**
     `SIGNAL_SIGNING_KEY`（≥16 字隨機字串，例如 `openssl rand -hex 24`
     產一顆）。設完簽章立即生效；jarvis 收到審計文件後補驗證端
   - [ ] **帳號 MFA**：GitHub、Railway、Vercel、Bitget／OKX 全開——
     這層任何程式碼都替代不了。
   檢查方式：金鑰設好後 `curl -sI <agent>/public/signal-feed | grep
   X-Signal-Signature` 有標頭即生效。

### 0.79 模型 maintenance refresh —— **到期 2026-10-07**（2026-08-30 排定）

現行 direction 模型 2026-08-08 部署（`indicator/model_version.py:
get_current_model_deploy_date()`），CLAUDE.md 重訓節律上限 **60 天** →
**10-07 到期**；使用者 08-30 指示「10 月初排」。目標窗 **10-01 ~ 10-07**。

**流程（例行維護，不是研究，照 08-08 先例）**：同特徵、同超參數、同 tier
定義，只換資料 → `research/validate_direction_refresh.py` 四關（G1 WF AUC
in band / G2 對中 |mean|<0.5×floor 且兩尾各 ≥2% 越過 floor / G3 解碼重放
DOWN 佔比 30-70% / G4 committed buffer 必須為空）+ G5 可達性（兩側各有
bar 構得到切點）→ 部署。warmup buffer 從 DB 重建（08-11 修法），不 seed。
**部署即新的 sample floor**（`sample_floor()`），所有 live 績效查詢自動
截斷；Q2 時鐘（§0.60）的樣本 floor 不受影響（訊號層，跨模型版本需標明）。

**提前觸發條件**（任一成立就不等 10-07）：復驗 §2b 亮 LEVEL-DRIFT；
clocks 2e 開火方向 ≥85% 單邊連續兩週；floor headroom 掉到 <0.5 std。
08-30 讀數：21d UP 37 / DOWN 52、headroom +0.98 std（DOWN 側最近）、
08-05 復驗 PASS（AUC 0.5848）——目前無提前理由。

### 0.55 產品端交接(2026-08-24,jarvis → flow_system)——四個發現、三個待研究

> 來源:jarvis 產品端 08-21~08-24 的實盤/回測工作。**這裡只寫需要研究線
> 判決或修正規格的部分**;產品端已自行處理的執行層問題(保護單對帳、
> 費率精確化、帳本雙計等)不佔研究資源。原始數據在 jarvis 的 CLAUDE.md
> 「磨坊樣本外審計」與「2026-08-23 追加判決」兩節。

#### A. 【規格缺口】dirCap(路線 B)對獵取形同虛設

路線 B 用**名目**計量同向曝險(≤ 2× 總預算),但獵取的風險載體是
**筆數 × 每筆風險**,名目極小:預算 100U、每倉名目 ~17U、5 倉合計 85U,
離 200U 的 cap 很遠 —— **實跑 13 筆從未咬合一次(capBites n=0)**。

2026-08-21 實例:9 筆做空在 10 小時內開出,分散在 5 個幣(BNB/SOL/ETH/
DOGE/AVAX),但它們是**同一個賭注的九種說法**(「這波漲勢會失敗」),
crypto 趨勢日高度同步 → 同生共死,單日 −3.7% 預算。dirCap 全程沉默。

**請研究端裁示**:路線 B 的規格是否應為策略類型分別定義計量單位
(名目 vs 風險筆數)?產品端不自創第二套風控閘。

#### B. 【方法學要求】Gate F 判決必須按 regime 分層

獵取變體 C 的 regime 條件績效(shadow log 全樣本、BTC 日報酬 ±1.5% 分類,
**分類器粗糙,僅供方向性參考**):

| 行情 | n | 平均 R | 勝率 |
|---|---|---|---|
| 震盪 | 218 | **+0.083** | 62% |
| 下跌趨勢 | 10 | −0.118 | 30% |
| **上漲趨勢** | 68 | **−0.330** | 38% |

實盤 13 筆的平均是 **−0.333R**,與上漲趨勢桶完全吻合(那段期間確實是
上漲)。**若總驗期的 regime 組成偏向某一側,判決結果會由運氣決定。**
建議 Gate F 至少報告分層後的 meanR 與各層 n。

註:此結論與 §0.54 的「變體 B 對 regime 敏感度只有 core 一半、TRENDING
桶 meanR +0.069(仍為正)」不一致。兩者差在:變體(C vs B)、分類器
(日報酬 vs 逐幣 ADX)、樣本(25 天 vs 全歷史)。**以研究端的口徑為準**,
此處只主張「要分層看」這個方法學要求。

#### C. 【預註冊提案】變體 F:方向對齊濾網

**動機**:regime 分類是後照鏡問題(ADX(14) 在 1h 線需 ~14 根確認,而獵取
持倉僅 8h)。「方向對齊」把同一件事降級成無歧義的測量:**訊號方向是否
與中期趨勢一致**(價格 vs EMA,一條線,沒有門檻爭議)。

探索性結果(shadow log,6 個可取得 K 線的幣,n=625):

| EMA | 對齊 n | 對齊 meanR | 逆勢 n | 逆勢 meanR |
|---|---|---|---|---|
| 6h | 41 | +0.125 | 584 | −0.045 |
| 1天 | 20 | +0.434 | 605 | −0.049 |
| 2天 | 40 | +0.130 | 585 | −0.045 |
| **1週** | **96** | **+0.216**(t≈3.1) | 529 | −0.079 |

四個週期方向一致。**但這是事後探索**(看到 regime 結果才想到測、一次測
4 個週期、同一批 625 筆、25 天樣本),不能當結論。

**建議預註冊規格**(產品端不實作,等判決):
- 週期固定 **1 週 EMA**,不掃描;樣本用**全歷史**非 25 天
- 判準:對齊組 meanR 顯著 > 逆勢組(t>2)且**逐年不反轉**
- **已知代價**:對齊訊號僅佔 6~15% → Gate F 的 n=1400 需 7~15 倍時間。
  判決時請一併裁示「值不值得為此放慢時鐘」

#### D. 【資訊價值】regime 分類器的邊際效益,取決於「可採取的行動」

假設有準確度 a 的二元分類器,用各策略的 regime 條件績效模擬:

| 策略 | 可採行動 | 無分類器 | a=60% | 完美 | 60% 捕獲 |
|---|---|---|---|---|---|
| 獵取 | 做/不做 | −0.019R/筆 | +0.005R | +0.061R | **29%** |
| 磨坊 | 做多/做空 | +27U/月 | +75U/月 | +129U/月 | **46%** |
| V7 | — | 三 regime 皆正 | ≈ 無改變 | — | — |

**結論**:同一份資訊,能雙向下注的策略價值高得多(避免虧損 vs 把虧損
翻成獲利)。**若要投入 regime 研究資源,優先順序是網格/磨坊(行動空間大)
而非獵取(+1.5U/月/千U,且路線 A 已在處理)。** 另注意時間尺度差異:
獵取需要 8 小時級判斷,磨坊的月度數字實際上要求月度擇時 —— 後者若真
做得到,它本身比任何下游策略都值錢,不應拿去驅動刷量工具。

#### E. 【新增策略備案】磨坊(SurfBot)—— 產品端已上線,研究端請知悉

perp-dex-tools 的刷量循環經 clean-room 重寫 + 四層風控後上線(V1.27~1.34)。
**它不是 flow_system 的策略,不佔研究線資源**,但兩件事與研究端相關:

1. **它的身份**:beta 0.42(BTC)/0.44(ETH)、與市場相關 0.73~0.78、
   R² 0.54~0.61、**alpha 為負**(BTC −2.57%/月、ETH −0.60%/月)。
   即「0.7 倍多頭曝險 + 停損紀律」,錢來自市場不來自技巧。產品端已據此
   改寫文案(不得當收益策略賣)。
2. **一個不需要新預測能力的 regime 免疫路徑**:它的 EMA 濾網本身就是
   方向判斷器,多空兩個實例的開工條件天然互斥。回測(4.6 年、含滑價、
   無返佣)多空同時開:BTC +17.1U/月、**ETH +89.6U/月**(只做多為
   +23.6/+46.9)。**卡在單向持倉模式**(同幣互斥)。已測否決的替代方案:
   單實例穿越均線就翻向 —— 清場成本使勝率 68%→41%,價值恰恰來自「不清場」。

#### F. 【已有判決,記錄備查】V7 出場邏輯

使用者詢問是否改用階梯式移動停損(GG-Shøt 型)。查 `v71_exit_variants.md`:
9 種出場變體 OOS 比較,且 `exit_variants.py` 檔頭記載「V7 自己的出場研究:
11 個變體全部輸給單純 3×ATR,因為收緊停損會提早剪掉贏家」。**維持現狀。**
唯一未測的結構是**分批出場(TP1-4 scale-out)**,V3(half_at_1r)只在獵取
上測過、未在 V7 上測 —— 若要推進,建議用同一批 1,112 筆訊號做 paired 比較。

### 0.0 Magnitude 的 warmup buffer 仍是 in-sample seed（2026-08-13 盤點發現，未修）

08-11 修 direction buffer 時做了全系統掃描，找到**同一個病的第二個實例**：
`training_stats.json::pred_history`（mag 的 expanding-percentile buffer）現在
仍有 **300 筆 in-sample 種子**（mean +1.78 σ 單位）。`_persist_pred_buffer`
只存 dir、`_rehydrate_pred_buffer` 只重建 dir——mag 這條**從來沒有被持久化過、
也沒有從 DB 重建**，每次 Railway 部署照樣重置回種子，跟 dir 側三個月的病同構。

**為什麼判定為可以先放著**（不是忘了）：
- 單尾（`|pred|` 的 expanding percentile），失效方式是「分數整體偏高/偏低」，
  不會出現 dir 那種「一側算術上不可達」的死鎖
- `mag_score` **不進任何交易決策**：`use_mag_gate=False`，grep 過全 repo，
  它只在 `inference.py` 內部產生後供圖表面板顯示
- 告警裡的 `Mag: N.NNx` 是 `mag_pred` 本身（另一條路徑），不是 mag_score

**修之前要先解決的障礙**：DB 的 `indicator_history.mag_pred` 存的是**已經
× realized_vol 還原成報酬尺度**的值，而 buffer 吃的是 **σ 單位**的原始輸出，
後者沒有落庫。所以照 dir 那樣「從 DB 重建」在現況下重建不出來——要先讓
σ 單位的預測落庫（新欄位或新表），才談得上修。

### 0.52 縮帆線·ADX 減碼規則（2026-08-20 預註冊，跑數之前凍結）——第一個把生存條件層接進倉位的規則

> **「縮帆」（reefing，2026-08-20 命名）**＝這條風控研究線的總名，涵蓋
> §0.52（ADX 二元減碼）、§0.53（vol targeting 連續縮放）、clocks 2e-e
> （相關性預算）。命名取自航海：**不預測風暴，只在風大時收帆**——
> 不停航（停火＝放棄正 EV）、只減不加（w≤1）、靠當下風速不靠預報
> （ADX 前瞻力已被 ER/VR 雙度量證偽，活下來的機制是同期波動+22%與
> 慣性）。與天氣站一脈相承：天氣站量風，縮帆決定掛多少帆。

**動機（使用者提問，2026-08-20）**：「單邊行情網格跟流動性獵取會死很慘，
現有數據能不能做趨勢策略當對沖？」當天掃描的答案是**不能**——29 幣 ×
2.5 年 1h、含 5bps/邊、全部帶 cost=0 對照：時間序列趨勢 SMA50/200 年化
+2.98%／Sharpe 0.03（16/29 幣，等於零）、Donchian20 −32.96%、ADX gate 反而
把趨勢派從 +2.98% 打到 −0.66%（gate 換手吃掉 TRENDING 的薄利）。橫斷面
動量（168h/24h/tercile，唯一一組，未掃參數）全期 −5.16%；只看 TRENDING
是 +8.58%／Sharpe 0.53，但 **bootstrap CI [−5.75,+10.12] 含零，且逐年
2024 −3.94%／2025 −0.71%／2026 +39.98%** ——與 §longhorizon NO-GO
（2026-06-08，BTC 週動量五年只有 2026 為正）**同形狀、同年份、不同資料
與方法**，兩次獨立觀測指向同一個 regime 事實。**趨勢策略這條路正式第二次
關上**，不再以「換指標／換週期」的形式重開。

於是對沖只能從**減少曝險**下手，而不是找反向 alpha。「不做」不需要 edge。

**這條規則不新創任何統計量**：ADX(14) 25/20 與 TRENDING/RANGING 標籤全部
沿用 §0.49d 打對頭勝出、已達**二級（CI 離零）**的定義，一個參數都不調。

**凍結規則**
- 對 **SF（流動性獵取）**：用**該幣自己的** ADX（B-P9 是逐幣算的，8/9 幣）
- 對 **V7**：用 **BTC** 的 ADX（B-P8 就是 BTC）
- 檔位：`TRENDING → 名目 ×0.5`／`NEUTRAL → ×1.0`／`RANGING → ×1.0`
- **只減不加**。RANGING 不加碼——那是另一個 sizing 主張、沒有證據，且會把
  一條風控規則偷渡成報酬規則。

**為什麼是減碼 0.5 而不是停火**：TRENDING 桶對兩條策略**都仍是正的**
（SF +0.016R、V7 WR 58.3%），只是明顯較差。停火＝放棄一段正 EV，那是
對證據的過度反應。0.5 是唯一檔位，不掃 0.25/0.75（掃了就是 threshold
sweep，mistake.md 2026-06-20）。

**機制陳述（2026-08-20 當天補測，這條規則靠的不是「趨勢預測」）**
先說被**證偽**的：ADX 對未來 regime **沒有**前瞻力。兩個獨立的前瞻度量、
29 幣、n≈30 萬 vs 21 萬，全部指向同一結論——
- Kaufman 效率比（未來路徑多直）：TRENDING 0.2876 vs RANGING 0.2945，
  gap **−0.0069**（12h）／−0.0095（24h）／−0.0110（48h），CI 全含零，
  廣度 7/29 → 4/29 → 2/29
- Lo-MacKinlay 變異比（未來自相關）：TRENDING 0.8320 vs RANGING 0.8596，
  gap −0.0276、CI 含零、**廣度 1/9**

我當時提的「ADX 測不到路徑幾何但測得到均值回歸強度」假設**被 VR 證偽**，
兩個度量方向一致：ADX 說 TRENDING 的時候，接下來反而**稍微更**均值回歸。

活下來的機制是弱得多、但證據紮實得多的一條——**ADX 是同期狀態標籤，
而狀態有慣性**：
- forward 8h 實現波動：TRENDING **69.8bp vs RANGING 57.1bp（+22% 相對）**，
  **廣度 9/9 幣**
- 標籤持續性：`P(TRENDING→TRENDING, 8h)=77.8%`（base 48.2%）、
  `P(RANGING→RANGING, 8h)=68.6%`（base 32.1%）

所以減碼的正當理由**不是**「趨勢會傷害均值回歸」（那需要前瞻力，已證偽），
而是「**TRENDING 環境的波動高 22% 且會持續 ~8 小時，固定名目 = 風險被放大
22%**」。減碼 0.5 是把風險拉回可比水平，不是在賭 regime 會延續多久。

這也一併解釋了為什麼 B-P9 成立卻不矛盾：B-P9 是**同期條件化**，靠的是
波動聚類，從來不需要預測未來。**同一個儀表，用於收手有效、用於開火無效**
——當天的 ADX-gate 趨勢測試（+2.98% → −0.66%）是這句話的實證。

**凍結預測**
- A-P1：TRENDING 期間的**回撤深度** > RANGING 期間（這是使用者擔心的東西，
  也是均值報酬看不到的東西）
- A-P2：減碼版的**全期 MDD 較淺**，且**總報酬的損失小於 MDD 的改善**
- A-P3：減碼版的總報酬**會下降**（放棄了一段正 EV 的曝險）——預先寫死，
  免得事後把「報酬變少」讀成失敗

**判準（沿 §0.49d 兩級制，但對準風險不是報酬）**
- **一級（可接線 shadow 顯示）**：MDD 改善 ≥10% 相對值，且總報酬回吐
  ≤ MDD 改善的一半（風險換報酬的匯率要划算）
- **二級（可議進 executor / 產品端）**：MDD 改善的日聚類 bootstrap CI95
  離零，SF 側另需逐幣同號 ≥6/9
- **未達一級 → 這條規則作廢**，不得改檔位重跑

**記帳方式（誠實分段，這是本節最容易作弊的地方）**
減碼是對**既有訊號的權重變換**，可以直接重放 shadow log，不必等新樣本。
但 ADX 門檻是 08-17 凍結的，所以：
- **08-17 之前**的重放 = in-sample，**只列參考、不得作為判決依據**
- **08-17 之後**的重放 = 乾淨 forward，**判決只看這段**
兩段必須分開印。任何只報合併數字的版本視為儀器失效。

**今天已看過什麼（避免自欺）**：上面的趨勢掃描跑的是 SMA/Donchian/RSI/
橫斷面動量**自己的損益**，**沒有**跑過 SF/V7 的減碼版本。本節寫下時
A-P1/P2/P3 的數字一個都還沒產生。

**首跑記錄（2026-08-20，`research/adx_desize_shadow.py`）——無判決**

跑完立刻發現註冊的判準有**兩個會產生假陽性的洞**，當場補上。兩處都是
**收緊**（讓 PASS 更難），不是放寬——事後放寬是預註冊要擋的事，堵住
偽陽性通道則相反，但一樣必須留紀錄，所以記在這裡：

1. **缺對照組**。註冊的 A-P2 拿減碼版跟「什麼都不做」比。問題是 forward
   這段 SF 整體是虧的（base −8.981R），**任何**減碼都會同時改善報酬與
   MDD——規則會因為「少下注」而拿到 PASS。已加**曝險匹配的無差別減碼
   對照**（把每筆都乘上 ADX 規則的平均權重），要贏過它才算 ADX 有貢獻。
2. **缺樣本跨度下限**。註冊只寫了門檻沒寫窗口。掃單成交在時間上**高度
   聚類**，131 筆擠在 2.8 天＝一段行情，不是 131 個獨立觀測。已加
   `MIN_SPAN_DAYS=30` 守門，未達即拒絕判決。

**首跑數字（僅記錄，不構成證據）**

| 段 | n | 跨度 | base netR | ADX 減碼 | 無差別對照 | base MDD | ADX MDD | 對照 MDD |
|---|---|---|---|---|---|---|---|---|
| PRE（in-sample） | 716 | 19.9d | +38.47 | +24.04 | **+31.33** | −20.86 | −20.10 | **−16.99** |
| POST（forward） | 131 | **2.8d** | −8.98 | **−5.25** | −7.10 | −24.98 | **−16.53** | −19.73 |

**兩段的方向完全相反，這比任何一段的數字都重要**：
- PRE：TRENDING 是**最好**的桶（meanR **+0.1085** vs RANGING +0.0130），
  減碼因此**輸給**無差別對照（報酬 −7.3R、MDD 更深 3.1R）
- POST：TRENDING 是**最差**的桶（**−0.1357** vs RANGING −0.1002），
  減碼贏過對照
- 而 B-P9（08-17，更長歷史、core9）說的是 RANGING +0.075 > TRENDING +0.016

三個時期三種強度、其中一個符號相反。**A-P1 在 PRE 段被證偽**。這可能是
母體不同（shadow log 只有 07-28 起，B-P9 用全期），也可能 regime 條件
效應本身就不穩——現在分不出來，而**分不出來就不能接線**。

**狀態：等樣本**。forward 跨度到 30 天約在 **2026-09-16**，屆時重跑一次
定判決。在那之前這條規則**不得進 executor、不得進產品端**，`clocks`
只顯示不告警。若到期時 PRE/POST 仍然反向，判 FAIL 收掉——不改檔位、
不換 ADX 門檻、不延長窗口再試。

### 0.56 班車上雲（2026-08-21 啟動）——數據工程五弱點 #1，cutover 規則先凍結

**目標**：每小時班車（shadow recorder＋三 publisher＋pf 流）從這台筆電
遷到 Railway `research-train` 服務（`Dockerfile.research`＋`cloud_train.py`，
volume `/data`，Binance 走 data-api.binance.vision 鏡像——前人在
shadow_engine 裡已鋪好 `SWEEP_DATA_DIR`/`SWEEP_KLINES_BASE`，本次補齊
`level_types`/`pf_dry_intents` 兩個沒認 volume 的消費端）。

**兩段式切換（帳本是 Gate F 證據，絕不允許兩個 recorder 同時寫）**：
- **parallel（現在）**：雲端只跑 shadow_engine 進自己的 volume＋每小時
  發 `train_parity` 列（CLOSED 列數＋key hash）；筆電班車仍是 authority，
  每小時跑 `train_parity_check.py` 比對並落 log；新鮮度看板新增
  「cloud train parity」列盯雲端車活著
- **cutover 條件（現在凍結，達標才動）**：**連續 7 天 MATCH** → 筆電
  bat 降級為 puller-only、雲端 `TRAIN_PHASE=authority` 接管全部 publisher。
  持續 MISMATCH＝分歧未解釋，封鎖切換
- 帳本種子＝git 內 repo 副本（決定性重算會自我補齊，補齊列 first_seen
  較晚不計入 prospective——誠實的一次性成本，shadow_engine 註解原文）

**Railway 資源（已建）**：service `research-train`（GitHub main 自動部署、
`RAILWAY_DOCKERFILE_PATH=Dockerfile.research`、`TRAIN_PHASE=parallel`）、
volume `research-train-volume` @ /data、MySQL 引用變數五枚。

**#2 同供應商雙儲存（2026-08-21 當日完成）**：盤點發現雙儲存本體已在
先前修復中統一（backfill_all_parquet 與 backfill_historical_alt 都寫
market_data/raw_data，14 個小時級檔全部健康 1d 內）；實質殘留＝兩個
從未被排程的日頻檔死 130 天。處置：**F&G 復活**（`refresh_fng.py` 接
daily_collect step1.6，全歷史冪等重寫，已跑通至 2026-08-21 值 71——
保留原因：縮帆 phase-2 註冊候選，vol 控制後增量 +0.080/9 幣）；
**ETF 退役**（NO-GO 特徵＋不在部署 136＋不在 git，檔案歸檔至
research/results/retired/）。看板 coinglass 列轉綠。

**五弱點隊列（2026-08-21 使用者定調「一個一個修」）**：#1 班車上雲
（本節，進行中）→ #2 同供應商雙儲存退役 → #3 DB 表目錄 → #4 秘密管理
→ #5 慢性小病（編碼/CRLF/散落參數）。

### 0.55 統一產物新鮮度看板（2026-08-20 上線）——四次同族病的族滅工程

`research/freshness_board.py`＋獨立 Windows 排程 `FreshnessBoard`（每
6 小時，**刻意不搭每小時班車**——班車死掉正是它要抓的）＋clocks 2e-i
週報腰帶（看板自己 >12h 沒跑也會被週報點名——看門者的看門者只需要
一個，就是週報）。凍結註冊表 12 項：本機班車產物×3、DB 發布列×4、
雲端服務×2（indicator bars＋executor WS snapshots）、日頻×2、慢守衛×1
（tracked_signals ≥14d 無訊號＝解碼又鎖死）。**告警只在紅綠轉換時發**
（transition-only＋狀態檔去重），glob 類一律報**最爛的那顆**（2026-04-12
教訓：健康檢查蓋最脆弱成員）。缺表/缺檔＝RED 不是跳過。

**首跑判讀（2026-08-20）**：11/12 綠——整條班車、雲端、executor WS
當下全數驗證活著。唯一紅：`cg_etf_flow_daily.parquet` 停更 **129 天**
（同資料夾 fear_greed 停 4 個月）。**嚴重度已定性為衛生非正確性**：
查證部署中的 direction 模型 136 特徵**不含任何 etf/fng**（06-06 NO-GO
剪掉了），爛檔只餵 feature_builder_live 的死路徑。後續選擇（使用者
決定，不急）：(a) 把死路徑與檔案一起退役，紅燈消失；(b) 修 collector
讓檔案活回來（若 F&G 要當縮帆第二階段儀表，見 §0.53 排名 +0.080，
就得走這條）。在決定前紅燈**保留不隱藏**——藏已知壞線是下一條壞線
藏身的方式。

### 0.77 長持有出場：對面獵取當止盈（2026-08-28 使用者提案）——三關全掛，收案

**提案**（使用者原話）：「如果我不按時間出場，才有機會吃到一大段……
我現在做空，就等到下一次做多獵取……當作止盈」。九個測過的出場變體
**沒有一個持有超過時間上限**（pool_target 註解明寫不能延長持有），
所以這是真空白；V7 的 opp_signal 先例（回測 85.7% WR）給它機制動機。

**兩變體一起凍結**（`exit_opp_raid.py`，配對、同成本模型、n=12,023）：

| 出場 | meanR | Δ vs 基準 | Δ 日聚類CI | 廣度 | 平均持有 |
|---|---|---|---|---|---|
| baseline（8根+停損） | +0.0797 | — | — | — | 8 根 |
| **opp_raid**（對面獵取止盈） | +0.0587 | **−0.0210** | [−0.051,+0.011] | **2/9** | 31 根 |
| no_cap（純停損對照） | +0.1803 | +0.1006 | [−0.024,+0.236] | 9/9 | 220 根 |

**① opp_raid 死**：反向掃單平均 31 根就出現，把想吃的段提早剪掉。

**② no_cap 的 +0.10 沒過任何一關**（`exit_nocap_by_side.py` beta 檢查）：
- 分方向兩側皆正（LONG +0.082 / SHORT +0.119）→ 不是單純 beta，**但**
- **LONG 的加值全是前半段**（+0.275 → **−0.097** 翻負）——beta 換了個
  藏法：前半猛多頭抱 9 天躺著印錢，行情一換同一招虧錢
- 兩臂 CI 皆含零，無一成立

**③ 吞吐帳獨立判死**：變體 B 每幣年 ~110 個機會；抱 220 根擋掉 ~27 個
後續機會 → 年化 ~5.2R vs 基準 ~8.8R。**單筆翻倍、組合反輸**——
出場慢直接吃掉進場頻率，是出場設計的隱藏成本。

**出場戰役的總帳更新（~24 次嘗試）**：V7 11 變體全輸 3×ATR、獵取 9 變體
活 2 個皆「更早離場」、RL 4 折全輸、meta 出場 AUC 0.458、opp_raid 死、
no_cap 死。**出場沒有 alpha，只有紀律財**——meta 出場那次挖出原因：
「該不該平」的答案隨 regime 翻轉，無跨期穩定結構可學。

**獵取出場的最終形**：3.5 ATR 停損 + 8 根上限 + fail_fast（§0.59 同批
新樣本驗證中）。不再回來。

### 0.76 池價整數程度＋§0.71b 前瞻記帳上線（2026-08-28）

**§0.71b 堆疊數接進 shadow log**（時間敏感——判決日只能用前瞻值）：
`confluence_kinds` 欄位，**在開掃棒計數**（§0.71c 的 158→5 教訓）、
失敗降級空白永不擋班車（同 `_regime_cell` 契約）。驗證：凍結算術一字不差
（B n=1237 meanR −0.0160 前後同值）、已填 3255/3567、四桶分佈合物理、
零 WARN。從此每小時記的是前瞻值。

**§0.76 整數關卡（`level_roundness.py`）——通過完整判準的正面結果**：
定義凍結（前五位有效數字的尾零數，跨幣可比），權威母體 n=40,817：

| 桶 | n | meanR | 廣度 | |
|---|---|---|---|---|
| 0 不整 | 9,256 | +0.0714 | 8/8 | ✓成立 |
| 1 個零 | 25,231 | +0.0664 | 9/9 | ✓成立 |
| 2+ 個零 | 6,330 | **+0.0382** | 6/9 | ✓成立 |

**三臂全成立、單調、差 −0.0332R。第三個獨立確認「明顯的位置付得少」**
（§0.71b 堆疊 −0.050、D5 密度 −8pp 之後）。與民間直覺（整數=強關卡）相反：
強關卡人人看得到，人人看得到的付得少。列為進場模型候選特徵；
不動凍結規則。

儀器紀律又應驗一次：「先過已知答案」抓到**我自己的預期答案錯**
（80,000 尾零是 4 不是 3）——這關抓的是檢查者，不只是被檢查者。

**磨坊現況**：研究端可獨立做的（何時切向）已於 08-24 判 NO-GO 且用的
就是凍結四格；Q1/Q2（四格月損益、regime 取代 EMA25）**卡在產品端資料**
——需要他們匯出逐筆（open/close 時間、幣、方向、淨損益），已在磨坊裁示
§三寫明格式，等他們送。

### 0.78 磨坊逐筆匯出管道（2026-08-29 兩端落地，等一顆 token）

配置 v1（2026-08-29）把磨坊標為「20% 學費、無資料不可最優化」。管道
已通：產品端 V1.51.4 `/api/u/export/fills`（RESEARCH_EXPORT_TOKEN 保護、
只讀 fills.jsonl、src 按現行 cid 規則重算）＋研究端 `research/
mill_ingest.py`（FIFO 配對成含費回合，合成資料已知答案驗證過）。
**卡在使用者一步**：Railway（jarvis）設 `RESEARCH_EXPORT_TOKEN`，
flow_system `.env` 設 `MILL_EXPORT_URL/TOKEN/UID` 同值。
資料到手 → 首輪體檢（每回合淨益/勝率/費用佔比）→ 貼 regime 四格標籤
→ Kelly → **配置 v2**（磨坊那欄從佔位變計算值）。
誠實註記：08-24 前的列 cid 缺失、歸屬不可靠，統計只用可靠列。

### 0.75 兩場館永續套利（entropy-arb 型）——第四條策略線，錄資料階段（2026-08-28 登記）

> **本線的家在 `research/arb/`（2026-08-28 使用者要求與交易策略分開）**：
> README 載明隔離規則（永不 import 交易路徑、資金獨立、只共用兩個顯示
> 看板）、五條件偵察表、戰場帳本。判決程式 = `research/arb/premium_verdict.py`。
> 本節保留為登記與風險清單的原始紀錄。

**來源**：使用者研究了 `your-quantguy/entropy-arb`（兩場館 perp 套利：
Entropy/Hyperliquid vs Lighter/trade.xyz，量 `premium_bps = (A/B − 1)×1e4`
偏離中線超過門檻就雙腿對鎖，等收斂平倉）。使用者：「我是想做一個這個」。

**為什麼值得排**：與現有三條線**完全不同的棋格**——結構性 edge 不是統計性,
機會存在性直接可觀測,**驗證週期是天不是月**（錄一兩週掛單簿 → 扣費後的
溢價分佈就是答案）。不需要預註冊/CI/樣本時鐘那一套。

**風險清單（先寫,免得到時候忘）**
- 斷腿 = 主要事故源：一腿成交一腿被拒 → 裸倉在動的市場裡
- **中線漂移 = 2026-08-08 解碼漂移的同款病**：參考分佈不是被判斷的分佈,
  每筆小虧且看起來像正常交易
- **零費率是獲客補貼,有內建過期日**：Lighter taker 0 bps 不是均衡態
- 撿到的是做市商不要的：簿太薄或風險他們算過不值得
- 該 repo 一次 commit / 零 issue：**下單程式碼要逐行審**（滑價符號 bug 的
  先例——「看起來扣了成本」騙過所有人）
- **「沒有倉位也會爆倉」（2026-08-30 使用者轉來的 Binance 案例）**：統一
  帳戶裡留著平倉後的 USDT 負債，抵押品（RLUSD 折 90%、SOL 折 80%）用
  「最大」轉出後，折算抵押只比負債多 0.6u，任何一個 tick 就強平；查倉位
  歷史看不到任何倉。對本線的規格含義：(1) 對帳要讀**餘額與負債**，不只讀
  倉位——「查無倉位」對負債是瞎的（同 mistake 07-28「DB 只記自己下的單」）；
  (2) **任何資金劃轉都是風險事件**：轉出前自己算轉出後的折算保證金率
  （含各資產折價），留緩衝，不信交易所的「允許轉出」；永不點「最大」；
  (3) 保證金一律用場館的報價穩定幣（USDC/USDG），不用計息的替代穩定幣
  當抵押；(4) 產品端跟單用戶同樣適用——「0 倉位 ≠ 0 風險」要進風控規格。
  **通則（08-31 補，作者的機制拆解證實）**：uniMMR 的分母只算持倉維持保證金
  ＋利息 5%，沒倉位時塌到 0.137u，「轉出後 ≥1.2」的守衛實際只留 0.16u 緩衝
  去對 23,000u 抵押品——**守衛量錯了分母**（同族：08-08 解碼參考分佈≠被判斷
  分佈）。**任何比值型安全門檻，先問分母會不會在某個正常狀態下塌到零**；會的
  話該門檻在那個狀態等於不存在。本線接兩個保證金帳戶時，「淨曝險／保證金」
  在一腿平掉後分母同樣會塌，設計要照此寫。

**錄製已上線（2026-08-28 18:30 起）＋判準先凍結**

- **場館發現**：Entropy 不是 crypto 所——上市 6 個標的全是**股票/私募永續**
  （io:OAI＝OpenAI、io:ANTH＝Anthropic、SNDK、IONQ、NBIS、EWY）。
  「盤後預言機制度不同」是溢價主要來源；OAI/ANTH 連公開市價的錨都沒有
- **首錄配對**：SNDK vs lighter-rh（README 自己的例子；兩邊 taker 0 bps
  **當場驗證為真**）。首兩分鐘：溢價 mean −1.4 bps、std 0.6、buy 側瞬間
  可成交空間最高 +1.38 bps——**中線不在零**，README 的頭號警告當場應驗
- **耐久化**：`run_recorder.bat`（30 秒重啟迴圈）＋使用者啟動資料夾
  （schtasks 要管理員權限，改走 Startup）＋ freshness board 監控
  （1h 上限——**靜默斷錄 = 一週資料白等**）
- **本機補丁**（clone 內，未推遠端）：6 處 `open()` 補 `encoding="utf-8"`
  （cp950 家族）、Windows 無 `add_signal_handler` 的 fallback
- **判準（跑滿 7 天之前凍結，不看資料調）**：錄滿 ≥7 個完整天後，
  存在一個扣費後往返 ≥1 bps 的門檻帶、平均每天觸發 ≥10 次、且
  **前後兩半皆成立** → 進工程閘門討論；否則本線關閉。
  帶寬不掃描——用 analyze.py 的 p90 建議值起步

**判準修訂 v2（2026-08-28 22:40，已看 230 分鐘＝窗口的 3%；只加嚴不放寬）**：
使用者問「有沒有機會」時發現 buy 側 94% 的分鐘都有帳面肉——這不是好消息，
是**中線偏移**的症狀（永遠在的價差賺不到，要收斂才落袋）。而原判準只量
「帶存在＋開火頻率」，量不到「會不會收斂」，也量不到「肥的那刻盤口有沒有
量／是不是卡價」。三項修法，全部在判決程式裡寫死：
1. **加第四關「收斂」**：溢價偏離滾動中線（6h 中位數）超過帶之後，
   ≥70% 的事件要在 240 分鐘內收斂回帶的一半以內——過不了 = 帳面肉是
   結構性偏移，不論多肥都關線
2. **錄價程式補儀器**（本機補丁 v2）：新增 8 欄——最肥時刻兩腿頂檔
   名目的較小值（能吃到多少錢）＋該刻較舊一邊的報價年齡（>5s = 卡價
   嫌疑）。舊 230 分鐘無此欄位，只從量/卡價統計中排除，不從判準排除
3. 深度/卡價統計**列報告不進判準**（供工程閘門評估）——數字是這次才
   開始錄的，拿它當關卡等於在 3% 資料上發明門檻

**階段閘門（依 CLAUDE.md 既有紀律）**
1. **現在可做**：`--record-only` 錄兩邊掛單簿（零憑證、零錢、零風險）
   → `analyze.py` 量扣費後溢價分佈 → 有肉/沒肉,一兩週結案
2. **接錢包之前**：走統一風控框架的門（兩層 kill/中央曝險帳本——
   「第二條策略上線必須先有統一風控」對第四條同樣適用）,且下單路徑逐行審
3. 排序：**在 §0.59/Gate F/地形扳機結算之前不投入工程**,只允許錄資料
   （錄資料越早越好,因為分析需要的就是時間長度）

**錄製家族（2026-08-30 登記；使用者：「有很多標的為什麼只錄一個」）**

08-28 只錄 SNDK 是因為對 Entropy 六個標的做**同名比對**只對到它；重查發現
Lighter 的代號不同（`OAI`→`OPENAI`、`ANTH`→`ANTHROPIC`），加上 HL 主網
本身可當一腿（加密標的）。錄價程式補了兩個本機補丁（`hedge.symbol`
別名、`entropy.dex=""` 走 HL core），每配對獨立設定檔／log 目錄／
30 秒重啟迴圈／啟動資料夾項目，freshness board 逐條 1h 上限。

| 配對 | 腿 A | 腿 B | 定位 | 起錄 |
|---|---|---|---|---|
| SNDK | Entropy io:SNDK | lighter-rh SNDK | 原始 | 08-28 18:28 |
| NBIS | Entropy io:NBIS | lighter NBIS | 股票永續 | 08-30 ~23:00 |
| ANTH | Entropy io:ANTH | lighter-rh ANTHROPIC | 私募永續、無現貨錨 | 08-30 ~23:00 |
| **BTC** | HL BTC | lighter-rh BTC | **對照組**：兩邊深簿零費率，帶應≈0；它一肥就是儀器壞了（mistake 07-29） | 08-30 ~23:00 |
| ZEC | HL ZEC | lighter-rh ZEC | 薄加密 | 08-30 ~23:00 |
| NEAR | HL NEAR | lighter-rh NEAR | 薄加密 | 08-30 ~23:00 |

**io:OAI、io:EWY 首次連線即 `isDelisted`**（meta 仍列但已下市）——登記
當晚就掉兩個，**下市風險是這類場館的實際風險**，記入風險清單。io:IONQ
兩邊都沒有對沖腿。

**紀律**：(1) **同一套凍結判準（四關）逐配對套用，全部報告、不挑**——
六個裡挑最好看的上，就是變體 C/D；(2) 各配對從自己的首列起算 7 天，
SNDK 照舊 09-04；(3) 判決程式（`premium_verdict.py`）已改逐配對，新增
**「可捕獲美元／天」報告欄**＝次數／天 × 帶寬 × 最肥時刻深度——**只報告
不進判準**，用途是「過閘之後值不值得投工程」（SNDK 期中 ≈ $12–16/天，
就是為此登記的）；(4) BTC 若過閘 → 判「對照組亮了」，先查儀器。

**§0.75b 掃描器（2026-08-30 登記，使用者：「新幣／熱門迷因機會大，但需要
搜尋篩選機制」）**：獨立於錄價程式的 REST 掃描（`../entropy-arb/tools/
scanner.py`，每 ~2 分鐘一輪）掃 HL 主網 ∩ Lighter 主網／RH 的全部配對：
兩邊最佳買賣、頂檔美元深度、24h 量、上市日；輸出 `logs/scan/scan_*.csv`
與新上市／下市事件 `listings.csv`。**排名指標先凍結**（`research/arb/
scan_rank.py`）：逐配對逐側 band=p90 正向可成交空間、fires=≥band 的次數
／天、depth=那些時刻 min(兩邊頂檔) 的中位 → **可捕獲美元／天 = fires ×
band × depth**，取兩側較大者排名。掃描滿 ≥3 天且 ≥500 樣本才出名單；
上市 <48h 不列；前 3 名升格到完整錄價，**升格之後的資料才進判準**
（選擇用一段資料、驗證用另一段——防「掃描器變成生產漂亮假象的機器」）。
BTC 永遠在名單當對照。

**與主體的關係（使用者 2026-08-30 定調）**：「除了流動性獵取跟 V7，其他
都是附加價值，這兩個才是主體」——本線維持「只錄不做、判決日看數字」，
不佔研究線的預註冊／統計資源；掃描器與錄製家族是零風險的資料累積，
不是投入。

### 0.71~0.74 流動性「位置」的完整驗證（2026-08-26/27）——12 個候選，0 個過關

**起點**：使用者看完 7 天流動性圖說「**這是個事件／流動性獵取的區域位置
沒有處理好／昨天看圖表就覺得怪怪的**」，並先後提供兩份 TradingView 指標
（FX365 "Liquidity Pools"、LuxAlgo "Liquidity Swings"）。

#### 位置本身：三層檢查，全部通過

| 檢查 | 結果 |
|---|---|
| **錨定**（池價須等於某根 K 的實際高/低） | **9,349 個，0 個對不上** |
| 開掃三角形落在線上 | ✓ 六筆全部 |
| 與參考指標交叉驗證 | ✓ `77,851` 兩套定義獨立指到同一點 |

**位置沒有跑掉。** 模型學不到東西的原因不在這裡。

#### 兩份指標的每一個概念都測過，全部無效

| 來源 | 主張 | 節 | 結果 |
|---|---|---|---|
| FX365 | 只留最近一個池 | §0.69 | ❌ 成立三桶只差 +0.0173R 且非單調 |
| FX365 | 區間 ±0.1% | §0.71 | ❌ 穿越深度**單調**，越淺越好（0.00–0.03 是 +0.1176/61.6%），線是對的模型 |
| LuxAlgo | pivot=14 | §0.73 | ❌ 過了峰值（+0.0658 vs PIVOT=10 的 +0.0754，廣度掉到 7/9） |
| LuxAlgo | 區域累積成交量／觸碰 | §0.74 | ❌ 成立桶差 +0.0214R／+0.0055R，**無分離** |

**§0.71 的次要檢查值得記**：最淺那桶的停損率與其他桶一樣（10.6% vs 10.3%）
——所以不是「淺＝比較安全」，是**淺＝勝率真的高**。一個 tick 被打回來是
最乾淨的掃單失敗。

**§0.74 解釋了為什麼專業指標在圖上好看**：成交量權重讓**勝率單調上升**
（56.3% → 59.3%，+3.0pp）而 **meanR 完全不動**。重的區域比較常贏但贏得小。
**人眼在圖上注意的是命中率，而它真的有 +3pp，但沒變成錢。** 同形狀見 V7
出場研究（收緊出場提高勝率、同時剪掉贏家）。

#### §0.73 PIVOT 粒度：粗糙本身就是濾網

```
PIVOT   變體B     meanR     廣度
  4    16,695   +0.0567    9/9   ← FX365
  5    14,477   +0.0587    9/9
  8    10,109   +0.0721    9/9
 10     8,273   +0.0754    9/9   ← 現行（峰值）
 14     5,966   +0.0658    7/9   ← LuxAlgo
```

單調遞增到 10、14 掉下來。**大擺盪點承載的流動性比小擺盪點多**，細化不是
多拿樣本是稀釋好樣本。

反直覺但不改變結論：用 CI 反推 t 值，PIVOT=4 約 **6.7** > PIVOT=10 約 **6.3**
（n 翻倍勝過均值下降）。**但問題從來不是量不出 edge**（t=6.3 早就夠），是
edge 蓋不過成本——扣 0.19R 後 PIVOT=10 −0.115、PIVOT=4 −0.133，細化更差。
**真正的槓桿是把 meanR 拉高過成本線，不是多拿低均值事件。**

#### §0.71b/§0.71c 堆疊：唯一撐到最後的，依預註冊仍 FAIL

**§0.71b（swing only）**：預註冊的「2+ vs 無堆疊」失敗（無堆疊臂 n=119-194
CI 含零）。但兩個**成立**的臂之間有東西，三容差幾乎不動：
1 種 +0.111 vs 2+ 種 +0.061，差 ~+0.050R。

**§0.71c（四家族全納入，使用者問「我的是 pivot 嗎」才發現的範圍限制）**：

```
合併      ≤1 +0.0808  ≥2 +0.0630  差 +0.0178   兩臂皆成立
swing     ≤1 +0.1220  ≥2 +0.0623  差 +0.0597 ✓
pdh_pdl   ≤1 +0.1086  ≥2 +0.0682  差 +0.0404 ✓
session   ≤1 +0.0655  ≥2 +0.0490  差 +0.0165
pwh_pwl   一臂 n=9，不參與
```

**依預註冊 FAIL**（需合併 >0.03 ∧ ≥3/4 家族；實際 +0.0178 ∧ 2/4）。
**不因「方向一致」升級**——那正是預註冊要擋的。

有資訊的是**稀釋來源**：`session` 佔母體 49% 而效應最小，**且它是最
「時鐘化」的池**（時段收盤即固定）。swing 是市場走出來的、pdh_pdl 是換日
固定的——**三種不同成因的池方向一致，但效應大小與成因相關**。

#### 範圍限制（使用者一句話問出來的）

四種池只有 **swing 是 pivot**；另外三種是時間定義。而今晚 §0.69/§0.71/
§0.73/§0.74/§0.70 全部走 `SC.backtest_symbol` = **swing only**。
實際 shadow log：swing 20% / session 49% / pdh_pdl 25% / pwh_pwl 4%。
**五個結論只涵蓋五分之一的實際交易量**，§0.71c 是唯一補起來的。

#### 我自己寫的儀器，這一輪錯了三次，三次都在解讀之前抓到

1. **驗證圖濾掉 82%**（§0.72）：±4% 帶寬讓 72 個 pivot 只畫出 13 個。
   使用者圈出「沒記錄到」的三個點，**兩個是我的錯**。
   **驗證用的圖不可以過濾它要驗證的東西。**
2. **`first_hit` 批次版**（§0.71c）：在第 `est` 根就推進堆疊，同根被穿過
   的池被彈出但不標記 → 永遠算「未掃」。600 抽驗 90 個不一致；
   修後 2,672 抽驗零不一致。
3. **在成交棒計數而非開掃棒**（§0.71c）：掃單本身會掃掉同價位其他家族的
   池，所以 `hit >= fill` **正好把要數的東西全部排除**——158 筆變 5 筆。
   **這不是「數錯」，是「把定義刪掉」**，而輸出看起來完全正常（乾淨的表、
   合理的 n、四家族都有數字）。**若沒回頭對照 §0.71b 的舊數字，會報出一個
   由儀器錯誤產生的乾淨結論。**

**第 3 條是本輪最該記的一條**：儀器錯誤的最危險形態不是讓數字變畸形，
是讓它**變成一個看起來完全正常的、不同的問題的答案**。

### 0.70 獵取進場 meta-model（2026-08-27 建置並跑完）——回測過、前瞻不過

使用者提案：把獵取訓練成模型，用掃單前後的資料投票判斷這一筆會不會成。
查證屬實**從未做過**——`sweep_failure/` 零個模型，A-E 變體與組合觀察名單
全是手寫布林；`meta_exit_model.py` 是 V7 的**出場**（且判決是訊號不存在）。

**建置**（`raid_entry_model.py`，判準跑數之前凍結）
- 母體 **8,272 個變體 B 事件**、29 幣、2024-01~2026-08。全部是 swing 池
  （`backtest_symbol` 走 `detect_sweeps`），故 `level_kind` 是常數、不列特徵
- 目標二元（`net_r>0`）；14 個特徵**全部在開掃棒收盤算出**
  （§0.66 已量測：同根成交 0 筆，86/106 是下一根）
- 三條基準線，**B3 = §0.59 regime 濾網才是門檻**
- 決策門檻固定 **P>0.5**，不掃（2026-06-20 陷阱）

**結果**

| 關卡 | 結果 |
|---|---|
| (a) 時間 WF（purge+embargo，8 折） | per-fold lift vs B3 **+0.0370**、正折 6/8、CI [+0.010,+0.061] ✓ |
| (b) 留幣 CV（模型沒見過的幣） | 4/4 組贏過兩條基準 ✓ |
| **(c) 前瞻留出**（訓練 <FREEZE、評分 ≥FREEZE） | **✗** |

前瞻：全進 +0.0040／**模型 +0.0276**／**regime +0.1147**。
**模型贏過全進 +0.0236（它確實學到真東西），但輸給 regime 濾網 −0.0871。**

**三個配置全數報告（2026-08-27，事先宣告只測三個，不得再加）**

| 配置 | 主模型 | 回測四關 | 前瞻留出 (c) |
|---|---|---|---|
| `base` | 變體 B（模型與 regime **競爭**） | ✓ | **✗** −0.0871 |
| `compose` | 變體 B ∧ 主場（模型在 regime **之上**） | ✓ | **✓** +0.0859 |
| `recent` | 同 base，訓練只用近 12 個月 | ✓ | **✗** −0.0808 |

**第一版只測 base 就宣告整族死亡——那是偽陰性**，與整晚防的偽陽性方向相反。
使用者當場指出「這麼快就放棄了喔」，正確。`compose` 不是調參：
**meta-labeling 的定義就是次模型精修主模型，不是跟它賽跑**；把它們對打是
架構錯誤。

**分佈本身可解讀**：模型能從全母體挑時挑得比一條規則差；被限制在**沒有
衰退的那一格內**才有增量。與 §0.58 一致——`base`/`recent` 的訓練資料被
已經死掉的非主場格主導。

**但這一切都不是證據，原因是致命的**

(c) 關用的前瞻窗是 **FREEZE(07-28) → 今天**，而 §0.59 的樣本 floor 是
**08-26**，且該節白紙黑字寫著「那批樣本已經被用掉，不得再當證據」。

**所以 (c) 的前瞻窗有 96% 是已宣告作廢的資料。** compose 在「產生 regime
假設的同一批資料」上證明自己比 regime 好——那不是 out-of-sample，那是
同一批資料算第二次，正是我在 `獵取_跳過記帳_規格` 裡要求產品端不要做的事。

**真正乾淨的樣本今天 n≈0。**

**一個被更正的錯誤（我自己的）**：先前列窒礙時寫「lift +0.0859 < 執行成本
0.19R 所以被吃光」——**那個比較是錯的**。執行成本是對所有臂一致的水位平移，
不是跟 lift 相減：

| | shadow | 扣 0.19R 後 |
|---|---|---|
| 全進 | +0.0040 | −0.186 |
| regime | +0.1147 | −0.075 |
| **regime+模型** | **+0.2006** | **+0.011** |

**模型反而是唯一能把它推過成本線的東西。**

**驗證時間（用觀測均值反推 t=2 所需筆數）**

| | 均值 | 所需筆數 |
|---|---|---|
| 全進 | +0.0040 | ~119,000 不可能 |
| regime | +0.1147 | ~145 可行 |
| 模型 | +0.2006 | ~47 很快 |

**濾網縮短驗證時間**（所需 n 隨均值平方反比下降）。所以這條路統計上不是
死路，只是**還沒開始**。

**處置**
1. `compose` 降級為**假設**，不是通過。與 §0.59 綁同一個時鐘，樣本 floor
   **08-26**
2. 判準加一條：**shadow 上為正不夠，扣掉實測滑價（0.19R）後仍為正**
3. 三個配置已用完，**不得再加第四個**
4. 特徵重要度 `sweep_close_pos` 0.143 一枝獨秀 —— 若重啟，特徵往**事件
   本身**挖，不往市場背景找（與 §0.65 持倉背景全滅互相印證）

**這一節最重要的不是結論，是它差點過關**
只跑 (a)(b) 會得到「四關全過、成為預註冊候選」。第 (c) 關是**臨時加的**，
觸發它的是兩個看起來不對勁的細節：
1. **B3 在回測母體上只比 B1 好 +0.0008** —— 我設計的「硬門檻」在這個母體
   上根本不硬（與 §0.58 一致：regime 效應主要是**前瞻**現象）
2. **最近兩折（7、8）都是負的** —— 而那正是前瞻樣本所在的時期

**規矩補一條**：任何在回測母體上訓練的模型，**必須有一關是「訓練只用舊
世界、評分只用新世界」**。(a) 的 walk-forward 與 (b) 的留幣都在**同一個
世界內**重抽，看不到世界本身變了——而 §0.58 已經證明它變了。

**誠實的樣本限制**：前瞻的 regime 臂 n=147（CI [+0.018,+0.210]、廣度 6/9），
依成立性閘**未成立**。所以 (c) 的失敗是「模型沒能贏過一個未成立的臂」，
方向明確但也不是壓倒性。要真正定案需要更多前瞻樣本——與 §0.59 同一個時鐘。

**特徵重要度**（全樣本重擬合，僅供理解不作證據）：
`sweep_close_pos` 0.143 一枝獨秀，其次 `sweep_range_atr` 0.093、
`approach_atr` 0.080。**掃單棒自己的形狀比任何背景變數都重要**——若日後
重啟這條線，特徵應集中在「掃單事件本身長什麼樣」，而不是市場背景。

### 0.65~0.69 持倉資料與流動性定義：一夜的完整計數（2026-08-26/27）

使用者：「確認所有系統的資料來源，只要有 Coinglass 可以用的就是個可能的
突破口」。盤點 + 五輪檢定，**淨結果零個新特徵**，但排除的速度是重點。

**§0.65 持倉資料進生存條件層 —— NO-GO**
五個儀表（OI z / 大戶vs散戶 / 費率極端 / 清算強度 / 現貨溢價）。
- 壞判準下 **5/5 過關**。判準問「最佳桶 CI 是否離零」，而最佳桶多半是
  最大桶，最大桶貼近母體均值、CI 最窄，母體均值又是正的 → **幾乎必過**。
  與同日「CI 下緣低於全體」是同一種假門檻。改把 CI 放在**差**上 → 殺 4 個
- G4 清算強度存活（差 +0.1480、ADX 兩層內都在、與 RANGING 重疊僅 38%）
- **前視檢查殺掉最後一個**：落後一根 +0.1480 → **−0.0218**（翻號）。
  `fill_ts` 那根的清算總額要到收盤才完整 → 量的是成交自己那根 bar
- 與既有判決一致：`project_orderflow_edge_verdict` 早就寫過「小時級**瞬時**
  流在 4h 零邊際價值，edge 在**積分**流」。清算是瞬時流

**§0.66 drv_q 因果性 —— 因果關過、新鮮度關差 0.063**
- **同根成交 0 筆**（86/106 是 gap=1，開掃那根必先收盤）→ **完全因果**
- 前瞻 87 筆：Q=1 +0.2405 vs Q=0 −0.1642，差 +0.4047，CI [−0.063,+0.539]
- 回填 19 筆：差 +0.7649 → **in-sample 膨脹約一倍**，典型挑選形狀
- **第一版判決是錯的**：用 `drv_gap_oi` 的 "na" 當「同根」代理，但 "na"
  含 gap=1（因果的），於是「確定因果」組只剩 n=20，我拿那個無力比較
  宣告整個家族死亡。**又一次「量不到當成沒有」**

**異源盤點 —— 異源性確實只有 Coinglass，但那是篩選結果**
方向 136：cg 91 (67%) / 價格 31 / Binance 流 12 / 時間 2
幅度 76：cg 60 (79%) / 價格 10 / Binance 流 4 / 時間 2
**Deribit 0、跨市場 0** —— 兩者都接過並被剪掉（DVOL 2026-06-14 判 mirage、
與 cg_fcvd_cum 共線 0.876）。Coinglass 打贏其他異源後自己飽和。

**§0.67 小幣比較沒效率？—— 前提未獲支持**
29 幣、凍結規則、同參數：流動性排名 vs meanR 的 Spearman **+0.134**；
非 core9 −0.0267 vs core9 −0.0069，差 −0.0198，CI [−0.091,+0.051] 含零。
**BTC 是 29 幣中第 5 好（+0.1149）**，與「BTC 太有效率」的預測相反。
逐幣 meanR 從 −0.21 到 +0.28，但**與流動性無關**，n 只有 26–62 —— 那個
範圍光靠雜訊就跑得出來。**測 30 幣挑最好的必然找到假陽性。**

**§0.68 流動性位置目視檢查（`liquidity_map_check.py`）**
- 第一版印出「72h 內 307 個未掃池」→ 不是 bug，是**計數口徑**：
  ±3% 內只有 10 個，其餘是很久以前建立、價格再沒回去碰過的
- **同一價位被登記成多種池**（76,670 同時是 PDH/PDL + session + swing）。
  D4 曾為此修過分桶，但**池的定義本身沒改**。對密度特徵與模型是實質問題
- 目視通過的部分：swing 確認延遲 10 根看得出來、session 是收盤才出現、
  掃過的線正確中斷 —— **因果性在圖上可見**

**§0.68b 對照使用者提供的參考指標（`liquidity_calibrate.py`）**
「Liquidity Pools」(FX365_Thailand)：PIVOT=4、**只留最近一個**、±0.1% 區間。
- **凍結版 64 個價位，參考指標 100% 都找得到** → 數學必然（10 根 pivot 必
  然也是 4 根），所以這個 100% 是**通過的正確性檢查**，不是巧合
- 指標原始碼有 bug：`period=="60" and period=="240"` 永不成立（應為 or），
  1h 落到 fallback 1.001
- 三個差異裡只有「只留最近一個」是實質的 → 轉成 §0.69 的檢定

**§0.69 池的新近度 —— 無分離（`pool_recency.py`）**
n=10,668、29 幣。「被掃的池在當下是不是最新的未掃池」：

| 桶 | n | meanR | 日聚類CI | 廣度 |
|---|---|---|---|---|
| 0 最新 | 8194 | +0.0712 | [+0.048,+0.096] | 28/29 |
| 1 第二新 | 1769 | +0.0555 | [+0.015,+0.097] | 24/29 |
| 2-4 | 655 | +0.0728 | [+0.007,+0.136] | 20/29 |
| 5+ 陳舊 | 50 | +0.2157 | **[−0.020,+0.458]** | 10/17 |

**第一版判讀又錯了（今晚第三次同型）**：拿 n=50、CI 含零的桶宣告「陳舊
反而較好」。已加**成立性閘**（n≥200 ∧ CI 離零 ∧ 廣度≥6）——比較之前每一臂
都要先成立。成立的三桶只差 +0.0173R 且非單調 → **無分離，不列入特徵**。

**這一夜的方法論收穫（比任何一個數字重要）**
同一種錯犯了三次：**拿未成立的臂去驅動判決**。
- §0.65 判準問最佳桶的 CI（幾乎必過）
- §0.66 用 n=20 的無力比較殺掉整個家族
- §0.69 用 n=50、CI 含零的桶宣告反向
**修法已寫進三個檔案的判讀邏輯：比較之前先問「這一臂本身成立嗎」。**

### 0.63 / 0.64 V7 拆成多空兩個策略？（2026-08-26 使用者提案）——切錯軸了，出場那半測不了

**提案**：把 V7 的做多與做空拆成兩個策略，各自有各自的進出場。

#### 進場那半：方向軸是平的，已兩次 NO-GO

同日量測（Strong n=815）：

| | n | WR | CI95 | bps |
|---|---|---|---|---|
| 做多訊號 | 372 | 58.1% | [53.0, 63.0] | +18.8 |
| 做空訊號 | 443 | 60.3% | [55.6, 64.7] | +18.7 |

**勝率差 2.2pp、每筆報酬差 0.1 bps、CI 大幅重疊。** 切成兩個策略只會得到
兩個統計上分不出來的東西。這與 2026-07-06 的 CLOSED NO-GO 一致（新鮮 OOS
上 UP/DOWN Strong 70.8%/69.5%，前提消失）。

**但使用者感覺到的結構是真的——它在 side × regime 上**（48.1% ~ 67.2%、
−8.0 ~ +38.8 bps，見 §0.60 Q2 表）。做多不是普遍差，它在 NEUTRAL/RANGING
最好、只在 TREND_UP 崩掉。**按方向切會把這個結構整個抹平。**
這也解掉 `project_short_is_the_edge` 的表面矛盾：那個不對稱是**條件性**的，
不是邊際的。

拆策略的三個結構成本（與效果無關）：參數面翻倍樣本減半
（mistake.md 2026-04-13：切群後 <500 筆會讓 GBM 變雜訊放大器，BEAR 子模型
AUC 0.378）、`max_position_count` 1→2（同時持多空 = 較小淨部位＋雙倍手續費）、
兩套 Gate 各自統計力減半。**便宜得多的形式是條件式規則——那正是 Q2。**

#### 0.63 出場那半的機制：上升段 DOWN 開火率（觀察，n 太小）

先量了 DOWN 是否在上升段被餓死（`v7_down_rate_by_regime.py`）。
**第一次跑錯了**：把全歷史池在一起，得 0.95×、結論「沒餓死」——但那
**橫跨解碼修法的語意分界**，違反 CLAUDE.md 核心原則 7（同 mistake.md
2026-04-13 混合模型版本那條）。分開之後：

| 期間 | cell | UP/bar | DOWN/bar |
|---|---|---|---|
| 修法前 | TREND_UP | 4.42% | 6.50% |
| **修法後** | **TREND_UP** | **8.46%** | **1.54%** |
| 修法後 | RANGING | 3.06% | 13.27% |
| 修法後 | TREND_DOWN | 5.88% | 17.65% |
| 修法後 | NEUTRAL | 2.33% | 13.95% |

**修法後上升段的 DOWN 開火率是其他格的十分之一**——若成立，`opp_signal`
對多單結構性失效。但 **TREND_UP 那格只有 130 bar / 2 筆 DOWN**，兩個事件。
且全體 16 UP : 24 DOWN **並未失衡**，所以不是 08-08 那種全域單邊。

**值得註記的第二層**：修法前 UP 在 TREND_DOWN 開火最多（9.05%，均值回歸的
形狀），修法後變成在 TREND_UP 開火最多（8.46%）。**這可能是窗口 500→200
的副作用，沒有人看過。** 列為觀察。

#### 0.63b 用正確口徑重量：擔心成立，而且是**營運級**發現（2026-08-26）

**§0.63 量錯了對象。** live 的 V7Bot（`jarvis src/v7bot.js:108`）觸發條件是
`sig.direction !== 'NEUTRAL'` —— **不要求 Strong**。§0.63 只量 Strong，
量的是一個比實際觸發條件**更窄**的事件，所以它的數字不能當成出場覆蓋率。

放寬到「任何反向讀數」重量（`v7_opp_coverage.py`，只取修法後 12.2 天）：

| cell | bar | Strong↓ | 任何↓ | 任何↓/bar | 平均等待 |
|---|---|---|---|---|---|
| RANGING | 98 | 13 | 26 | 26.53% | 4 小時 |
| **TREND_UP** | **130** | **2** | **3** | **2.31%** | **43 小時** |
| TREND_DOWN | 17 | 3 | 6 | 35.29% | 3 小時 |
| NEUTRAL | 49 | 6 | 9 | 18.37% | 5 小時 |

**擔心成立：0.09×。** 上升段的多單平均要等 43 小時才等得到反向訊號，
其他環境 3–5 小時。時間上限是 72 小時，所以分佈的尾巴會直接撞到上限。

**「n=3 不能下結論」這個打發是錯的**（我先前這樣寫過）：這不是 3 次獨立
試驗，是 **130 根 bar 的曝險期間只發生 3 次**。若真實率與 RANGING 相同
（26.5%），130 根 bar 預期 **34 次**。觀察 3 次，差距不是小樣本能解釋的。
（誠實補：訊號叢集＋遲滯冷卻使有效樣本小於 130，故不報精確 p 值。）

**營運級的部分——遷移掉了一道出場**：`conviction_decay`
（`OKX_CONVICTION_DECAY_BARS=2`，2026-07-25 上線，**設計目的正是接住
opp 餓死**）**沒有隨執行面遷移到產品端**。live 出場盤點只有
`trail(3×ATR)` / `opp(任何反向)` / `time(72h)`。

所以在 TREND_UP 裡，多單實質只剩**移動停損**與**到期**兩道。

**兩個獨立問題落在同一格**（互相佐證，不是同一件事的兩種說法）：
- 進場側：TREND_UP × UP 是八格最差（48.1%、−8.0 bps）——**訊號層**指標，
  與出場完全無關，所以不是被出場污染的
- 出場側：同一格的反向訊號覆蓋率是其他格的十分之一

**§0.60 Q2 因此變強**：它擋掉的正是「進場最差 ∧ 出場最弱」的那一格。
但 Q2 要 n≥60（數月），**不能拿它當這個出場缺口的解**。

**建議（已交產品端）**：把 `conviction_decay` 補回去。定位是**遷移回歸
的修復，不是新參數**——它在 OKX 時代就是凍結規則的一部分。誠實標註：
該規則 2026-07-25 是在 **0 shadow 樣本**下上線的（informed override），
真實觸發僅一次（08-06 id22，行為正確）。它的**動機**今天被獨立證實了，
但它的**效果**仍未經驗證。

#### 0.64 出場變體按方向切：測了，但這份資料答不了

`v7_exit_by_side.py`（重用凍結的 `exit_variants_backtest`，基準＝生產的
3×ATR trail + 72h + opp=any，逐側切）：

```
基準  LONG  n=18  WR 66.7%  +0.4 bps  opp_signal 收尾 61.1%
      SHORT n=17  WR 64.7%  +0.5 bps  opp_signal 收尾 58.8%
六個變體在兩側全部為負，無一通過兩半同號
```

**第一版腳本的判讀是「基準兩側皆勝，沒有理由按方向切」——那比資料能支撐
的強，已修。** 每半不足 10 筆時兩半同號檢查**依建構必然失敗**，於是每個
變體都被拒絕，不論它有沒有效。**一個什麼都通不過的檢定，不是「沒有效果」
的證據。** 已加統計力閘（每側 n<40 不出判讀）。

**更根本的限制**：這個 harness 的 OOS 窗是 2026-06-09 ~ **08-08**，
**結束於解碼修法（08-12）之前四天**——所以它**在結構上測不到 §0.63 提出
的那個機制**。

**唯一站得住的正面發現**：在**修法前**的資料上，`opp_signal` 對多單沒有
偏少（61.1% vs 空單 58.8%）。這與 §0.63 的分期結果**獨立吻合**——修法前
兩側都健康。所以若真有問題，它是修法**之後**的現象，而那邊 n=2。

**處置**：進場側不動（兩次 NO-GO ＋ 方向軸實測是平的）。出場側**不進
預註冊**——不是因為證否，是因為**現有資料在結構上答不了**。要答需要
修法後的樣本，時鐘與 §0.60 Q2 相同。

### 0.62 倉數上限有沒有代價（2026-08-26，產品端提問）——方向一致、強度撐不住

**產品端主張**：被倉數上限擋掉的訊號**平均好 4.3 倍**，是「先到先得」的
抽籤偏誤而非隨機減樣；並問 (a) Gate F 是否被污染、(b) 能否給一個「按 R
值排序」的分配規則。

**先驗前提**（`raid_slot_bias.py`，判準跑數前凍結）。兩個機制會在**上限
零效果**的情況下憑空造出這個形狀，而且兩個在本系統都是活的：

- **A 計分不對稱**：被擋的用 shadow log 計分（level 價、零執行成本），
  進場的用真實成交（實測滑價 0.19R，§0.57 的結構落差 0.1328R）。對一個
  ~0.07R 的基準，光這個差就能造出這種倍數
- **B 高並發條件化**：訊號**只有在並發已滿時才可能被擋**，所以「被擋」
  天生條件在高並發上。meanR 若隨市場忙碌程度變動（原因與上限無關），
  這個比較就把它算到上限頭上

shadow log **沒有倉數上限**，所以可以直接模擬一個：兩臂同計分 → **A 被
建構性排除**；B 直接量。剩下的才歸因於上限機制本身（叢集內先後順序）。

**結果**（變體 B · core9 · 已結算 n=346）：

| K | 進場 n | 被擋 n | 被擋佔比 | 進場 meanR | 被擋 meanR | 差 | 差的日聚類 CI |
|---|---|---|---|---|---|---|---|
| 3 | 184 | 162 | 46.8% | −0.0801 | +0.0763 | +0.1564 | [+0.023,+0.299] |
| **5**（實際值） | 259 | 87 | 25.1% | **−0.0333** | **+0.0717** | **+0.1050** | **[−0.090,+0.302]** |
| 8 | 324 | 22 | 6.4% | −0.0139 | +0.0962 | +0.1101 | [−0.257,+0.587] |
| 12 | 343 | 3 | 0.9% | −0.0074 | +0.0460 | +0.0534 | [−0.054,+0.156] |

**混淆檢查（機制 B）**：meanR 依到達時並發數分桶 —— 0 → **+0.1073**、
1-2 → **−0.1049**、3-5 → +0.0365、6-11 → −0.0017、12+ → +0.1261。
**跨桶差 0.2310R，比上限效果 0.1050R 還大**，而且**非單調**——後者說明
這些桶自己就很吵，不是一個乾淨的「越忙越好」。

**判讀**：
1. **在他們實際的 K=5，效果與零分不開**（CI 含零且很寬）
2. **方向與他們一致**（被擋的點估計較好），但強度在 n=346 撐不住
3. K=3 的 CI 離零，但那是四個 K 裡的一個、而且不是他們的設定
4. **「4.3 倍」這個統計量本身就有問題**：同計分之下進場臂是**負的**
   （−0.0333R），**比值在分母趨近零時無定義**。用差不用比

**(a) Gate F 沒有被污染，而且是結構性免疫**：Gate F 判在 **shadow log**
上，那裡**沒有倉數上限**，每一筆訊號都記。這點裁示 §A 已寫過。真正帶著
這個偏誤的是**十月的執行品質總驗**（「我們實際跟到哪些」非代表性樣本）。

**(b) 按 R 值排序的分配規則：不能給，理由是根本性的**
1. **R 只有平倉後才知道。** 依已實現 R 排序是前視，不可實作
2. **依預測 R 排序 = 一個新模型**，而且是在看到「被擋的比較好」之後才
   決定要建的 —— 這正是變體 C/D 的陷阱
3. **沒有證據顯示叢集內的順序帶資訊**。上面的檢定在 n=346 上證不出來，
   在一個未證實的效果上建排序器就是在擬合雜訊
4. **排序與 §0.57 的修法直接衝突**：武裝觸價的語意是「碰到就進」。要在
   並發候選之間排序就必須**等**，而等正是 §0.57 量到要付 0.1328R 的那件事。
   **先到先得對這個策略可能是結構上正確的**，不是將就

**能給的三件事（按價值排序）**
- **改統計量**：比值 → 差。分母趨零時比值不穩
- **改記帳而不是改分配**：逐筆記下被擋的訊號 + 原因 + 它那個 level 的
  shadow 結果。十月總驗就能用**同計分**在**他們自己的資料**上量出上限的
  代價。「樣本有偏誤」的正解是**記下被丟掉的是什麼**，不是去修樣本
- **§0.59 免費解掉一部分**：主場濾網之下被擋佔比 **25.1% → 17.5%**。
  **濾與排序是替代品**——移掉非主場訊號本身就釋放了倉位，零新參數

**供給側的缺口（研究端的錯，已修）**：要「用同一把尺計分被擋的訊號」，
產品端得拿得到那筆訊號的**結果**——而 `raid_signals_live` **只發
`status=OPEN` 的列**，4 小時窗過了就消失。**結果從來沒有被發布過。**
所以他們就算記下了被擋的訊號也無從計分，只能拿自己的真實成交去比研究端
的數字——**那正是製造出「4.3 倍」的計分不對稱本身**。

已補 `raid_outcomes` 表 + `/public/raid-outcomes`（變體 B、已結算、45 天
窗，含 `net_r`/`gross_r`/`stopped`/`regime_cell`）。**兩條 join 路徑**：
看訊號饋送進來的用 `(symbol, level_kind, fill_ts)`；看武裝清單進來的用
`(symbol, level_kind, entry_px≈trigger_px)`——shadow engine 跑 `SLIP=0`，
所以 `entry_px` 就是 level 價。**武裝那條路不可用 `fill_ts` 對接**：
消費端看到的是即時觸價，帳本蓋的是成交 bar 的收盤。

**若仍要排序規則**，唯一站得住的形式是預註冊**一個**滿足三條件的鍵：
已凍結、訊號當下可得、**不是從這批資料挑出來的**。目前唯一候選是
`level_kind`（`pwh_pwl` 明顯最差 −0.2012R / t=−2.04 / n=49，見每小時
記帳）——但 n=49、四選一、t 僅 −2.04，**很弱**，而且仍是看過資料才想到，
必須走完整預註冊與新樣本，不得直接打開。

### 0.61 研究進度看板上牆（2026-08-26）——紀律從外面看不見就等於不存在

**觸發**：使用者問「有把這些新資訊跟預註冊的內容都放到網站上嗎，**這樣我
才知道進度怎麼樣**」。答案是沒有——§0.58/§0.59/§0.59b/§0.60 全部只在
TODO 與交接文件裡。

**真正的問題比「忘了上牆」更深**：預註冊散在 TODO 各節，每條時鐘各有
一支腳本，**進度只有跑腳本才看得到**。從外面看，五條時鐘同時在累積的
狀態，和什麼都沒發生，長得一模一樣。

**做法**（第四個 off-cloud-recorder 實例）：`research/prereg_publish.py`
每小時把「所有未判決假設 + 進度 + 已判決結論」寫進 `prereg_clocks`，
agent 出 `/public/prereg-clocks`，網站 `PreregBoardCard` 呈現。

**設計上最重要的一條：看板只出進度，不出判決數字。** 每個判決都有唯一
的擁有者（凍結它判準的那支 scorer）；在這裡複製一個數字＝第二份實作，
會安靜地跟真正做決定的那個不一致。

**這條規則當場就自我應驗了**：`_n_gate_f` 第一版多篩了 core9，得出
**346/1400**，而權威值是 **1127/1400**——「第二份實作安靜地不同意」
這個錯，就犯在警告它的那個檔案裡面。已修，並在函式 docstring 寫死
「若這個數字不再等於每小時 log 的 B 列，錯的是這裡不是 log」。
註：`_n_059` **保留** core9 是對的（§0.58/§0.59 的證據基礎全程 core9），
同一張 CSV 兩種篩法各自正確，不得為了一致而改。

**驗證踩到的第二個坑**：`cmd /c "...bat"` 在 Git Bash 下，`/c` 會被
MSYS 當成路徑轉換掉 → cmd 開了互動 shell 什麼也沒做 → **退出碼 0**。
我差點拿這個假的 0 當作「.bat 驗過了」。要寫 `cmd //c`。這正是
mistake.md 2026-08-20 那條「判準是產物內容不是退出碼」的價值——
log 尾巴沒有新步驟的輸出，才發現整支根本沒跑。

**已上牆的內容**：6 條未判決（§0.59、§0.60 Q2、地形扳機、Gate F、
§0.52、§0.53）+ 5 條已判決（含負結果與陣亡：§0.60 Q1 方向軸不成立、
撤單流 FAIL）。**只出百分比／R／筆數／日期**，符合公開面硬規則。

### 0.60 V7 × regime（2026-08-26 開跑，Q1/Q2/Q4 已出，Q3 待地形扳機）

**為什麼是這個**：獵取那條線上，方向軸推翻了「TRENDING＝逆風」的簡化
（§0.54b：TREND_DOWN 跟主場一樣好，只有 TREND_UP 是問題）。而 V7 的
regime 判決 **B-P8 用的正是同一個沒分方向的 ADX**（RANGING WR 63.6%
vs TRENDING 58.3%，+5.3pp，一級證據）——**同一個簡化很可能也在那裡**。
樣本足夠：Strong 已結算 n=815（DOWN 443 / UP 372）。

**開跑前先凍結的四個問題**（跑數之前寫，順序即優先序）：

1. **V7 的方向軸**：B-P8 加上 TRENDING 的上下分向後，四格各是多少？
   若 TREND_DOWN 與 RANGING 齊平（獵取的形狀），B-P8 的告警語意同樣
   要改成「只有上漲趨勢是逆風」。
2. **V7 弱側 × regime 的交互**：live 的病灶是 LONG（−3.13% vs SHORT
   +2.63%，§0.51b）。**LONG 在 TREND_UP 是不是特別差**？若是，那
   「弱側」與「非主場」其實是同一件事的兩種說法，濾掉一個就夠。
3. **地形濾網 vs regime 濾網是不是同一個效應**：地形（流動性位置）
   45/60 九月第一週結算，gap +13.5pp；regime（ADX）+5.3pp。兩者
   **可能是同一件事的兩種量法**——必須測交叉表（kept×RANGING 四格），
   否則會把一個效應算兩次、疊兩層濾網卻只拿到一層的效益。
4. **V7 的 within-cell 有沒有衰退**：獵取的落差 75% 來自格內衰退
   （§0.58）。V7 的訊號層近 90 天 53.7% vs 全期 59.5%，**同樣的分解
   要做一次**——是組成變了還是格內變了。

**紀律（先寫死）**
- 四個問題**一次全報告**，不得只報好看的格（地形戰役的全格報告規矩）
- 分桶前先看 **n 的分佈合不合物理**，有桶為 0 或佔比 >90% 一律先當
  儀器壞掉（mistake.md 2026-08-02）
- 問題 3 的交叉表若顯示兩濾網高度重疊，**不得兩個都上**——擇一，
  且選擇理由要寫在判決裡
- 樣本 floor 照 `sample_floor()`，跨模型版本的比較要標明

**與現有時鐘的關係**：地形扳機（~9 月第一週）結算前，問題 3 沒有
forward 資料，只能用歷史交叉表先看形狀；扳機結算後才有前瞻對照。
**問題 1、2、4 現在就可以跑，不必等。**


**結果（`v7_regime_axis.py`，Strong 訊號層 n=815，四桶物理檢查通過）**

**Q1 方向軸：不成立，B-P8 的合併無害。** TREND_UP 58.0% vs TREND_DOWN
58.5%（差 **−0.6pp**）。**與獵取相反**——那條線的方向軸差 −0.099R 且
推翻了簡化，V7 這裡沒有。**不能因為一條線有就假設另一條也有**，這是
本次最重要的負結果。

| 格 | n | WR | CI95 | bps |
|---|---|---|---|---|
| RANGING | 189 | **64.0%** | [57.0,70.5] | +13.4 |
| TREND_DOWN | 282 | 58.5% | [52.7,64.1] | +26.9 |
| TREND_UP | 176 | 58.0% | [50.6,65.0] | +14.0 |
| NEUTRAL | 168 | 56.5% | [49.0,63.8] | +16.2 |
| 全體 | 815 | 59.3% | [55.9,62.6] | +18.8 |

B-P8 的 +5.3pp 在此重現（RANGING 64.0% vs TRENDING 合併 58.3%）。

**Q2 弱側 × regime：成立，且是交互作用不是主效應。**

| 格 | UP 訊號 | DOWN 訊號 |
|---|---|---|
| RANGING | 67.2% (64) | 62.4% (125) |
| **TREND_UP** | **48.1% (77)** | **65.7% (99)** |
| TREND_DOWN | 57.1% (154) | 60.2% (128) |
| NEUTRAL | 62.3% (77) | 51.6% (91) |

**上漲趨勢裡，做多訊號 48.1%（比擲硬幣差），做空訊號 65.7%（第二好）。**
UP 訊號在 TREND_UP vs RANGING 差 **−19.1pp**。這解釋了 live 的病灶
（§0.51b：LONG −3.13% vs SHORT +2.63%）——**不是 LONG 全面差，是
LONG×TREND_UP 特別差**，而 8 月正是 TREND_UP 超額的月份。

機制上自洽：V7 是 4h 均值回歸專家，在上漲趨勢中逆勢做空（抓回調）
有效、順勢做多失效。

**證據強度（誠實）**：TREND_UP×UP 的 Wilson CI [37.3, 59.0]，**上緣
59.0% 剛好低於全體 59.3%** —— 勉強站住，不是壓倒性。兩半 37%/55%
方向一致但擺盪大。且 4 格 × 2 方向 = 8 格，這是其中最極端的一格，
**多重比較未校正**。

**Q4 格內衰退：成立，且與獵取同形狀。** 早期 60.0% → 近期 53.6%
（−6.4pp），拆解為**組成效應 −0.1pp、格內效應 −6.3pp**。逐格：
RANGING **+8.3pp**（63.1→71.4）、TREND_UP −11.7、TREND_DOWN −13.1、
NEUTRAL −4.8。**主場不但沒衰退還變好，崩的是非主場**——與 §0.58
獵取的結論同構。近期樣本小（n=97，各格 21–32），列為觀察不列為證據。

**綜合含義（尚未預註冊，等下一步決定）**
V7 的濾網若要做，**不該是「只在 RANGING 開火」**（那會丟掉 TREND_UP
的 99 筆 DOWN 訊號，那格 65.7% 是第二好），而該是**條件式**：
「TREND_UP 時只接受 DOWN 訊號」。這比獵取的濾網精細一層，也更貼機制。
但 CI 勉強、多重比較未校正，**必須預註冊 + 新樣本驗證**，不得直接上線。

**【反證，2026-08-26 同日】Q2 的規則在最近這批訊號上會扣分**

使用者觀察「最近 regime 都有抓到行情變動」，於是拿 08-17 以來的 19 筆
Strong 做實地檢驗（若套「TREND_UP 只收 DOWN」）：

| | n | WR | 平均 bps |
|---|---|---|---|
| 通過 | 8 | 75% | +18.8 |
| **被擋** | 11 | **55%** | **+23.5** |

**被擋的反而賺更多**，其中三筆是 +153.8 / +107.0 / +67.9 bps——正是
08-19~21 那波大漲（BTC 三天 +7.5/+5.2/+6.3%）。

**這不是儀器錯**：全歷史上該格的勝率與報酬完全一致
（`TREND_UP × UP` 是唯一兩者皆負的格：48.1%、平均 −8.0、中位 −15.1 bps），
而八格對照顯示其餘各格的 WR 與 bps 也彼此吻合，沒有「勝率高但賠錢」
的矛盾格。

**所以真正的問題是：這一波是不是 TREND_UP×UP 的例外，還是該格本來就
高變異？** n=11 對 n=77，單一行情段落，無法分辨。可能解釋（待驗）：
1. **趨勢強度未分層**——08-19~21 是三天 +20% 的極端段，該格歷史樣本
   多為溫和上漲；「上漲趨勢」內部可能還要再分強弱
2. 該格報酬**右尾厚**：中位 −15.1 但平均 −8.0（平均高於中位），代表
   少數大贏家撐著，濾掉它等於砍掉右尾
3. 純粹是 11 筆的運氣

**處置：Q2 不進預註冊，降級為待驗假設。** 理由——一個規則若在它被
提出後的第一批新樣本上就反向，直接預註冊等於把運氣寫成規則。
需要先回答「第 2 點的右尾」：若濾掉該格會系統性砍掉右尾，那 Q2 的
勝率優勢是假的，因為 V7 的交易層靠 trailing 讓 winner 跑（§0.51b
已證出場端的正貢獻主要來自讓利潤跑），砍右尾會直接傷害它。

**【三選一收斂，2026-08-26】右尾與趨勢強度兩個解釋都死，只剩運氣**

**解釋 #2（右尾撐著）死亡**（`v7_regime_tails.py`，四檢定一致）：
- T1 尾部佔比 **49.1%** vs 其他七格中位 42.4% → 不異常
- T3 去掉兩端各 10% 後 **−8.0 → −9.9**，八格排名 **1 → 1** → 去尾後
  **更差**，仍是最差。若靠尾巴撐，去尾應往中間靠，結果相反
- T4 偏度 +7.1，八格中排第五（中段）
- T2 拆解：該格合計 −618 bps ＝ **尾部 +932、本體 −1550** → 不是
  「少數大贏＋多數小輸」，是**本體在虧、尾部只補回一部分**
- 濾掉它：全體平均 **+18.8 → +21.6 bps**（+2.8 bps/訊號）

**解釋 #1（趨勢強度未分層）死亡**：依開火當下 24h 漲幅分三層——
溫和 44.0%/−5.2、中等 48.0%/−9.7、猛烈 51.9%/−9.1。**無單調關係、
三層全負**；08-19~21 那波（24h ~7.5%）落在最猛烈層，該層平均仍 −9.1。

**所以只剩解釋 #3：那 11 筆是運氣。** 這與統計預期一致——該格
CI [37.3, 59.0] 很寬，11 筆抽出 55% 完全在分佈之內。

**Q2 的處置（定案）：正式預註冊，檔案即註冊書**
——`research/v7_regime_q2_clock.py`（2026-08-26 凍結）。

規則、樣本 floor、閘門、判準全部凍在該檔的常數區；腳本唯一的工作是
報進度並且**在閘門未達時拒絕出判決**。那個拒絕就是重點：這個假設已經
歷過一次反向樣本，最該防的失效模式就是一路偷看到數字好看才收。

- **規則**：TREND_UP 格只接受 DOWN 訊號
- **樣本 floor**：2026-08-26。此前全部已用掉，**含那 11 筆已知反例**
  （55%／+23.5 bps，寫進註冊書的 `KNOWN_COUNTEREXAMPLE`，不得事後遺忘）
- **閘門**：該格新樣本 **n≥60** 且經過 ≥30 天
- **一級判準**：該格 Wilson **CI 上緣 < 全體點估計**
- **二級**：該格 bps<0 且濾掉後全體 bps 改善
- **附加**：兩半皆低於全體

**⚠ 判準更正（同日）**：本節原先寫「一級須 CI **下緣**仍低於全體」。
**那不是門檻**——任何寬區間都滿足它（本格下緣 37.3% vs 全體 59.3%）。
真正讓原始發現「勉強站住」的是 **CI 上緣**：59.0% < 59.3%。已更正。

**預註冊在這裡買到的東西**：原始發現是 8 格中挑最極端的一格。**事先
指名這一格、在還不存在的資料上測，是把多重比較問題「解決掉」而不是
「揭露一下」**——這是這份註冊書的全部功能，而且一旦為了迎合結果去改
格、改閘門或改判準，它就作廢。

**時程誠實說明**：不用歷史佔比硬推。該格佔 Strong 約 9.4%，但 Strong
開火率本身在變（07 月 2.0% → 解禁後 13.3%，CLAUDE.md 已記為分佈擴張
的必然而非門檻放寬）。時鐘依**實測速率**推估，跑腳本即得。

**下一步（V7 專線）**：Q2 進時鐘、不再動。剩 Q3（地形 vs regime 是否
同效應）等地形扳機結算。

**Q3（地形濾網 vs regime 濾網是否同效應）**：等地形扳機結算
（45/60，~9 月第一週）才有前瞻對照，屆時必測交叉表。

### 0.58 H 的答案：非主場崩了，主場沒事（2026-08-26，產品端回報觸發）

**產品端問（`../jarvis/產品端回報_獵取執行管線_20260826.md` 項 H）**：
回測到期組 +0.24R vs 前瞻 −0.01R，落差比執行成本還大，且 shadow log
不含執行成本所以滑價解釋不了。他們拒絕在未對齊的基準上優化出場——
**這個判斷正確**，本節先答。

**分解**（`sweep_hist_vs_fwd.py`，變體 B core9、同為 level 價、兩樣本
不重疊：回測取 FREEZE 之前、前瞻取之後）：

| 格 | 歷史 n | 歷史 R | 前瞻 n | 前瞻 R | **格內差** | 歷史佔比 | 前瞻佔比 |
|---|---|---|---|---|---|---|---|
| TREND_UP | 477 | +0.0221 | 93 | **−0.2254** | **−0.2475** | 20.5% | 27.2% |
| TREND_DOWN | 570 | +0.1076 | 37 | +0.1570 | +0.0495 | 24.5% | 10.8% |
| NEUTRAL | 491 | +0.1011 | 57 | −0.0491 | −0.1502 | 21.1% | 16.7% |
| **RANGING** | 793 | **+0.0983** | 155 | **+0.0933** | **−0.0050** | 34.0% | 45.3% |
| 全體 | 2331 | +0.0856 | 342 | −0.0102 | −0.0958 | | |

**落差 −0.0958 的來源：regime 組成只佔 7%，格內衰退佔 75%。**
「窗口 regime 倒楣」這個最省事的解釋**被推翻**。

**但真正的結論在最後一列**：**主場（RANGING）兩期一致**
（+0.0983 → +0.0933，差 −0.005），崩的是 TREND_UP（−0.2475）與
NEUTRAL（−0.1502）。所以不是「edge 死了」也不是「運氣不好」，是
**非主場崩了、主場完好**——§0.54 附件的判讀規則只有這兩種可能，
**第三種要補進去**。

**證據強度的誠實說明**：前瞻各格的日聚類 CI 全部含零（RANGING
[−0.022,+0.196]、TREND_UP [−0.374,+0.054]），單期不足以下結論。
**支撐這個讀法的不是任一期的 CI，是兩個獨立樣本的一致性**——主場
在兩期之間穩定、非主場在兩期之間翻轉。

**對 Gate F 的含義**：若判 FAIL，附件必須指出是「非主場拖累」而非
「主場失效」，兩者的後續行動不同。§0.54 判讀規則補第三分支。

### 0.59 regime 進場濾網（2026-08-26 預註冊，跑數之前凍結）——效益是出場優化的 13 倍

**動機**：§0.58 顯示只在主場開火的前瞻 meanR 是 **+0.0933**，對比全體
**−0.0102**，差 **+0.1035R**。對照產品端項 G 的最佳出場變體
（fail_fast +0.0078R）——**13 倍**。「讓策略發揮最大效益」的最大槓桿在
進場的 regime 條件，不在出場。

**凍結規則（一個參數都不新創）**
- 進場條件加一條：開火當下該幣的 **ADX(14) 標籤 == RANGING**（<20）
- ADX 25/20 與四格定義沿用 §0.49d／§0.54b，已凍結
- 檔位只有一檔（開/不開），**不做 0.5 減碼版**——那是縮帆線的事，
  混在一起會無法歸因

**凍結預測**
- R-P1：新樣本的 RANGING-only meanR > 全體 meanR，差 ≥ +0.05R
- R-P2：RANGING-only 的日聚類 CI 下緣 > 0
- R-P3：筆數約降至 45%（前瞻佔比），**這是預期成本不是失敗**

**判準（沿 §0.49d 兩級制）**
- 一級：符號正確 ∧ 差 ≥+0.05R ∧ 逐幣同號 ≥6/9
- 二級（可議進 executor／產品端）：CI 下緣離零
- 樣本守門：**新** forward ≥ 30 天且 n ≥ 150

**紀律上最關鍵的一條**：本規則是**看過現有 forward 樣本之後**提出的，
所以**那批樣本已經被用掉，不得再當證據**。判決只能用
**2026-08-26 之後**新累積的 fills。這正是 C/D 變體踩過的坑
（凍結時統計量最漂亮的 D，forward 最慘），不重蹈。

**判決清單追加：出場變體按格重測（2026-08-26，`exit_by_regime.py`）**

原本的 exit_variants 九變體是在**全歷史**上評分的，而 §0.58 證明衰退
幾乎全在非主場——所以那個「fail_fast +0.0078R」的頭條是在一個已經
不存在的世界量的。按四格重跑（同一批 fills、只換出場，paired）：

| 格 | n | baseline | 唯一過關的變體 | ΔR | 廣度 | 兩半同號 |
|---|---|---|---|---|---|---|
| **RANGING** | 1108 | +0.0907 | **fail_fast** | **+0.0138** | 7/9 | ✓ |
| **TREND_UP** | 782 | +0.0232 | **hold_4** | **+0.0260** | 7/9 | ✓ |
| TREND_DOWN | 878 | +0.1380 | 無 | — | — | — |
| NEUTRAL | 693 | +0.1110 | 無 | — | — | — |

**關鍵結論：濾網與出場是互補不是競爭。** 濾網管「別進 TREND_UP」，
fail_fast 管「進了主場之後怎麼走」——後者在主場獨立有效
（+0.0138R，比全歷史頭條高 **77%**，原數字被非主場稀釋了）。
`hold_4` 在 TREND_UP 最有效，但那格若被濾網擋掉就用不上，**列為
濾網 FAIL 時的備案**，不與濾網同時上。

**探索性聲明**：9 變體 × 4 格 = 36 次比較，p<0.05 下約兩格會僥倖過關。
`fail_fast`@RANGING 是其中最乾淨的（兩半同號＋7/9 廣度），但**最乾淨
不等於已證明**。與 §0.59 主規則同批用新樣本驗證。

**記帳已就緒（2026-08-26）**：`shadow_engine` 新增 `regime_cell` 欄位
（凍結 ADX 四格，純時間查表），新 fills 從此帶著 regime 標籤落盤——
不必等判決日回頭重算（「前瞻值而非回算值」的既有規矩）。
驗證：凍結算術一字未動（n=1118、meanR −0.0224，改動前後同值）。

**踩到並修掉的一個坑（值得記）**：原本想把 `fail_fast` 的 R 也標進
同一列，結果標出 **−0.196R**，與 paired 重測的 **+0.014R** 反號。查
儀器發現 `exit_variants.entries()` **沒有** sweep_core 的 `last_exit`
非重疊約束（它自己內部 paired 所以無妨），兩邊的 fills 是**不同母體**，
用 fill_ts 對接等於錯配。**已移除該欄位**；判決日的 fail_fast 分數一律
由 `exit_by_regime.py` 產生（它全程 paired）。教訓同 mistake.md
2026-08-02：跨檔案貼標籤前先問「兩邊的母體是同一個嗎」。

**判決日要回答的三件事**（同一批新樣本，一次跑完）：
1. RANGING-only 的 meanR 是否 > 全體，差 ≥+0.05R（R-P1/P2/P3）
2. RANGING-only ∧ fail_fast 是否 > RANGING-only（追加）
3. 兩者疊加後是否仍滿足二級判準（CI 下緣離零）

**與 Gate F 的關係**：Gate F 判的是**變體 B 本身**，不因本節改變。
本節若過關，是 B 之上的一層濾網（等同新變體），需要自己的 forward
證據，**不得用來救 Gate F 的判決**。

### 0.59b 主場定義修正：RANGING → RANGING ∪ TREND_DOWN（2026-08-26，同日修正）

**觸發**：產品端 `產品端詢問_主場定義是否納入TREND_DOWN_20260826.md`
指出濾網會把 TREND_DOWN 一起擋掉，而該格前瞻 meanR 高於主場。**他們
沒有自己動手改**，要求研究端裁示——這個紀律是對的。

**這是研究端的錯，不是產品端的新發現。** §0.59 的證據來源是 §0.58 的
分解，其判準是「哪些格在兩個不重疊樣本間撐住」。**把那個判準誠實套到
它自己產生的表上，選出的是兩格不是一格。** RANGING-only 是從生存條件
層 §0.49（ADX **無方向的** RANGING vs TRENDING）帶過來的殘留，而那個
簡化正是 §0.54b 在這條線上已經推翻過的。§0.54b 當時就寫了
「TREND_DOWN +0.112R 跟主場一樣好」——**已知但沒接進規格**。

**不採用產品端的 t 值表裁決**。那張表是看過前瞻樣本之後算的，靠它決定
就是 C/D 陷阱。裁決回到 §0.58 的兩樣本一致性——它早於那張表。

**判準（`raid_home_regime_def.py`，跑數之前凍結）**：一格入選須同時
- C1 兩個不重疊樣本 meanR **皆為正**
- C2 **回測**樣本逐幣廣度 ≥5/9（只對大樣本要求；前瞻太薄，在那裡要求
  廣度等於對雜訊做篩選）
- C3 **回測**樣本日聚類 CI 下緣 > 0（同上理由）

| cell | 回測 n | 回測 R | 廣度 | 回測日聚類 CI | 前瞻 n | 前瞻 R | 判定 |
|---|---|---|---|---|---|---|---|
| RANGING | 793 | +0.0983 | 9/9 | [+0.050,+0.145] | 155 | +0.0933 | ✓✓✓ 納入 |
| **TREND_DOWN** | 570 | +0.1076 | 8/9 | [+0.040,+0.178] | 39 | +0.1580 | ✓✓✓ **納入** |
| NEUTRAL | 491 | +0.1011 | 8/9 | [+0.040,+0.165] | 59 | −0.0346 | ✗✓✓ 排除 |
| TREND_UP | 477 | +0.0221 | 5/9 | [−0.041,+0.086] | 93 | −0.2254 | ✗✓✗ 排除 |

**反證關抓到的事（產品端的表看不出來）**：TREND_DOWN 的**前瞻**樣本
**脆弱**——前瞻總 R +6.16 之中 **65% 來自單一 UTC 日**，逐幣只有 4/9
為正。所以產品端看到的 +0.1832 / t=+1.41 **實質是一天**。裁示因此
**不建立在前瞻數字上**：C1 只要求前瞻的**符號**不反對，證據重量全在
回測那 570 筆（廣度 8/9、CI 明確離零，與 RANGING 同級）。

**最不舒服的一點，寫出來而不是藏起來**：NEUTRAL 的回測輪廓與
TREND_DOWN **幾乎相同**（+0.1011 / 8/9 / [+0.040,+0.165]），兩者的
唯一分野是前瞻符號，而 TREND_DOWN 的前瞻符號站在一天上。支撐這個
分野的不是那一天，是另外兩條**獨立於本次分析**的證據：
1. §0.54b 在不同問題上獨立發現 TREND_DOWN 與主場同級
2. 本專案跨策略的多空不對稱先驗（`project_short_is_the_edge`：V7 的
   edge 也在空側）——上漲趨勢持續、下跌較易均值回歸

**NEUTRAL 就此封死**：日後不得因為「它回測也很好」而追加。要加必須有
**新的**前瞻證據，且走完整預註冊。這條寫在這裡就是為了擋定義蠕變。

**敏感度（無條件報告）**：若 TREND_DOWN 真實值是 0，聯集 meanR 從
+0.1063 退到 **+0.0745**（RANGING 單格 +0.0933）——代價 **−0.019R**，
換到 **+25% 訊號量**。錯的代價有界，而訊號量直接決定 §0.59 判決日
多久到得了 n≥150。

**前瞻樣本上的三個選項**（僅供理解量級，**非證據**）：

| 規則 | n | meanR | 日聚類 CI | 訊號量 |
|---|---|---|---|---|
| RANGING 單格（原規格） | 155 | +0.0933 | [−0.021,+0.195] | 45% |
| **RANGING ∪ TREND_DOWN** | 194 | +0.1063 | [+0.003,+0.203] | **56%** |
| 不濾 | 346 | −0.0069 | [−0.116,+0.097] | 100% |

**判決結構（階層式，現在就定，不留事後選擇權）**
- **主假設**：`home_regime = {RANGING, TREND_DOWN}`，判準沿 §0.59 不變
- **附件**：判決日逐格報告（`regime_cell` 已逐筆落盤），這是**分解不是
  選擇**
- **備援**：RANGING-only 只有在「聯集 FAIL **且** RANGING-only 以**自己
  的** n≥150 獨立通過同一組門檻」時才可取而代之——這是額外的門檻，
  不是免費的第二次機會
- **移除條款**：若 TREND_DOWN 在 08-26 之後的**新**樣本上 meanR 為負，
  直接移出主場，不需要等聯集判決

**R-P3 修正**：筆數預期從「約 45%」改為**約 56%**。

### 0.57 Gate F 隱藏成本：進場時機落差（2026-08-24，產品端回報觸發）——判決前必須解決

**產品端回報（jarvis CLAUDE.md 2026-08-23）**：實盤 17 筆被 drift 濾網
擋掉，偏離中位 **2.80%**（最小 1.12%，<1% 者 0 筆）。「60 秒輪詢不可能
造成 2.8% 位移 → 破口在訊號發布時間差，產品端調輪詢無效，要回頭找研究線。」
**這個診斷正確。** 量測結果比他們估的更嚴重：

- **發布延遲**：raid_signals_live 實測 lag 中位 **342 分鐘**、最新筆 132
  分鐘。RaidBot 時效窗是 4 小時——**中位延遲已超窗，訊號到就過期**。
  部分主因是班車 08-20~24 停擺（排程被停用，已修復重啟），但即使班車
  準時，結構下限仍是 **65 分鐘**（fill bar 收盤 +1h，班車 +5min）。
- **結構性進場落差（新量測，變體 B 近 400 筆）**：回測進場價（level±滑價）
  vs 該 bar 收盤價（實盤最快可行動點）= 中位 **+0.19%**、平均 **+0.27%**、
  19% 的訊號劣化 >1%。以停損 3.5 ATR 換算約 **0.077R**。
- **對照變體 B 的 meanR +0.035R —— 落差是 edge 的兩倍。**

**含義（判決前必須處理，不是判決後）**：Gate F 現行成本模型（逐幣真實
bps 費用，2026-07-28 修正）**不含這一項**。若不處理，即使 n=1400 判 PASS，
那個 PASS 描述的是一個**實盤拿不到的進場價**——與 2026-07-28 抓到的滑價
符號 bug 同族（「宣稱含成本但實質零成本」），只是這次漏的是時機成本。

**量化結果（2026-08-24，`research/sweep_realizable.py`，不動凍結規則的平行重算）**

| 進場 | n | meanR | sumR | 日聚類 CI95 | 逐幣廣度 |
|---|---|---|---|---|---|
| 凍結（level） | 2396 | **+0.0839** | +201.1 | [+0.052,+0.117] | **9/9** |
| 可實現（fill bar 收盤） | 2396 | **−0.0489** | −117.2 | [−0.076,−0.023] | **3/9** |

**發布閘門成本 = 0.1328 R/筆 = 凍結 edge 的 158%。** 可實現版 CI 整段在
零下——**批次發布架構下，變體 B 不是沒有 edge，是顯著虧損**。

機制（也解釋為何比先前估的 0.077R 更大）：sweep-failure 的 edge 有一大
部分發生在 **fill bar 之內**——回踩 level 後往策略方向走，那就是它賺的
東西；等 bar 收盤，這段已經兌現，進的是 edge 之後的價格。低 ATR 幣受傷
更重（同一位移除以較小的 risk＝更大的 R 損失），同 2026-07-28「統一 ATR
單位奉承低波動幣」的結構。

**三條路因此壓成一條**：改回測基準＝直接宣告 FAIL；判準加扣 0.133R＝
加扣後 CI 整段負，同樣 FAIL；**只剩改發布架構**。

**正確形狀：發布「等待清單」而非「已成交訊號」**。問題不在市場而在架構
——RaidBot 每 60 秒輪詢價格，有能力即時反應，是研究端只在每小時批次
告訴它「已經成交的訊號」。改法：掃穿當下即發布「此 level 在等回踩、
觸價即進、有效 W 根」，產品端自行監控觸價。進場價回到 level 價＝與回測
一致，0.1328R 落差歸零。研究端已有 `detect_sweeps` 與 level 計算，
工程量不大；`raid_signals_live` 需新增 pending-level 欄位族。

**在此改動落地前，Gate F 的 forward 累積數字不代表可實現報酬**——
時鐘照走（樣本仍有效，是同一批訊號），但判決必須在可實現基準上做。

**三條路（尚未選，需使用者定調）**：
1. **改回測基準**：進場價改用 fill bar 收盤價重算全歷史 → Gate F 判準
   在新基準上重新校準（最誠實，但等於承認過去的數字偏樂觀）
2. **改發布架構**：intrabar 偵測 retest touch、觸價即發（把 65 分鐘壓到
   秒級）→ 回測假設才成立。是新工程，且需要即時價格串流
3. **接受並標註**：Gate F 判準加一條「扣 0.077R 後仍需 CI 下緣 >0」
   —— 最便宜，但會讓 PASS 門檻實質提高一倍以上

**不可做**：拿現行 shadow log 的 meanR 直接當可實現報酬——那是本節記錄
的錯誤本身。

### 0.51b V7 出場：可解的部分只有 15%，其餘是進場問題（2026-08-25 量化）

**使用者反映「V7 出場問題遲遲沒解決」**。live 全歷史出場歸因（n=20）：

| 出場 | n | 勝率 | 平均 | 合計 |
|---|---|---|---|---|
| **trail_stop** | 13 | 31% | −0.42% | **−5.40%** |
| opp_signal | 4 | 75% | +1.19% | +4.76% |
| conviction_decay | 2 | 50% | +0.07% | +0.14% |

**不可直接讀成「trailing 很爛」**（§0.51 記過的選擇偏誤：走到 trail_stop
的正是沒等到反向訊號的那批）。改用反事實重放——13 筆若有 decay(2 根)：

- **觸發 2/13（15%）**，合計 −5.40% → **−3.25%**（改善 **+2.15%**）
- 效果換算全體：21 筆從 −0.50% 翻正為 **+1.65%**（符號翻轉）
- **但 11/13 完全沒觸發** —— 持倉期間模型從未連續反向兩根：
  **decay 只能救「模型改變主意」的虧損，救不了「模型堅持、市場不同意」的**

**病灶其實在方向，不在出場**：

| | n | 勝率 | 合計 |
|---|---|---|---|
| trail_stop · LONG | 9 | 22% | **−4.91%** |
| trail_stop · SHORT | 4 | 50% | −0.49% |
| 全部 · LONG | 13 | 31% | −3.13% |
| 全部 · SHORT | 7 | 57% | +2.63% |

同一套出場規則、同一個停損倍數，**差別只在方向**——與
[[project_short_is_the_edge]]（2026-06-07）完全一致。

**結論（出場端的天花板）**：
1. **decay 移植值 +2.15%，是出場端唯一剩下的正貢獻**（規格已於 08-24
   交接至 `../jarvis/風控_同向上限_規格.md`，feed 的 `pred_sign` 已上線，
   待產品端實作）
2. **做完就到頂**——剩下 85% 的虧損出場救不了
3. 真正的槓桿是**進場過濾**：地形濾網 45/60，九月第一週結算

### 0.54c 「何時切向做空」——NO-GO，且覆蓋缺口本身不存在（2026-08-24）

**產品端需求③**（jarvis CLAUDE.md 08-23）：「下跌只有 V7 守…現成解是
磨坊的做空循環，但何時切向需要 regime 規則 → 屬研究線課題。」

**兩條可能路徑，都關**：
1. **預測轉折**：已於 2026-08-20 證偽——ADX 對未來 regime 無前瞻力
   （Kaufman ER gap −0.007/−0.010/−0.011，CI 含零，廣度崩到 2/29；
   Lo-MacKinlay VR gap −0.028，廣度 1/9）。
2. **偵測當下＋跟進**（比 1 弱得多的主張，本節新測）：持續性確實存在
   （`P(TREND_DOWN→同, 8h)=67.4%` vs 隨機 25.4%），**但方向錯**——

| 狀態 | 時間佔比 | P(8h 後同) | P(24h 後同) | **前瞻 24h 報酬** |
|---|---|---|---|---|
| TREND_DOWN | 25.4% | 67.4% | 37.3% | **+0.021%** |
| TREND_UP | 23.0% | 63.2% | 32.6% | +0.251% |
| RANGING | 32.0% | 68.6% | 43.1% | +0.076% |

**偵測到下跌趨勢之後，前瞻 24h 報酬是正的**（+0.021%）。切向做空 =
在一個平均正報酬的環境放空，預期虧損。狀態會持續，但**價格不跟著
持續**——這正是 08-20「ADX 是溫度計不是天氣預報」的第二次確認。

**更重要的是：覆蓋缺口不存在。** §0.54b 同日量測顯示獵取在
**TREND_DOWN 的 meanR = +0.112R（8/9 幣，CI 離零）**，與主場 RANGING
（+0.101R）齊平——**下跌趨勢時獵取本身就是守備**。產品端「下跌只有
V7 守」的判斷來自 B-P9 的簡化敘事（TRENDING＝逆風，不分方向），那個
敘事在同一天被推翻。**不需要新策略，也不需要切向規則。**

真要在下跌時再降風險，正確工具是**減碼**（縮帆線 §0.52/§0.53，
09-16/19 判決），不是反向開倉。

### 0.54b 方向軸：只有上漲趨勢是逆風（2026-08-24，產品端提示觸發）

**產品端回報**（jarvis CLAUDE.md 08-23，變體 C／shadow 窗）：震盪
+0.083R、下跌 −0.118R、**上漲 −0.330R**，並要求「Gate F 判決必須按
regime 分層，否則樣本期的 regime 組成會決定判決結果」。

**在變體 B（Gate F 正式軌道）全歷史上驗證**（`sweep_regime_directional.py`，
ADX 25/20 × 同期 24h 報酬符號，不新增門檻）：

| 格 | n | 佔比 | meanR | 日聚類 CI95 | 廣度 |
|---|---|---|---|---|---|
| RANGING | 822 | 34.3% | +0.1013 | [+0.054,+0.146] | **9/9** |
| **TREND_DOWN** | 576 | 24.0% | **+0.1116** | [+0.047,+0.180] | 8/9 |
| NEUTRAL | 504 | 21.0% | +0.0939 | [+0.035,+0.155] | 8/9 |
| **TREND_UP** | 494 | 20.6% | **+0.0124** | [−0.047,+0.074] | **4/9** |

**方向性確認（gap −0.099R），但推翻了 B-P9 的簡化敘事**：「TRENDING 是
逆風」把兩個完全不同的母體平均了——**TREND_DOWN 跟主場一樣好**
（+0.112 vs RANGING +0.101，CI 離零、8/9 幣），**只有 TREND_UP 是問題**
（唯一 CI 含零、廣度崩到 4/9）。與產品端的差異（他們下跌也是負的）
可歸因變體 C ＋ 4 週窗；方向的**排序**兩邊一致。

**forward 視窗（27 天，附件已接方向軸）**：TREND_UP 佔比 **30.0% vs
base rate 22.0%（超額 36%）**，meanR **−0.1906**、MDD **−80.85R**——
**全部回撤集中在這一格**。TREND_DOWN +0.102、RANGING +0.001。
這就是產品端擔心的事的實體化：**累積窗口恰好碰上超額的上漲趨勢**。

**接線**：`sweep_regime_attach.py`（clocks 2e-h）四格改為 TREND_UP /
TREND_DOWN / NEUTRAL / RANGING，判讀規則改看 TREND_UP。判決日必須三格
並陳。**B-P9 的告警語意需連帶修訂**——天氣站「TRENDING＝獵取逆風」
對下跌趨勢是錯的，只有上漲成立（產品端規格的「打對折」註記同步升級為
「只在上漲趨勢成立」）。

### 0.54 Gate F 判決附件：regime 分解（2026-08-20 註冊）——附件不是判準

`research/sweep_regime_attach.py`（clocks 2e-h 每週跑）。變體 B netR 按
逐幣同期 ADX 狀態分桶＋窗口 regime 組成 vs 29 個月 base rate。**Gate F
的凍結判準一字不動**；附件的職責是讓判決可讀——FAIL 時分辨「edge 死了
（主場 RANGING 也虧）」vs「窗口 regime 倒楣（虧損集中 TRENDING、RANGING
仍正）」，兩者的後續行動相反。判讀規則預先寫死（見腳本 docstring），
且帶充分性守門：**span ≥60d ∧ RANGING 日聚類 CI 離零**才render判讀
（首跑就靠這道守門擋下一個過早的 edge-dead 判讀——23 天 ≈ 3 個有效
episode）。

**首跑標記的觀察（2026-08-20，無判讀）**：23 天窗口 TRENDING +0.061 >
RANGING −0.005——**與 B-P9（2.5 年回測，CI 級）反向**。CI [−0.109,
+0.093] 含零，可能只是 3 episode 的雜訊；若隨 span 增長持續反向，
這個對話要在判決日**之前**發生。兩個候選解釋待分辨：樣本母體不同
（B-P9 用全歷史 archetype 重放、這裡是 pierce 濾網後的 forward 記錄）
或 B-P9 本身的條件效應不穩（§0.52 的 PRE/POST 符號翻轉是第二個同款
訊號——兩處同時出現，這條線索升溫）。

**同日結案（分辨測試，`bp9_discriminate` 掃描）**：兩個解釋當天可分，
跑了——**判窗口雜訊（解釋 b），警報降級**：
- sanity：core 群組完美重現 B-P9（RANGING +0.0754 / TRENDING +0.0130，
  8/9 幣，與註冊值一致）→ 儀器可信
- **變體 B 自己的全歷史條件輪廓（首次量測）**：RANGING +0.1009 >
  TRENDING +0.0692，gap +0.032、8/9 幣、**2024/2025/2026 三年逐年
  全部 R>T 零反轉**。23 天 shadow 的反向確定是 ~3 episode 的窗口
  雜訊，不是母體差異、不是條件效應不穩
- 附帶收穫：pierce 濾網把 TRENDING 桶從 +0.013 拉到 +0.069——
  **變體 B 對 regime 的敏感度比 core 低一半**（gap 0.062→0.032）。
  B-P9 告警是照 core 校準的，對實際交易群組的逆風程度**高估約一倍**；
  告警保留（方向仍對）但解讀時記得這個折扣
- §0.52 的前提不受影響：全歷史上 TRENDING 仍是較弱桶，且其波動 +22%
  的機制證據獨立於此。PRE/POST 符號翻轉最可能同為窗口雜訊，判決日
  照原計畫（09-16）
- 附件（2e-h）照常每週盯——這次結案靠的是全歷史；若 forward span
  拉長後仍持續反向，那會是「市場結構改變」的新問題，不是舊問題復發

### 0.53 縮帆線·波動目標化（vol targeting）預註冊（2026-08-20，跑數之前凍結）——統一風控層第一個實體元件

**動機**：使用者定調「只要減少受傷幅度，讓所有策略降低風險」。當日儀表
排名（core9，目標＝forward 8h 實現波動）：trailing 24h vol IC **+0.464~
+0.493（9/9）**、168h 慢波動增量 **+0.186（9/9）**、ADX 增量 +0.085、
F&G 增量 +0.080（已控制快+慢波動，非波動殘餘 89%）。主力是波動本身；
ADX 減碼（§0.52）只是它的二元粗糙近似。本規則不需要任何預測力，只需要
波動自相關——今天全部死掉的測試都死在「需要前瞻力」，這條不需要。

**凍結規則（寫下時未跑過任何重放數字）**
- `σ_fast` = 進場前 trailing **24h** 1h 報酬標準差
- `σ_slow` = 進場前 trailing **168h** 同上
- `σ_hat = (σ_fast + σ_slow) / 2`（等權，不調；兩者都已各自證明，等權是
  唯一不引入自由度的組合）
- `σ_ref` = trailing **720h**（30d）的 σ_hat 中位數（純 trailing，無前視）
- **權重 `w = clamp(σ_ref / σ_hat, 0.25, 1.0)`**——上限 1.0 = **只減不加**
  （低波動不加碼，同 §0.52 的理由：加碼是另一個未經證明的主張）；
  下限 0.25 = 防止除以極端值把部位歸零（歸零＝變相停火，那需要另外的證據）
- 所有參數（24/168/720/0.25）一次寫死，**不掃**（mistake.md 2026-06-20）

**凍結預測**
- V-P1：重放 SF shadow log，MDD 較 base 改善
- V-P2：贏過曝險匹配的無差別減碼對照（同 §0.52 首跑補的那個 null——
  這次**從第一天就在判準裡**，不是事後補）
- V-P3（對打 §0.52）：**每放棄 1R 報酬換到的 MDD 改善**優於 ADX 二元
  減碼——連續波動縮放應該比二元檔位更準；若輸，vol targeting 撤回、
  ADX 版留任（兩者不並列，儀表不通脹）
- V-P4：總報酬**會下降**（預先寫死，防事後把它讀成失敗）

**判準（沿 §0.52 修訂版全套，含首跑補的兩道守門）**
- 一級：POST-freeze MDD 改善 ≥10% 相對值 ∧ 報酬回吐 ≤ MDD 改善一半
  ∧ 贏過無差別對照
- 樣本守門：POST-freeze 跨度 ≥ **30 天**（fills 時間聚類，不足即拒判）
- 二級（可議進 executor / 產品端）：MDD 改善的日聚類 bootstrap CI95 離零
- **判決分界 = 本節 commit 日（2026-08-20）**：之前的 fills 全部 in-sample
  只列參考；判決只看之後。判決日 ≈ **2026-09-19+**
- 未達一級 → 撤回，不改 clamp、不換窗口重跑

**範圍註記**：首輪只重放 SF shadow log（n=847，唯一夠大的樣本）。V7 live
n=21 不夠判；網格在產品端無研究線樣本。**若過關，這層的歸宿是統一風控
框架（PORTFOLIO_RISK_FRAMEWORK）而非單一策略**，屆時三策略（含 jarvis
產品端）共用，需另走產品端同步。

**首跑記錄（2026-08-20，`research/vol_target_shadow.py`）——無判決，
POST 樣本 0 筆（凍結日即今日，全部 fills 都在 PRE）**

PRE in-sample 參考（n=847、22.9d，僅記錄）：

| | base | vol-tgt | 無差別null | ADX×0.5 |
|---|---|---|---|---|
| netR | +29.49 | +26.31 | +27.92 | +18.79 |
| MDD | −24.98 | **−23.21** | −23.65 | **−20.10** |

觀察（不是結論）：平均權重 **0.947**——median 基準的 clamp 讓規則大部分
時間近乎不介入，只在波動尖峰收縮，所以它是「尖峰剪刀」不是「常態減碼」。
in-sample 上 MDD 每 1R 回吐換到的改善：vol-tgt 0.022/R vs ADX 0.018/R
（vol-tgt 略優但兩者都弱）；vol-tgt 的 MDD 有贏 null（−23.21 vs −23.65）
但報酬輸 null（26.31 vs 27.92）——**若 forward 重現這個型態，V-P2 會 FAIL**，
先寫在這裡免得到時候找理由。判決日 ≈ 2026-09-19+（forward 滿 30d）。

**單邊行情覆蓋檢查（2026-08-20，使用者問「這確定能救網格/獵取嗎」；
in-sample 機制檢查非判決）**：用凍結的 trend_z |z|≥2 標單邊時段（v1 用
14d ER>0.4 全桶空＝門檻物理不可達，按 2026-08-02 規矩判儀器壞換凍結
定義）。core9×29 個月×網格代理：單邊佔 3.8% 時數但每小時虧損強度 ~14×；
**單邊時段波動分位 65-88 → 剪刀確實張開**（w 0.744 vs 平時 0.903），
虧損幣的單邊段虧損被砍 18-57%（合計 34%）。「慢磨型單邊不觸發」的
疑慮比預想小，但 34% 是減震不是免疫——機制性虧損（逆勢開火）只被
縮小不被消除，最後防線仍是 kill switch / 庫存上限。

**過關後的挑戰者路線（2026-08-20 使用者指出，僅登記不啟動）**：縮帆的
σ̂ 概念上就是 Mag 模型在做的事（|return|/realized_vol 的幅度預測），
差別是笨後視鏡 vs 訓練預測器。**§0.53 若過關＝07-29 否決波動預測時缺的
那顆旋鈕從此存在**，被封存的兩個波動預測器（Mag、撤單流 TEST B）都取得
「挑戰 σ̂」的資格。屆時的關卡照今天 F&G/ADX 的同一套：**控制 trailing
vol（IC +0.46，9/9）之後仍須有增量**，且 Mag 有 drift 前科（04-13 IC
0.30→0.10）而笨基線永不衰退。前置工程：Mag 的 σ 單位輸出落庫（§0.0）。
範圍：Mag 只有 BTC 4h，最多挑戰 V7 側的 σ̂，不及 core9 全線。

### 0.49e 網站同步（✅ 完成 2026-08-17）

生存條件層是新研究結論上牆，須同步 product-site（分支 master）：
dashboard 加「生存條件」卡片——三個儀表現值（ADX regime + TRENDING
幣數、突破派 PAID 幣數、趨勢派狀態）+ 狀態標籤（ADX=已驗證·二級，
其餘=一級·顯示）。資料走 agent-mcp `/public/*` 唯讀端點（需在
agent 側加一個讀 clocks 產物或即時算的 endpoint——注意 agent-boundary：
純讀 DB/快取計算可以，不得 import 交易路徑）。公開面規則照舊：只出
狀態與百分比，不出部位與金額。TG bot 的 /clocks 類指令若有也帶上。
**完成記錄**：鏈路 = `weather_station_publish.py`（搭 SweepShadow 每小時班車）→ `weather_station` 表 → agent `/public/weather-station`（已上線回 200）→ 網站 `WeatherStationCard`（dashboard，V7FilterCard 下方，證據等級 chip 照規則渲染）。product-site commit ee0dffe。TG bot 未做（低優先，另議）。

### 0.49 生存條件卡（2026-08-17 預註冊，跑數之前凍結）——「策略何時失效」研究線

**定位（使用者 2026-08-17 定調）**：所有人都在把策略優化到結構性上限，
差異化的能力是**知道策略的 edge 何時活著**。績效監測在薄 edge 下是數學
死路（+7bps/筆 vs ~100bps/筆波動 → 分辨生死需數百筆 = 數年），所以測的
不是「還賺不賺」，是「**賺錢的機制前提還在不在**」——前提直接可觀測，
延遲天級不是年級。方法論沿地形戰役（先寫預測 → 凍結分桶 → 全格報告）。

**兩個已付學費的禁區**：不用 ML 學失效條件（regime 子模型 BEAR AUC
0.378、meta-labeling AUC 0.458——「何時」本身非平穩，學了就是把問題
搬上樓再犯一次）；條件必須機制推導、事前凍結、categorical（2026-06-20）。

**凍結定義（全部先寫死）**
- **trend_z(t)** = (ln C(t) − ln C(t−720)) / (σ₁ₕ(t)·√720)，σ₁ₕ = trailing
  720 根 1h log 報酬的 std。純 trailing、無前視。720 根 = 30 天。
- 分桶（絕對門檻，不從資料取分位）：**CALM |z|<1 / MID 1≤|z|<2 /
  TREND |z|≥2**。
- 資料：V7 = tracked_signals 全部有結果的 Strong（主）與 Moderate（副），
  BTC z 取 `.cache/BTCUSDT_1h.csv`；策略 #2 = sweep_core 凍結規則在
  core9 快取 CSV（2024-01→今）重生成的全部歷史交易，逐幣自身 z。
- 統計：日聚類 bootstrap（2000 次）on CALM−TREND 差；全格報告不挑格；
  **空桶或佔比>90% 先當儀器壞**（2026-08-02）。

**卡 1：V7（機制＝4h 均值回歸、空側為主）**
- 前提：市場非單邊強趨勢（回歸專家在趨勢裡必死）
- V7-P1：Strong 勝率 TREND 桶 < CALM 桶 ≥5pp
- V7-P2：單調性 CALM ≥ MID ≥ TREND
- V7-P3：最差格 = **逆勢方向 × TREND**（在 z≥2 的上升趨勢裡開 DOWN、
  鏡像同理）；報告完整 3×2 格（bucket × 順/逆勢）

**卡 2：掃單失敗（機制＝竭盡回歸——「該來的競速沒來」）**
- 前提：雙邊流動性供給正常；失效型態 = 單邊流 regime（掃單變真突破）
- SF-P1：pooled netR TREND < CALM
- SF-P2：逐幣 (CALM−TREND)>0 同號 ≥6/9
- SF-P3（機制可觀測）：**回填率**（刺穿後 W 根內回到水位的比例）在
  TREND 桶顯著低——趨勢裡刺穿不回頭，策略連進場機會都消失

**驗過才接線**：任一卡通過自己的預測 → 才把該前提變數接進 clocks 週報
（顯示 + 告警）；不過 = 該前提假說死，換機制推導的下一個候選另行註冊。
記分程式與本節同 commit（C/D 教訓）。

**判決（2026-08-17 當天）：兩卡皆未達接線標準，一個機制假說陣亡**

卡 1 V7（Strong n=796 / Moderate n=1398，z 全覆蓋、無空桶）：
- P1 點估計過（CALM−TREND：Strong +6.0pp、Moderate +11.2pp）**但 CI 都
  跨零**（Strong [−14.7,+36.0]）——TREND 桶只有 n=22/51，BTC 在樣本期
  |z|≥2 的時間僅 ~3%。P2 Strong 單調 ✓、Moderate ✗（52/56/41）。
- **註冊時的疏漏要認**：P1 只寫了「≥5pp」沒寫 CI 要求。點估計技術上
  過，但拿 n=22 的桶宣稱驗證正是本 repo 記過的罪，**判不接線**。
- 網格裡有一個機制一致的細節：CALM×逆勢是全表最好的格
  （64% / +26.6bps / n=271）——均值回歸專家的簽名式長相。
- 下一個檢查點：TREND 桶累積到 n≥50 再重跑（被動等市場給樣本，
  或 BTC 真的走出一段強趨勢時主動跑）。

卡 2 掃單失敗（重生成 6,893 筆歷史交易 + 12,666 個掃單事件）：
- SF-P1 符號過（+0.0285）但 CI [−0.075,+0.144] 跨零；SF-P2 恰好 6/9；
  pooled 非單調（MID +0.014 < TREND +0.020）。
- **SF-P3 死了，而且死得乾脆**：回填率 CALM 88% / MID 87% / TREND 89%
  ——**趨勢裡刺穿照樣回頭**。「單邊流讓掃單不回填」這個機制描述是錯的；
  若掃單失敗在趨勢裡衰退，路徑是**回測之後的漂移消失**，不是進場機會
  消失。這改寫了下一張卡該監測什麼：不是回填率，是回填後續航。

**橫向教訓（這條線的固有困難，第一次撞上就記下）**：「何時失效」的
樣本天生稀少——regime 轉換次數遠少於交易筆數，TREND 桶佔比只有 3-4%。
這正是為什麼它難、為什麼別人不做；耐心是這條線的入場費。

**§0.49b V7 衰退分解（2026-08-17 預註冊，跑數之前凍結）——第一個應用**

CLAUDE.md 掛著「進場準度在衰退」（全期 59.5% → 近 90d 53.7%）。嫌疑：
近 90d 樣本大半由**鎖死的解碼**在弱側開火（UP 壓倒性多數，而 LONG 是
−26bps 側）——若衰退是「方向組成被壞儀器灌歪」的 artifact，edge 沒爛。

凍結方法：Strong 訊號分 ERA1（90d 前）/ ERA2（近 90d），每 era 按
direction × trend bucket 分格；Oaxaca 式分解 WR 差 = 組成效應（era 間
direction/bucket 佔比變化 × ERA1 格內 WR）+ 格內效應（同格 WR 變化）。
凍結判讀：組成效應 ≥ 總差一半 → 衰退主因是儀器偏斜，CLAUDE.md 的
「準度衰退」敘述要改寫；格內效應主導（尤其 CALM 格內也掉）→ 真衰退，
重訓節律照舊且不可放鬆。n=54 的 ERA2 很小，CI 誠實報告，不硬判。

**判決（2026-08-17 當天）：嫌疑駁回——衰退是真的，而且是格內的**

ERA1 n=717 WR 60.0% / ERA2 n=79 WR 51.9%，總差 −8.1pp 的分解：
- **組成效應 +1.1pp ≈ 零**（CI [−8.4,+7.6]）——era 間的方向/regime
  組成幾乎沒變（ERA2 其實 UP:DOWN ≈ 54:46，沒有想像中偏斜——90d 窗
  比 21d 窗涵蓋了更多解碼還沒完全鎖死的日子），「壞儀器灌歪樣本」
  的假說**不成立**。
- **格內效應 −9.1pp = 全部的差**（CI [−22.4,+4.8]，寬但方向一致）。
  最關鍵的格：**CALM——均值回歸的主場——兩個方向都在掉**
  （UP 60%→52%、DOWN 63%→54%），不是單格噪音，是同構的下滑。
- 按凍結判讀走第二條路：**真衰退，重訓節律（60 天上限）照舊且不可
  放鬆**；CLAUDE.md「進場準度在衰退」的敘述**保留並升級**——現在它
  不是印象，是排除了組成混淆的分解結論。
- 誠實註記：ERA2 n=79、CI 寬，這是點估計層面的結論；但 CALM 兩格
  同幅下滑的一致性，比單一 pooled 數字更難用噪音解釋。

### 0.49c 群眾策略電池（2026-08-17 預註冊，跑數之前凍結）——生存條件線第二層

**使用者的洞見**：監測的策略不必是自己的——大眾化策略（傳統指標）的
損益是**免費的群眾行為感測器**。「哪些大眾策略正在被餵食」就是 regime
的操作型定義，而且我方策略的對手方往往就是某個大眾策略的使用者：
掃單失敗吃的是被套的突破交易者，「突破派賺不賺錢」幾乎是 SF 機制的
直接反面。這比 §0.49 的 trend_z 更貼機制一層（trend_z 首輪 CI 寬抱
不動，可能正因隔了這一層）。

**邊界聲明**：2026-04-01 鐵律（技術指標禁進模型特徵集）不受影響——
指標在此是**被研究的群眾行為代理**，不進任何模型、不預測任何價格。

**凍結電池（教科書預設參數，一個都不調——調參就不是「大眾」了）**
- **趨勢原型**：SMA50/SMA200 交叉（1h），50>200 持多、反之持空，永遠在場
- **回歸原型**：RSI(14)（Wilder），<30 進多至 RSI>50 平；>70 進空至 <50 平
- **突破原型**：Donchian 20 根通道（turtle），收破 20 根高持多、收破低持空，
  stop-and-reverse
- **狀態變數**：各原型 trailing 720 根（30d）紙上累積報酬，>0 = PAID
  （被餵食）、<0 = STARVED（挨餓）。二元、無中間帶。

**凍結預測**
- **B-P1（最重要，對手盤直連）**：SF 交易 netR 在突破原型 STARVED 時
  > PAID 時——突破派挨餓 = 刺穿不延續 = 竭盡回歸的環境
- **B-P2**：V7 Strong WR 在回歸原型 PAID 時 > STARVED 時（同類確認）
- **B-P3**：V7 Strong WR 在趨勢原型 PAID 時 < STARVED 時（回歸專家在
  趨勢派盛宴時挨餓）
- 判準：日聚類 bootstrap CI；預測只押邊際（2×2×2 全格照報但不對格
  下注——多重比較紀律）；任一狀態佔比 <5% 先當儀器嫌疑
- 通過 ≥2 條 → 該狀態變數有資格接 clocks（顯示先行、告警另議）

**判決（2026-08-17 當天）：2/3 過方向、B-P2 死、頭條是雜訊減半**

- **B-P1 ✓（方向+廣度）**：SF 在突破派挨餓時 meanR +0.0283 更高、
  **7/9 幣同號**、CI [−0.014,+0.074] 含零但——這是頭條——**與 trend_z
  版本（§0.49 SF-P1）點估計幾乎相同（+0.0285 vs +0.0283）而 CI 寬度
  約一半**（[−0.075,+0.144] → [−0.014,+0.074]）、廣度 6/9→7/9。
  使用者的猜想成立：**對手盤損益比抽象統計量貼機制一層，同樣的效應
  用一半的雜訊量到**。狀態佔比 47:53，無儀器嫌疑。
- **B-P2 ✗（乾脆死亡）**：V7 與回歸派同類確認**不成立**，方向反過來
  （PAID −2.3pp）。2×2×2 全格顯示 V7 的資訊軸在趨勢原型不在回歸原型
  （趨勢派挨餓的格普遍 61-73%）——「同類共榮」的故事錯了。
- **B-P3 ✓（方向）**：V7 在趨勢派盛宴時 WR 低 4.9pp（56.9 vs 61.8），
  CI [−15.6,+5.3] 含零，與 §0.49 卡 1 同向、互為佐證。
- 註冊同樣漏寫 CI 要求（與 §0.49 P1 同病，第二次，該學會了：**以後
  凍結判準必須寫明「點估計/符號」還是「CI 離零」**）。按凍結條款
  ≥2 條過 → 突破派狀態（SF 用）與趨勢派狀態（V7 用）接 clocks
  **顯示行**；告警等 CI 級證據，不提前。

### 0.49d 電池擴編 v2（2026-08-17 預註冊，使用者供清單，跑數之前凍結）

**來源**：使用者提供 10 項大眾指標與群眾參數。分類：**受測 6 格**
（SuperTrend 10/3.0、ADX14 25/20、MACD 12/26/9、EMA9/21、BB 20/2.0 觸軌
回歸）+ **純顯示感測器 3**（Stoch 14/3/3 80/20、VWAP 日錨、Ichimoku
9/26/52）+ 不進電池（ATR 非策略；RSI 已在 v1）。全部 1h、群眾預設參數、
凍結不調。

**凍結預測（機制推導，只押這 6 格）**
- B-P4 MACD×V7：PAID 時 WR 較低（趨勢家族，同 B-P3 邏輯）
- B-P5 BB回歸×SF：PAID 時 netR 較高（回歸 regime 同側確認；V7 版死過
  換押 SF——SF 本質是失敗突破的回歸收割）
- B-P6 EMA9/21×V7：PAID 時 WR 較低（快趨勢家族）
- B-P7 SuperTrend×SF：STARVED 時 netR 較高——**與 Donchian 打對頭**，
  誰的 CI 窄、逐幣廣度高，誰當 SF 的正式對手盤儀表
- B-P8 ADX×V7：RANGING（<20）WR > TRENDING（>25）
- B-P9 ADX×SF：RANGING netR > TRENDING——B-P8/9 是**群眾自己的 regime
  儀 vs 我們的 trend_z 打對頭**
- Stoch/VWAP/Ichimoku 零預測，只進顯示行

**判準（這次寫死兩級，不再漏）**
- **一級（顯示接線資格）**：符號正確 **且** 幅度 ≥2pp（V7 WR）/
  ≥0.01R（SF），SF 側另需逐幣同號 ≥6/9
- **二級（告警資格）**：日聚類 bootstrap CI95 離零
- 打對頭判準：點估計同向前提下，CI 寬度較窄 **且** 廣度不劣者勝；
  勝者取代敗者進 clocks 顯示行（不並列，避免儀表通脹）

**判決（2026-08-17 當天，跑完即記）**

| 格 | 結果 | 級別 |
|---|---|---|
| **B-P9 ADX×SF** | **RANGING +0.075 vs TRENDING +0.016，差 +0.059R，CI [+0.010,+0.106]，8/9 幣** | **二級 ✓✓——全線第一個 CI 級結果** |
| B-P8 ADX×V7 | RANGING 63.6% vs TRENDING 58.3%（+5.3pp） | 一級 ✓ |
| B-P7 SuperTrend×SF | +0.037R、7/9 | 一級 ✓ |
| B-P4 MACD×V7 | +1.6pp（符號對、幅度不足） | ✗ |
| B-P6 EMA9/21×V7 | **−5.4pp 符號反**——快趨勢派 ≠ 慢趨勢派 | 死 |
| B-P5 BB回歸×SF | −0.011R、2/9 | 死 |

**打對頭判決（按凍結條款）**
- **ADX 完勝 trend_z，兩側都換**：SF 側 CI 寬 0.096 vs 0.219（少一半
  以上）、廣度 8/9 vs 6/9、且 CI 離零；V7 側寬 25.5pp vs 50.7pp，
  RANGING 桶佔 30% 時間（trend_z 的 TREND 桶只有 3%——樣本充足度
  天差地別）。**群眾自己的 regime 儀表打敗了我們的統計量**——§0.49
  卡片的條件變數自此以 ADX 為準，trend_z 退役。
- SuperTrend vs Donchian：實質平手（點 +0.037 vs +0.028、CI 寬 0.089
  vs 0.088、廣度同 7/9）。按條款字面（CI 較窄且廣度不劣者勝）
  **Donchian 留任**；註記 ST 的點與 CI 下緣都較好，兩者分歧時複審。
- **回歸家族三連死**（RSI×V7、BB×SF、加上 v1 的 B-P2）：均值回歸
  群眾的紙上損益對我們兩條策略都無資訊。資訊軸在趨勢/突破側——
  「誰在追」比「誰在接」重要。

**接線**：clocks 2e-c 加 ADX 狀態（BTC + core9 TRENDING 幣數）。B-P9
是二級證據，TRENDING 過半時的 SF 逆風標記有告警資格。

### 0.51 出場機制歸因（2026-08-19，反事實重放）——decay 是主力，opp 是配角

**為什麼要重做**：CLAUDE.md 掛的「opp_signal 85.7% WR vs trail_stop 37%」
**無效**——一筆交易走到 trail_stop 正是因為沒等到反向訊號，走到 opp 是
因為等到了；兩群交易從一開始就不同，那個比較量的是選擇效應不是出場品質。
唯一誠實的形式是反事實：同一批進場、四種出場政策各重放一遍
（`research/exit_attribution.py`，忠實對照 executor 語意：Strong-only
next-open 單倉、3xATR 棘輪 intrabar、decay N=2 先於 opp、10bps 來回）。

**結果（全期 n=38-48）**

| 政策 | net/trade | WR | MDD |
|---|---|---|---|
| A 純 trail | −20.3 bps | 45% | −1686 |
| B trail+opp | −12.4 | 45% | −1168 |
| C trail+decay | **−0.1** | **54%** | **−1052** |
| D trail+decay+opp（現行） | −0.3 | 54% | −1079 |

- **conviction_decay 是主力**：邊際貢獻 **+20.2 bps/trade**（C−A），三個
  時代一致（修法前 +16.2、修法後 +54.1）。0 shadow 樣本就上線的那個決定
  （2026-07-25 informed override）**事後被證明是對的**。
- **opp_signal 是配角**：單獨加只有 +7.9（B−A），而且**在 decay 之上是
  負貢獻**（D−C = −0.2 全期、−7.7 修法後）——decay 更早觸發、涵蓋了 opp
  的多數情境，剩下的 opp 觸發反而是較差的時點。
- **純 trail 是負的**（−20.3 bps）：3xATR 單獨用會賠，V7 現在的正報酬
  **全部來自 decay**。這修正了「正報酬來自出場紀律（trailing 讓 winner
  跑）」的舊敘述——讓 winner 跑的不是 trailing，是 decay 及早停損。

**不動手的理由（重要）**：D−C 的負差在全期只有 −0.2 bps、n=48，遠不到
拆掉 opp_signal 的證據門檻；而 opp 是 decay 的 fallback（pred_ret 恰為
0 的邊角）。**現階段只記錄，不改 executor**。要動需要：解碼修法後的樣本
累積到 n≥30 且 D−C 的 bootstrap CI 離零。

**誠實限制**：解碼修法後只有 4 筆，該欄的數字只能看方向不能下注；
全期樣本含被鎖死時代（opp 天生餓死），所以 opp 的貢獻可能被低估——
這正是分時代報告的原因。

### 0.5 SF 上線工程軌（2026-08-18 使用者拍板：工程並行＋守住判決）

**決策**：使用者認為 SF 已趨穩、想量化上線。數據裁定：穩的是 R∧V 組合
（CI 下緣 +0.087↑），主 gate B 仍在零下（−0.016，隨樣本走弱）。選項
攤開後使用者選**工程並行＋守住判決**——統計 gate 一個不鬆（B 1400 筆
判決 ~3 週後、以 ~250 筆/週的速度），工程現在開跑，讓 gate 通過成為
唯一剩下的閘。這同時拍板了 PORTFOLIO_RISK_FRAMEWORK §5 開放問題 3
（P1 提前並行，但用零接線方式）。

**里程碑**
- **M1 ✅（2026-08-18）**：`pf_intents`/`pf_positions` 建表 +
  `research/pf_mirror.py` 每小時鏡射 V7 帳（21 筆入帳，net_r 同構可比）。
  **零 live 代碼改動**——不走 executor dual-write（AST no-wire 測試
  完好），用 (src_table, src_id) 冪等 upsert 從外面讀。
- **M2 ✅（2026-08-18）**：shadow log **加 side 欄**（additive，兩個凍結
  核心的偵測層外傳，7 個 exact-arity 呼叫端補 `*_`）。迴歸：--gate
  改動前後 diff 為空；side 覆蓋 2192/2243，舊列逐 pass 確定性回填。
- **M3 ✅（2026-08-18）**：SF 乾跑 intent 產生器每小時上線——新 fill 的
  B 變體轉 Intent → `risk_engine.decide()` → `pf_intents` 落庫（含拒絕
  原因）。**首輪 48h 回填實證：51 候選 → 37 筆 `account_halted` 拒絕 +
  14 去重**——引擎對真實訊號流的第一批可稽核決策，且帳本誠實反映
  CAP-2 仍 HALT 的事實：資金基準解決就在上線關鍵路徑上。
- **M4（下單程式碼 gate 過後才動；唯讀前置 ✅ 2026-08-18）**：
  **可行性審計全綠**（`research/sf_m4_feasibility.py`，OKX 公開端點）：
  (a) core9 全部有 live USDT 永續（含 BNB-USDT-SWAP，本來最擔心的）；
  (b) 0.15% 風險 × 3.5 ATR 停損下逐幣 sizing 全部可行，lot 捨入誤差
  最大 −7.7%（ETH @ $274 基準）遠低於 20% 紅線；
  (c) 併發 5 的最壞名目只佔 2× equity cap 的 **23%**（$274 與 $777
  兩種基準都是）——容量餘裕極大。
  **進場機制凍結**：sweep_core 的 retest-touch stop-entry 語意 =
  OKX **trigger（條件單）**——掃高後價格從上方回落至水位觸發 SHORT
  （鏡像同理）。不是 resting limit（價在水位外側掛限價會立即成交在
  錯誤的一側）。TTL = W=8 根（8h）未觸發即撤。
  **剩餘（gate 過後）**：trigger 下單路徑 + facade 補齊（**facade-skip
  教訓 ×3**：OkxClient 需補 algo-trigger 與逐幣槓桿設定的 passthrough，
  AST 守衛測試會抓）、多幣對帳擴展、Scenario A 成本假設 vs 真實成交
  的首批驗證。
- **M5 上線前清單**：gate 判決 GO + 資金基準解決（CAP-2）+ M2-M4 +
  首筆人工確認（慣例）+ 天氣站 ADX 顯示在操作面（生存層首次接生產）。

### 0.52 天氣站 hold-out 監測（2026-08-19 建立，`research/weather_holdout.py`）

三個上牆儀表都是在「註冊當下的歷史」上判的——正確的註冊方式，但留下
一個沒答的問題：關係在樣本外還成立嗎。這支腳本只用**註冊日之後成交**
的樣本重跑同一套算術（凍結定義不動，只移動視窗）。

**首跑（2026-08-19，hold-out 僅 2 天）**
- ADX×SF：**符號翻了**（−0.195R vs 註冊時 +0.059R），但 n=9、只有 1 個
  幣有雙 cohort、CI [−0.311,+0.268] 跨零——**不構成證據，構成觀察項**。
- 突破派×SF、PSAR×V7：hold-out 期內只有單側 cohort（環境沒切換過），
  無法比較。
- 這正是這條線的固有困難（§0.49 記過）：regime 儀表的 hold-out 天生
  收斂慢，因為要等環境自己切換。

**排程**：不定期重跑（≥30 天或樣本 n≥60 時才有解讀價值）。**判讀紀律**：
符號翻轉且 CI 離零才降級儀表；小樣本翻號一律當雜訊，不得據此改牆上
的等級標示——那會變成用雜訊追著改結論，正是預註冊要擋的事。

### 0.49g 天氣站 v4 候選池（2026-08-18 使用者提供來源，未動工）

**候選登記（2026-08-31，來源：使用者轉的非線性動力學論點）——登記不研究，
主體（獵取/V7）優先**：臨界減速（critical slowing down）三變數作為
regime 轉變前兆候選：(a) 衝擊後恢復速度（大單/跳動後回到基線的時間）、
(b) 報酬 lag-1 自相關的爬升、(c) 跨資產相關性收緊速率。與既有 ADX 類
水平量測不同類（那些已證偽前瞻力）；生態/氣候文獻有實證先例。誠實
預期：轉換天生稀少（TREND 桶 3-4% 時間），樣本費極貴。若立卡照 §0.49
儀式：先寫預測、凍結分桶、全格報告。

使用者指定來源：https://github.com/paperswithbacktest/awesome-systematic-trading
——系統性策略大全（含論文出處與回測庫），可挖掘的群眾原型遠多於目前 11 個。

**v4 首輪（2026-08-18 開工，跑數之前凍結）**

來源庫誠實對照：幣圈區只有 2 個策略（Overnight Seasonality、Rebalancing
Premium），carry 掛在 FX 區。首輪選 2 個受測（都對應真實幣圈群眾＋資料
在手）＋1 個候選延後：

- **CY 資金費率 carry 農夫**（庫的 carry 家族幣圈翻譯 = basis farming，
  真實大群眾）。凍結：RICH = trailing 7d（168h ffill）平均 funding >
  +0.01%/8h（Binance 基準費率），否則 THIN。
  **CY-P1（受測）**：V7 Strong **DOWN** 勝率 RICH > THIN——carry 肥 =
  多頭擁擠 = 空訊號的擠壓燃料（live 實測空側 edge 的機制候選）。UP 列
  照報不下注。
- **LB 清算搶反彈群眾**（幣圈原生優先缺口；庫裡沒有，註明）。凍結：
  burst = 該小時多單清算 USD ≥ 3.0× trailing-24h 均值（3.0 沿
  cancel_shock intensity 凍結先例）；群眾玩法 = burst 後 4 根 bar 持多
  （搶刀反彈）。資料 cg_liq_agg_1h（BTC，2025-10 起，DailyCollect 日更）。
  **LB-P1（受測）**：BTC sweep_core 回測中 **LONG 側**交易（M2 的 side
  欄第一次派上研究用場）netR 在反彈群眾 PAID 時 > STARVED——清算瀑布
  掃低後的反彈與 SF 低側是同一族交易。
- **Overnight Seasonality（候選延後）**：來自庫的幣圈區，但機械化需先
  查原始論文的持有時段規則——不猜規則，查到再凍結。
- 判準：§0.49d 兩級制（單資產無廣度項：一級 = 符號 + ≥2pp/≥0.01R；
  二級 = CI95 離零）。記分程式同 commit。

**判決（2026-08-18 當天）**
- **CY-P1 不可判（gauge 在樣本年代退化）**：RICH 佔比 **0%**——查證
  非儀器 bug：12 個月 BTC funding 的 7d 均值最高 +0.0087%，**從未**
  越過 +0.01% 基準。宏觀事實副產品：**整個樣本年代沒出現過一次
  多頭擁擠狂熱**。假說未死、只是等不到它的天氣——狂熱年代重臨時
  自動可判。carry 感測器（RICH/THIN）上牆：它是牆上最宏觀的一支
  （THIN = 擠壓燃料不在）。
- **LB-P1 死（符號反）**：反彈群眾 PAID 時 BTC SF LONG 側 netR
  **+0.004 vs STARVED +0.075**（差 −0.071，CI 寬）。與預測相反：
  搶刀群眾吃得好的時候，掃低反轉反而賺得少——「清算反彈=低側掃單
  失敗同族」的機制描述不成立，兩者搶的可能是同一口飯（反彈太順=
  沒有失敗掃單可收割）。事後故事不下注，記錄即止。
- 天氣站新增 2 感測器：carry（THIN）、liq-bounce（PAID，日更 cadence
  隨 DailyCollect）。受測格 v4 首輪 0/2——survival 線的假說死亡率
  持續高於存活率，這是預期的（機制空間比 edge 空間大得多）。

**開工前的紀律備忘（沿 v2/v3 規矩）**：
- 挑「幣圈群眾真的在跑的」，不是「論文裡最漂亮的」——天氣站量的是
  群眾行為不是策略品質；沒有散戶群眾在跑的學術策略，量它沒有意義
- 每個候選先過機制審查分身分（受測 vs 感測器）；回歸家族已 0/4 不再
  開受測格；趨勢/regime 席位已有在任者（ADX/Donchian/PSAR），新原型
  只能以挑戰者打對頭
- 受測格控量（多重比較預算）、預測與記分程式同 commit、§0.49d 兩級判準
- 優先方向（依既有缺口）：幣圈原生群眾（清算/擠壓類）、carry/basis 類
  （與 K 線原型不同源）、跨資產動量（若資料可得）

### 0.49f 天氣站 v3 擴編（2026-08-17 預註冊，使用者選 4 個群眾，跑數之前凍結）

**凍結機械化（群眾預設，一個不調）**
- **Pivot 日內 S/R（受測）**：前一 UTC 日 H/L/C → P=(H+L+C)/3、R1=2P−L、
  S1=2P−H；收盤 < S1 進多至收 ≥ P 平、> R1 進空至收 ≤ P 平
- **Parabolic SAR（受測·挑戰者）**：標準 0.02 步進 / 0.2 上限，SAR 翻轉
  即持倉方向
- **資金費率反向（感測器）**：Binance 8h funding，|rate| ≥ 0.05%/8h 時
  反向持倉（正→空、負→多），否則空手；1h 前向填充
- **網格機器人（感測器）**：錨 = trailing 720 根 SMA，間距 0.5×ATR14，
  庫存 = −(價−錨)/間距 截 ±5 再 /5 正規化——回歸家族形狀，故不受測
- 狀態一律 = trailing 720 根紙上報酬符號（PAID/STARVED），同 v1/v2

**凍結預測（只押 3 格；funding/grid 出探索性讀數不下注）**
- **PV-P1**：SF netR 在 Pivot-fade 群眾 PAID 時較高（S/R 撐住 = 掃單
  失敗的日級同構）
- **PS-P1**：V7 Strong WR 在 PSAR 群眾 STARVED 時較高（趨勢家族同
  B-P3 邏輯）
- **PS-P2**：SF netR 在 PSAR 群眾 STARVED 時較高；**對頭 vs Donchian**
  （SF 對手盤席）
- **對頭 vs SMA50/200**（V7 逆風席）：同向前提下 CI 較窄且點估計不劣
  者勝，勝者上 clocks、敗者退
- 判準沿 §0.49d 兩級制：一級=符號+幅度（≥2pp / ≥0.01R）+SF 側廣度
  ≥6/9；二級=CI95 離零

**判決（2026-08-17 當天）**
- **PS-P1 ✓ 一級，且 PSAR 奪下 V7 逆風席**：STARVED−PAID +5.2pp，
  CI 寬 20.1pp vs SMA50/200 的 20.9pp、點估計不劣（+5.2 vs +4.9）——
  按凍結對頭條款字面，PSAR 取代 SMA 進 clocks。差距極小，註記兩者
  分歧時複審（與 SuperTrend/Donchian 同款保留條款）。
- **PV-P1 死**：Pivot-fade 群眾損益對 SF 完全零資訊（+0.001R、4/9）——
  「S/R 撐住=掃單失敗同構」的日級版不成立。
- **PS-P2 符號反**（−0.023R、3/9）：PSAR 翻轉太快，其狀態對 SF 無資訊
  ——Donchian 的 SF 對手盤席**無人挑戰成功**，續任。
- 感測器註記：funding-contra 在 0.05% 門檻下 BTC 幾乎永遠空手（PAID
  share 0%），是稀有事件感測器；探索性讀數 PAID +0.083 vs STARVED
  +0.037（n=276 vs 6619，僅 2 幣有雙 cohort）——樣本太薄，僅記錄。
  grid 感測器正常（PAID share 54%）。

### 0.47 F8 IFVG × 掃單位置 三-cohort 回測預註冊（2026-08-13，跑數之前凍結）

**來源**：使用者的手動交易配方——流動性獵取 + IFVG 回測反轉區進場 + 大單
在區域內。hypothesis-driven（2026-06-20 紀律裡「好」的那一種：來自 domain
knowledge 的 categorical 條件，不是 threshold sweep）。核心問題不是「IFVG
有沒有用」，是**「位置是不是 edge 的來源」**——地形戰役的分界線是純價格
結構全滅（S2 BOS/CHoCH、D6）、流動性位置全存活（D1/D2/D3/D5），裸 IFVG
屬於前者（先驗低）、「IFVG 疊在剛掃過的池子上」屬於後者的家族。

**凍結定義（跑任何數據之前寫死；佔位參數全部一次定死不掃）**
- 時間框架：**5m 與 15m 各自獨立計分、都報告**（使用者手動用這兩個；
  不得事後只挑好看的那個）。宇宙：core9（凍結 Gate F 籃子）。回測窗 12 個月。
- FVG：三根 K 的缺口。多頭 `low(t) > high(t-2)`，zone=[high(t-2), low(t)]；
  空頭鏡像。最小缺口 ≥ **0.10 ATR14**。壽命 200 根，未反轉即棄。
- **反轉（IFVG）**：多頭 FVG 被之後某根 K **收盤**穿破 zone 下緣 → 轉空頭
  阻力區（回測做空）；鏡像同理。
- **進場（使用者手法）**：反轉收盤之後（不含反轉那根），96 根內價格**首次
  回觸** zone 近緣，以近緣價掛限價成交。每個 IFVG 只進一次。
- 出場：沿用 sweep_core 凍結——disaster stop 3.5×ATR、HOLD=8 根**原生**
  K 棒（5m→40 分、15m→2 小時）後收盤出。同根 K 同時觸及進場與停損時
  **保守記停損**。
- 成本：Scenario A（進 7 bps、時間出 3、停損出 10），R 換算同 shadow_engine。
  **毛利與淨利都報告，判決只看淨利**（subhourly G2 教訓：5/15m 是成本
  最容易吃光訊號的地帶）。
- **三個 cohort**：
  (a) **位置條件**＝IFVG zone 距「96 根內被掃過的水位」≤0.5 ATR。水位＝
  pivot swing H/L（PIVOT=10，同 sweep_core）＋前日高低；掃＝刺穿 ≥0.05 ATR
  且 3 根內收回。
  (a2) ＝(a) ∧ 大單 proxy：回觸那根 K 成交量 ≥ trailing 288 根的 P80，且
  taker 方向對齊（空 ≤0.45 / 多 ≥0.55 taker share）。klines 級 proxy，
  誠實標註不是真 footprint。
  (b) 無條件全部 IFVG。
  (c) 隨機對照：每筆 (b) 配一筆同幣同向、隨機時點、同出場規則（seed=7）。

**凍結預測（terrain 式，先寫再跑）**
- P1：(a) > (b) 且 (a) > (c)；(b) ≈ (c)（CI 重疊）——位置才是訊號
- P2：**判決門檻＝(a) 的日聚類 bootstrap CI 下緣（淨利）> 0**，才有資格
  談 forward 時鐘；差一點就是 NO-GO，不調參數重跑
- P3：(a2) ≥ (a)——大單確認有精煉作用
- P4：若毛利 > 0 但淨利 ≤ 0 → PARK（與 subhourly 同判法），出口只剩
  「給 1h 主策略當 timing/確認」一條路另議
- 若 (b) ≈ (a) 都好 → 位置沒有增量，違反地形先驗，**先查儀器再解讀**
  （2026-08-02 兩個儀器 bug 的教訓）

**與既有註冊的關係**：F8 是獨立回測診斷，不動策略 #2 任何凍結時鐘；若過
P2 才走「凍結規則 → shadow forward」的標準路（屆時另立 §，記分程式同
commit——C/D 教訓）。

**判決（2026-08-13 當天，跑完即記）：全線 NO-GO**

| cohort | 5m netR (CI) | 15m netR (CI) |
|---|---|---|
| (a) 位置條件 n=51k/15k | −0.194 [−0.206,−0.182]、0/9 幣正 | −0.097 [−0.110,−0.084]、0/9 |
| (b) 全部 IFVG | −0.194（毛 −0.005） | −0.091（毛 +0.001） |
| (c) 隨機對照 | −0.175（毛 +0.003） | −0.088（毛 +0.003） |
| (a2) 大單 proxy | −0.047（毛 **+0.094**） | **+0.053 [+0.004,+0.103]**（毛 +0.124）→ **洩漏，見下** |

- **P1 FAIL**：(a)≈(b)≈(c)——位置條件（如凍結定義）零增量。註：predicate
  太寬鬆（~1/3 的 IFVG 都合格，5/15m 的 pivot 掃單無處不在），這是誠實的
  儀器觀察，**不是重調的授權**。
- **P2 FAIL**：(a) 兩個時間框架 CI 上緣都遠低於 0，連毛利都是負的——
  不是成本問題，是**沒有訊號**。(b) 毛利 ≈ (c) ≈ 0：bar 級的 IFVG 回測
  本身不帶方向資訊。
- **P3 形式上成立但是假的**：(a2) 唯一漂亮的格過不了洩漏審計
  （`ifvg_a2_audit.py`）。凍結定義用**回觸棒自己的量能與 taker share**
  過濾，那些要收盤才知道，而成交在棒內——「空單要求該棒賣壓重」等於
  挑「觸到我們的價然後跌走」的棒，markout 是**構造出來的**。改成
  「濾網確認後下一根開盤進場」（資訊完備的第一個可成交價）：
  15m 毛利 +0.124→**−0.006**、netR +0.053→−0.075、7/9 幣正→2/9。
  可實現的兩種版本（無條件掛邊緣 −0.097 / 濾網後next-open −0.075）
  **全部為負**。+0.05 那個版本只屬於能看見未來收盤的交易者。
- **P4 重演**：隨機對照的 netR（−0.17/−0.09）就是純成本地板——subhourly
  G2 的教訓第三次確認：5/15m 尺度上先問成本，再問訊號。
- 儀式有效的一次記錄：漂亮的格先審儀器（2026-07-29 教訓）這次在
  **報 GO 之前**抓到了洩漏，而不是之後撤回。與 R∧Q/V∧LIQ 同形
  （挑選面上 +0.97/100%WR，真前瞻歸零）。
- **對使用者手動交易的含義（誠實邊界）**：本判決殺死的是「bar 級機械化
  的 IFVG×位置」，不能反推使用者的裁量交易無 edge——手動看的是即時
  footprint（非棒收盤統計）、maker 進場、與更多未寫進凍結定義的 context。
  但它確立：這套配方裡**可被 bar 資料機械化的部分**沒有可提取的 edge，
  真 edge 若存在，住在 bar 之下（footprint/逐筆）——那是 F7 的地盤。

### 0.46 F7 秒級撤單事件流預註冊（2026-08-13，資料存在之前凍結）

**動機（為什麼舊判決不算撤單流的死刑）**：五個方向性 family 全 FAIL，但它們
全部測在 `depth_deltas_1m` 上——那張表把 @depth@100ms 壓成每分鐘四個純量
（600:1），**價位分層**與**cancel→re-add 抵銷**兩個最可能載訊號的結構在落庫
時就被磨掉了。同時唯一存活的波動訊號控制活躍度後只剩 h=5min 一格顯著
（偏 IC +0.040 [+0.019,+0.060]）——訊號指向更短的尺度，而 1m 解析度在
5 分鐘裡只有 5 個點。方向 FAIL 可能是假說錯，也可能是儀器磨掉的，
**現有資料無法區分**——F7 就是為了讓它可區分。

**理論框架（使用者 2026-08-13 提出：用做市 MM 的視角改編）**：撤單量的大宗
本來就是 MM 的報價維護（cancel-and-replace 調價）。這個視角事後一致地解釋了
全部既有結果——(a) 1m 的 skew 水平測不出方向：量到的是做市噪音的側別；
(b) 方向 FAIL + 波動 PASS 正是「撤單由 MM 風控主導」的預測：MM 察覺 toxic
flow 時**兩側同時**撤退，對稱行為只預測波動；(c) MM 是最快的玩家，其方向性
痕跡在秒級衰減，與「訊號在最短 horizon 最強」一致。F7 的假說直接從這個
框架生成，不是資料挖掘。

**採集（已上線同 commit）**：`market_data/adapters/depth_events_1s.py` →
`depth_events_1s` 表。BTC spot+perp only。凍結參數：band 依「距**更新前** mid
的 bps」分 NEAR<5 / MID 5-25 / FAR>25；re-add = 同側同價 5000ms 內的
cancel→add FIFO 配對（記在 re-add 落地那一秒；add 照常入 band，**採集層
永不互抵**，相減是研究層的事）；暖機 = 兩側各 ≥20 層且未交叉，交叉即重置。
存 MySQL 不存 parquet（容器檔案系統每天被部署洗 2-10 次，mistake.md
2026-08-11）。~17.3 萬列/天。

**凍結假說（三個 family，同時註冊——family-wise 語義同 cancel_shock_ic：
單一 marginal pass = 弱證據）**

- **F7-H1「真撤退」方向**：`true_withdrawal = cancel − readd`（NEAR band）的
  側別 skew 領先方向。MM 邏輯：扣掉報價維護後剩下的才是「真的離開」；MM 撤
  ask 側 = 不願在漲勢裡賣 = 看漲。預測符號：ask 真撤退 > bid → 正報酬。
  horizon h ∈ {1, 3, 5, 15} 分鐘，non-overlapping stride。
- **F7-H2「報價恢復不對稱」方向**：波動衝擊後，兩側 NEAR add 流恢復到自身
  基線的速度差領先方向。MM 邏輯：MM 不急著回來報的那一側，是他們預期價格
  要去的方向。預測符號：ask 側恢復較慢 → 正報酬。事件定義沿用
  cancel_shock_ic 的 intensity_shock ≥ 3.0，基線 = trailing 60min 中位數。
- **F7-H3「波動複製 + band 分辨」**：NEAR band 的撤單強度衝擊 → |未來報酬|，
  必須 (a) 在控制 update_count/volume/trades 的偏 IC 下存活，且 (b) 顯著
  強於 FAR band 的同構指標——MM 風險感知器坐在 touch，不在深處。這是對
  既有唯一存活格的高解析度複製 + 一個可證偽的層次預測。

**判準（事後不得放寬）**：檢查點 = joined 秒數 ≥ **30 天**（~260 萬秒，
約 2026-09-13）。每格 PASS 條件與既有 family 相同：bootstrap CI 離零 ∧
|IC| ≥ 0.02 ∧ 兩半同號。之前的任何跑分一律標 SMOKE。

**出口前置（G2 教訓，先寫死）**：subhourly Phase 0 的判決是資訊 PASS、
經濟 FAIL（毛利 6.2 < 8 bps 成本）→ 秒級訊號**不得**成為獨立策略。PASS 的
唯一兩個出口：(a) V7 進場 timing overlay（分鐘資訊的既定唯一出口），
(b) 策略 #2 的進場擇時。兩者都是「已要進場，選哪一秒進」——不新增曝險
決策，只改執行。連這兩個出口都接不上的訊號，PASS 也只是知識不是錢。

**誠實註記**：假說在任何 1s 資料存在之前凍結（與 07-10 的 cancel_lead_ic
同級，比 07-15 兩個 family 乾淨）；但動機來自已看過的 1m 結果（「訊號在
短尺度」），所以 horizon 與 band 邊界現在寫死、之後不得調。
**預註冊與記分程式同 commit**（C/D 教訓，§0.44 補記）。

**解讀備忘（2026-08-13 補，佇列位置文章；不改任何凍結定義）**：
- **已知盲點——repricing**：佇列管理的常見動作是「撤 P、改掛 P′」，我們的
  re-add 配對只認同價，repricing 會被記成真撤退 → **H1 的 true_withdrawal
  是高估的**。現在記下是為了判決時不把這個結構性偏差讀成訊號衰減；要修
  只能走新 family（F7b：鄰近價 cross-price 配對）另行預註冊，30 天檢查點
  前一律不動。
- **填空層/佔位單**：MM 為佇列位置掛的單無視方向與公允價值 → ADD 流同樣
  被機械噪音主導，解讀 H1/H2 時 add 側與 cancel 側同等對待。
- **掃後競速**（策略 #2 交叉）：大單掃層後 HFT 競速同方向重建新 BBO；
  「掃單失敗」的微觀對應可能就是**那場競速沒有出現**。F7 的掃後 NEAR
  re-add 速度未來可作為策略 #2 的確認訊號候選——**無時鐘、無註冊**，
  只是記下這個連結，等 F7 判決後再議。

### 0. 撤單劇本偵測器已上線（2026-07-16）
`market_data/tasks/cancel_playbook_watcher.py` 掛進 Service 2：機器前瞻記錄
撤單劇本事件（凍結定義 v1-2026-07-16：吸收/真破/真空/雙側避險）→
`cancel_playbook_events` 表 + 30/60/120m 結果自動回填 + TG 告警
（政策：vshock≥20 或 |淨|≥0.30，全域 60m cooldown，~4 則/天）。
- [x] ~~累積 30+ 筆/劇本後看 hit_60m 統計~~ → **2026-07-29 判決：方向性主張
      FAIL**。四個方向劇本全部過門檻（true_break UP 67 / DOWN 54 /
      absorption DOWN 46 / UP 41），**hit_60m 全部 <50%**（41-48%），合池
      方向修正 +60m = −5.6 bps（t=−2.04，但事件窗重疊會灌水顯著性，安全
      讀法是「方向不成立」而非「顯著為負」）。
      使用者質疑固定時間點量法會漏掉「走對再收回」→ 加做路徑分析
      （`research/cancel_path_analysis.py`，MFE/MAE + 5 段障礙掃描 + 同日
      配對對照）：**同樣 FAIL**——事件 MFE +34.3/MAE −36.4（比值 0.94）
      vs 對照 +29.1/−26.9（1.08），五個障礙尺度全部等於或劣於對照。
      兩個獨立量尺同結論。
      **唯一的正向發現**：事件總振幅比對照大 26%（MFE +18%/MAE +35%）=
      波動狀態偵測器。**使用者 2026-07-29 否決此角色**：系統 sizing 固定
      2x 名目不隨波動縮放、停損已用 3xATR（ATR 即波動）、2h 效應 vs 12h
      持倉稀釋到 17% → 結構上吃不到，不值得複雜度。
      **剩餘價值全繫於 cancel_lead_ic（原始 skew 領先性，獨立問題）**，
      joined minutes 28,559/40,000，~8/6 到期，已在 PortfolioClocks 自動排程。
- [ ] 8/10 `cancel_lead_ic`/`cancel_shock_ic` 判決日一起檢視；表現好的劇本
      才有資格升格下一個 pre-registered family

  **⚠ 判決在 n≥40,000 當下成立，不是在 8/10**（2026-08-05 23:53 UTC 時 n=39,974，
  約 26 分鐘後跨過）。預註冊寫的條件是 `n_minutes >= 40,000`，日期 `~2026-08-10`
  只是估算。**第一次 n≥40,000 的執行結果就是判決，不得重跑取更好的** ——
  從跨過門檻到排程日之間有四天，任何人隨手跑一次都會看到答案；若第一次 FAIL
  然後等排程日再跑一次希望數字變好，那就是重抽到過為止。
  誰先跑到誰就記下來，寫進本節並標明執行時間與 n。

  **判讀框架（2026-08-05 前置檢查後訂，判決日照這張表走，不要臨場想）**

  前置檢查已做，儀器沒問題：兩支腳本跑得起來、join 覆蓋率 98.7%
  （38377/38898 分鐘）、depth_deltas 即時、n 約 08-06 跨過 40,000（早於判決日）。
  所以 08-10 不會重演 [[2026-07-05 月度復驗]] 那種「排程準時、verdict 漂亮、
  跑在 16 天舊資料上」。

  | 判決結果 | 意義 | 該做什麼 |
  |---|---|---|
  | 方向 PASS | 撤單流這條線活了 | 進 Gate 設計 |
  | 只有波動 PASS，**且控制成交量後仍在** | 得到一個波動預測器 | 救不回方向策略，但可評估接進 V7 進場時機（[[分鐘級 PARK]] 註明「分鐘資訊唯一出口 = 4h 交易的 timing overlay」）|
  | 只有波動 PASS，**控制後塌陷** ← 目前的樣子 | 它是成交量的代理 | **收線**；depth_deltas 專門資料線的投資報酬到此為止 |

  **前置讀數（SMOKE，n<40000，非證據）**：方向 8 格只有 shock TEST A 的 h=15
  一格通過（預註冊自己寫了「單一 marginal pass = weak evidence」）；波動
  TEST B 四格全過但 —— 見 `research/cancel_shock_control.py`（**非預註冊**，
  不改 PASS 門檻）—— 控制活躍度後平均 IC +0.078 → **+0.024，只保留 30%**，
  且只有 h=5 的 CI 不含零（+0.031，[+0.008, +0.054]）。
  吸走它的是**成交量**不是 update_count：撤單強度帶的不是「掛單簿在忙」
  而是「市場在成交」，而成交量本來就有、免費、不需要這條專門資料線。
  剩下的 IC 0.031 在 5 分鐘尺度要換成錢得先跨過價差＋手續費 ——
  [[分鐘級系統 Phase 0]] 正是死在這裡（G1 資訊 PASS、G2 經濟 FAIL，
  最佳毛利 6.2 < 8 bps）。**同樣的形狀第三次。**

  **這一格若成立，影響大於「撤單流死了」**：CLAUDE.md 記載 V7 的唯一剩餘
  槓桿是異源資料，而 depth_deltas 是其中最貴、最花時間累積的一條
  （27/90 天，十月才到 L2 檢查點）。若它只是成交量的重新包裝，
  **十月那個檢查點的預期價值要往下修**，資源應該改投別的異源
  （options GEX / on-chain whale）。
- 紀律：記錄定義凍結不准調；告警門檻屬 UX 可調；此線永不接 executor
- 2026-07-17 三階分析入口完成：① `research/cancel_flow_analyze.py`
  (table/--summary/--json) ② MCP `analyze_cancel_flow`（Claude Desktop 需
  Python≥3.10 env，本機 l30d 3.12 已裝 mcp）③ TG `/cancelanalyze [mins]`
  → 進入實盤驗證階段：事件自動累積中，等 n≥30/劇本 + 8/10 判決

### 0.4 終局架構方向已定（2026-07-29 使用者決策）：統一風控框架下的 N 條薄策略
使用者選定組合路線（非單一肥策略）：多條互相低相關的薄 edge（V7 ML 持倉／
#3 價格結構／撤單微結構…）跑在同一個風控框架下，用組合概念控風險。
**時序紀律：現在只做設計、不動 live 代碼**——重構觸發點 = 第二條策略通過
自己的 gate（#3 Gate F 或撤單線判決）。在那之前 V7 executor 一行不動。
- [x] 寫 docs/PORTFOLIO_RISK_FRAMEWORK.md 設計稿（2026-07-30 v1 完成，含 3 個開放問題待拍板）。核心只有一條：**風險在
      組合層看待，策略是元件**。候選機制（設計時取捨，非承諾）：兩層 kill
      （策略層 halt／帳戶層 DEMOTE）、每策略風險預算、中央曝險帳本、
      並發相關性上限（#3 的 9 幣教訓）、策略=adapter 只產 intent。
      對沖是概念不是需求（2026-07-29 使用者澄清）——具體用哪些機制落地
      由設計稿討論後定
- [ ] **並發上限重新定價（2026-08-09 新增，附實測）**——目前的 5-10 是設計時
      的直覺值，實測顯示它是策略 #2 最大的單一改善槓桿，且它屬於**風控框架
      的相關性預算**、不是策略參數：

      | 上限 | 成交/被擋 | 總損益 | 日聚類 CI | 峰值佔用 | 最差單小時 |
      |---|---|---|---|---|---|
      | 5 | 157/911 | +$585 | [+138,+1035] | $200 | −$89 |
      | **8（現行）** | 253/815 | +$583 | **[−14,+1185]** | $320 | −$89 |
      | 12 | 370/698 | +$674 | [−267,+1643] | $480 | −$161 |
      | **15** | 450/618 | **+$1,231** | **[+174,+2377]** | $591 | −$161 |
      | 20 | 574/494 | +$1,192 | [−139,+2698] | $780 | −$191 |
      | 30 | 792/276 | +$2,264 | [+435,+4284] | $1,180 | −$223 |

      8→15：損益翻倍、CI 下緣由負轉正，最差單小時僅由 −$89 惡化到 −$161。
      **現行的 8 是最糟位置**——已付出 CI 轉負的代價（5 的下緣是 +138），
      卻還沒拿到放寬的好處。峰值佔用在 15 時僅本金的 30%，資金不是限制。

      **但不得直接改**，三個理由：(a) 樣本僅 11 天，並發是**尾部參數**而這
      11 天沒有真正的 crypto 崩盤；(b) 「最差單小時」是實測值不是最壞可能；
      (c) 它是相關性預算的一部分，要跟兩層 kill、風險預算一起定，不能單獨調。
      → 併入本節設計，與那三個開放問題一起拍板。

- [ ] 統一 trade ledger schema——設計已入稿（設計稿 §3.1 `pf_intents`/`pf_positions`），
      實作等觸發點（P1 dual-write，見設計稿 §4）
- 誠實註記：全 crypto 策略共享 beta，正交性有天花板；真分散最終要跨資產
  （#3 的 MNQ 方向一致是個種子）。$311 規模下「對沖」= 淨額互抵 + 相關性
  預算 + 兩層 kill，不是買避險工具（成本>保護）

### 0.42 「雙錨」—— **不是這套系統的策略**，只是交叉引用（2026-08-05）
**它住在 `jarvis` repo、跑 Bitget、與這套系統不共用資料層也不共用風控框架。**
記在這裡只是為了跨 session 交叉引用，**不佔這套系統的策略編號**（本系統只有
V7 / 流動性獵取 / 撤單流 三條）。所有任務與 Gate 都在那邊管。

雙向逆勢加碼 DCA（斐波那契階梯），來自使用者早年實跑過的參數表。
**完整規格與預註冊 Gate 在 `../jarvis/雙錨_規格.md`**（那份是真相源）。
命名：兩條腿各自錨在週期起點、逐倉獨立、一側死不拖死另一側 —— 這個結構
就是它對 BTC R²=0.003 的來源。**不叫「網格」**：jarvis 同 repo 已有真正的
等距網格（src/grid.js + GridBot），而這條是錨定階梯、金額遞增、整籃出場，
兩者損益結構差很遠（固定帶寬網格 −5.74%/季 vs 這條 +7.3%/年）。

凍結配置：雙向、25 階斐波那契、間隔 2%、整籃止盈 2%＋追蹤 0.2%、
**名目 ≤1× 權益**（硬性上限，觸發即停補、不平倉）、帳戶槓桿 5x。

已確立：16 幣 × 4.9 年 → 年化中位 +7.3%、淨MDD最差 −26.3%、**強平 0/16**；
逐年 79 個「幣×年」**90% 為正**、每年中位皆正；**對 BTC R² = 0.003**（與 V7 正交，
屬組合層而非訊號層）；配對選擇的偏離比值有持續性（分半 +0.425、OOS 20/20）。

未確立（決定能不能上線）：**存活者偏誤**（16 幣全存活，合成死亡螺旋會爆倉，
真實下市幣無資料）、**零前瞻樣本**、維持保證金用 0.5% 對小幣偏樂觀、
配對版資金費未建模（系統性低估成本）。

- [x] **P0 shadow 記帳**（2026-08-05 完成）：`jarvis/src/dcaledger.js` append-only
      JSONL + `gateN1Stats()` 把四關寫成程式碼；`src/dcabot.js` 執行器（live 被
      `start()` 擋下）；`/api/dca/{status,gate}` + POST `{start,stop}`。
      規則抽進 `src/dca.js` 為**唯一定義**，`backtest/lib-dca.js` import 同一份
      → 回測與實盤不可能漂移（同構於本專案核心原則 #2）。45 個測試綠
- [ ] 起 paper 實例開始累積（Gate N1 要 100 個「幣×月」）
- [ ] Gate N1（統計）：≥100 個「幣×月」、幣別分群 CI 下緣 >0、正比例 ≥70%、
      **強平 0 次**（任一次強平即停止累積並回頭檢討規格）
- [ ] Gate N2（執行）：N1 通過後才啟動，≥50 個完整週期、成交偏差中位 <5bps、
      名目上限在 OKX 上要有實際觸發紀錄
- 硬性前提：上線仍受 §0.4 約束 —— 第二條策略上線前必須先有統一風控框架。
  不進 executor、不碰 V7 任何路徑
- 教訓（已寫入規格檔 §不要重跑）：權益止損／動態減倉／逐階止盈／波動歸一間隔／
  regime 開關／流動性池階梯 **全部實測失敗**，多數輸給自己的 null。
  **需要預測的全滅，結構性約束有效** —— 名目上限贏在它條件於「我現在有多深」
  （已實現狀態）而非「環境接下來會怎樣」（預測）

### 0.43 策略 #2 變體總註冊表（2026-08-09 建立）—— 唯一的真相源

**為什麼要這張表**：變體散在四個地方（Gate F 在 `sweep_forward.py`、B/C/D/E 在
`shadow_engine.py`、combo 在 `combo_watchlist.py`、出場在 `exit_variants.py`），
2026-08-09 對話中連我自己都把「主 gate 是 A 還是 B」講錯一次。**四份真相 =
沒有真相。** 這張表是唯一的註冊簿；新變體一律先寫進這裡才准開始計分。

#### 進場變體

| 代號 | 定義（凍結） | 籃子/成本 | 凍結日 | 進度 | CI 下緣 | 判準狀態 |
|---|---|---|---|---|---|---|
| **A ＝ Gate F 正式軌道** | 原始版·波段池·**無濾網** | **core9 · 逐幣真實 bps** | 2026-07-28 | **292/1400**（~8 筆/天 → ~2027-01 中；09-02 更新）| **−0.087** ❌ | **累積中**（CI 含零 · 5/9 幣正；08-08 曾兩條達標，樣本一多退回）|
| A′（同定義·擴大籃） | 同 A | 29 幣 · 統一成本 | — | n=1227 | +0.012 | 參考，非 gate |
| **B** | A ∧ 淺穿越 ≤0.25 ATR（四種池）| 29 幣 | 2026-07-29 | **1428/1400 已達標** | **−0.094** ❌ | **FAIL（2026-09-02 判決，§0.92）** |
| **C** | B ∧ 1m 收回價位內側 | 29 幣 | **2026-08-09**（§0.44）| 767/400（時鐘停止）| −0.128 | **作廢（B FAIL 連坐，09-02）** |
| **D** | C ∧ 量能高於該幣自身中位 | 29 幣 | **2026-08-09**（§0.44）| 371/400（時鐘停止）| −0.149 | **作廢（B FAIL 連坐，09-02）** |
| E | BTC·OI↓∧CVD順破∧清算爆量高 | BTC only | 2026-08-02 | n=31（09-02）| — | **樣本過少，不判** |

**重要的意外**：Gate F 的**兩個質性條件都已達標**（CI 下緣 +0.063、7/9 幣正），
只差樣本；而**加了淺穿越濾網的 B 反而 CI 下緣為負**。也就是說目前資料下
**「不加濾網」比「加濾網」穩** —— B 的濾網價值尚未被證實。這推翻了先前
把 B 當唯一主線的敘事。兩條時鐘並行，各自判各自的。

**2026-09-02 判決落地（§0.92）**：B 達 1428 筆，CI 下緣 −0.094 → **FAIL**；
C/D 依下面的連坐規則作廢。A 退回「兩條質性條件都不成立」（CI 含零、5/9），
繼續累積到 1400。08-08 那個「意外」被證實：濾網沒有價值，剩下的候選只有 A。

**層級規則（連坐）**：A ⊃ B ⊃ C ⊃ D。**上層判 NO-GO 則下層一律作廢** ——
子集的好看可能只是母集內部運氣切片的選擇效應。順序不可顛倒。

#### 組合 watchlist（2026-08-02 凍結·八個提名·計分已套 forward_only）

| 組合 | n | meanR | CI 下緣 | 狀態 |
|---|---|---|---|---|
| **R∧V** 放量刺·縮回來 | 234 | +0.153 | **+0.121** | 樣本足、CI 穩 —— 第二輪最強候選 |
| **R∧快** 五分鐘搶完就跑 | 245 | +0.159 | **+0.076** | 同上 |
| **R** 有縮回就算（最寬）| 409 | +0.124 | **+0.046** | 同上 |
| R∧Q / R∧V∧Q / R∧快∧Q / PA / V∧LIQ | 2–7 | — | 含零或負 | **n<10，不判** |

**紀律**：combo 是 watchlist 不是 gate。**A 或 B 判 GO 之前不得升格**，
且升格時需各自 forward 註冊（前科：08-07 拆分後 V∧LIQ 100%→0%、E 78%→0%）。

#### 出場變體（§0.44b 詳表）

通過離線三關：**hold_4**（+0.0071, p=0.046）、**fail_fast**（+0.0078, 8/9, p=0.025）。
其餘八個 FAIL，含使用者提案的 pool_target / pool_ladder。
**兩候選都不得現在替換** —— 換出場 = 所有進場時鐘重數。

#### 2026-08-09 三份分析：濾網、幣種、並發（`sweep_filter_and_universe.py`／`sweep_capacity_weighted.py`／`sweep_concurrency_cost.py`）

**① 淺穿越濾網在扔錢 —— B/C/D 階梯的地基有問題**

| | n | meanR | 日聚類 CI 下緣 |
|---|---|---|---|
| A 全部（無濾網） | 1062 | **+0.1047** | +0.0236 |
| B 濾網留下的 | 379 | +0.0990 | +0.0076 |
| **濾網丟掉的** | 683 | **+0.1078** | +0.0090 |

留下 − 丟掉 = **−0.0088R**：被丟掉的那批**反而略好**，而且丟掉了 **64% 的樣本**。
這解釋了註冊表的怪現象（A 的 CI 下緣 +0.063 vs B 的 −0.016）：**不是 B 的 edge
較差，是 B 砍掉三分之二樣本、統計力垮掉**。C/D 是 B 的子集，地基同樣受影響 ——
它們 08-09 才起算、尚未累積，**現在停損最便宜**。待 A 判決後一併重估。

**② 幣種精簡（29→18）反而更差，不要做**

按「容量 ≥$50/筆 + 樣本 ≥8 筆」篩（**不用績效排序**，C3 紀律）留 18 幣：
CI 下緣從 **+0.024 掉到 −0.017**。因為被剔掉的小幣訊號多且好（HBAR +12.65R、
SAND +8.03R、VET +7.30R），它們只是**吃不下錢**，不是沒有 edge。
→ 結構性衝突：**edge 分佈與容量分佈不在同一批幣上**。

**③ 容量加權（方案 C）是三者中唯一可執行且不丟訊號的**

本金 $2000、每筆目標 2%＝$40、並發 8：

| 方案 | 成交 | 總損益 | 日聚類 CI | 幣正 | 峰值佔用 |
|---|---|---|---|---|---|
| A 齊頭 29 幣 | 253 | +$695 | [−1,+1383] | 18/27 | $320 |
| B 精簡 18 幣 | 232 | +$483 | [−687,+1697] | 10/18 | $320 |
| **C 容量加權 29 幣** | 253 | **+$583** | **[−9,+1201]** | **18/27** | $320 |

`risk_usd(sym) = min(帳戶×風險%, 該幣容量)`。C 拿到 A 的 84% 損益、保住
27 幣廣度，且**每一筆都是真的下得出去的量**（A 的數字是紙上的：小幣吃不下 $40）。

**④ 最大的單一槓桿：並發上限（見 §0.4）**

1068 筆訊號有 **815 筆（76%）被並發上限擋掉**，而且不是因為沒錢
（峰值佔用僅本金的 16%）。**同一小時內配對比較：成交 vs 被擋 = +0.059R，
CI[−0.016,+0.134] 含零 → 先到先得不是在挑好的，是隨機丟掉等值的 edge。**

#### 新變體的註冊程序（不得跳過）

1. 寫進本表：定義（可執行的文字）、籃子、成本情境、層級歸屬
2. 宣告**凍結日**與**起算日**（起算日之前的樣本一律只列參考）
3. 宣告樣本門檻與判準（CI 下緣 >0 + 幣別一致性），**事後不得放寬**
4. 起算後才開始計分；`first_seen < exit` 才算真前瞻

### 0.44 變體 C / D 正式預註冊（2026-08-09）—— 第二輪 family，時鐘今天起算

**為什麼要補這道手續**：C（+0.126、CI 下緣 +0.019、n=357）與 D（+0.132、
CI 下緣 +0.009、n=168）的數字都已經比主 gate B（+0.090、CI 下緣 **−0.002**）
好，使用者問「為什麼一直只看 B」。答案不是 B 比較好，是 **B 是唯一在看到
任何 forward 資料之前就凍結的**（07-28 註冊時 forward = 0）。C/D 的定義雖然
也早就存在，但它們被「拿出來當候選」是在資料累積之後 —— 沒有正式的起算日，
就永遠只能算觀察，不能算證據。這一節把那個名分補上。

**同族前科（為什麼這件事不是形式主義）**：2026-08-07 拆分挑選期 vs 真前瞻，
R∧Q +0.973/100% → n=4 CI −0.206；V∧LIQ +1.085/100% → −0.206/0%；
E 三面板 +0.766/78% → −0.206/0%。**挑選期 100% 勝率的組合前瞻全滅。**
「跑得好」本身從來不是授權。

**凍結定義（不可改，改就是新變體）**
- **C = B ∧ 收回**：變體 B 成立，且 1m 價格在確認窗內收回到價位內側
- **D = C ∧ 量能高**：C 成立，且該筆的訂單流量能高於**該幣自身歷史中位**
- 進出場規則完全沿用 sweep_core 凍結版（3.5 ATR disaster stop、HOLD=8、
  0.05 ATR/邊滑價）。**本節不改任何出場**——出場候選見 §0.44b

**起算與判準（預註冊，事後不得放寬）**
- **時鐘起算日 2026-08-09**。此前的 C n=357 / D n=168 是**既有基礎**，
  列為參考，**不計入新時鐘**
- 樣本門檻：各自 **n ≥ 400**（新起算後），約需 4-5 個月
- 判準：**日聚類 bootstrap CI 下緣 > 0** 且 **≥6/9 核心幣同號**
- **層級規則（關鍵）**：C 與 D 是 B 的**子集**（C⊂B、D⊂C），不是替代品。
  **B 若判 NO-GO，C/D 一律連坐作廢** —— 地基不成立，上層的好看很可能只是
  B 內部運氣切片的選擇效應。順序不可顛倒
- B 若判 GO → C/D 以本節的樣本獨立計分，過了才談用 C/D 取代 B 當交易規則

**2026-08-13 補記：這道手續當初只寫在文件裡，程式沒跟上，差點白做。**
`shadow_engine.gate_progress` 對 C/D 一直印**未過濾**的數字，每週報告因此讀作
C +0.0965 / CI 下緣 +0.0097、D +0.1021 / +0.0062——兩個都像是已經過門檻了。
套上 08-09 起算後：

| | 未過濾（含既有基礎） | **真前瞻 ≥08-09** |
|---|---|---|
| C | n=474、+0.0965、CI 下緣 **+0.0097** | **n=117/400、+0.0068、CI 下緣 −0.0886** |
| D | n=236、+0.1021、CI 下緣 **+0.0062** | **n=68/400、+0.0283、CI 下緣 −0.1178** |

**edge 全部在挑選期裡**，真前瞻的 meanR 幾乎是零。這正是本節開頭那段「同族
前科」的第二次上演（R∧Q +0.973 → 真前瞻 n=4、CI −0.206），只是這次差點被
自己的報表蓋過去。已修：`shadow_engine.py` 加 `CD_CLOCK` / `CD_GATE_N` 與
`_since_cd_clock()`，gate 行改印 `n/400 since 2026-08-09`，既有基礎只以
`base N excluded` 呈現、永不併入計分。排除掉的 base 數（357 / 168）與本節
記載的數字對得上，是切點正確的獨立佐證。

**教訓（寫進儀式）**：預註冊的「起算日」如果只存在於文件，等於沒有——
記分的程式必須自己知道那條線。任何新變體註冊時，**同一個 commit 要同時
改記分程式**，否則下一份週報就會用挑選期的數字替它背書。

### 判決補記（2026-08-31；時鐘 ~08-22 已滿，漏出判決——與 §0.491 Moderate 同款的漏）

**變體 C：依凍結判準 FAIL。** n=695/400（滿）、meanR **−0.0261**、日聚類
CI 下緣 **−0.1478**（判準要求 >0，第一條即敗）；逐幣廣度 core9 6/9 正
恰觸門檻但已無意義。29 幣全樣本逐幣：13 幣正 16 幣負，最差 TRX −0.42、
SOL −0.21。**「B ∧ 收回」的漂亮數字（挑選期 CI 下緣 +0.0097）在真前瞻
上第三次重演為零下**——同族前科 R∧Q、V∧LIQ 之後又一例。
正式地位依層級規則隨 B：B 判 GO → C 的 FAIL 記錄成立；B 判 NO-GO →
C 連坐作廢。**兩種情況 C 都永不成為交易規則**，此節到此收案。
變體 D：336/400，約 09-03 滿，同一讀法屆時補記；當前 meanR −0.0426、
CI 下緣 −0.163，走向與 C 相同。

**流程教訓（第二次了）**：時鐘滿了不會自己出判決——Moderate 時鐘（08-26
滿）與本節（08-22 滿）都是事後盤點才補。已知的結構性缺口：prereg 看板
只顯示進度，**沒有「已達標待判決」的告警狀態**。修法（小）：看板對
n≥gate_n 的 open 時鐘標「⚠ 待判決」，讓漏判自己現形。

### 0.44b 出場變體現況（2026-08-09 更新，含使用者提案的池子目標）

`research/sweep_failure/exit_variants.py` 十變體配對檢定（同一批進場只換出場，
三關：G1 配對均值>0且兩半同號、G2 ≥6/9 幣同號、G3 bootstrap CI 不含零且置換 p<0.05）：

| 變體 | 配對 dR | 兩半 | 幣同向 | p | 判決 |
|---|---|---|---|---|---|
| hold_4 | +0.0071 | +0.007/+0.008 | 7/9 | 0.046 | **PASS** |
| fail_fast | +0.0078 | +0.010/+0.006 | 8/9 | 0.025 | **PASS** |
| trail_2atr / trail_1atr / half_at_1r / hold_12 / giveback | ≤+0.0003 | — | 3-6/9 | — | FAIL |
| **pool_target**（使用者提案·TP1 全出） | **−0.0020** | −0.004/+0.000 | 4/9 | 0.091 | **FAIL** |
| **pool_ladder**（TP1/2/3 各 1/3） | **−0.0010** | −0.001/−0.001 | 4/9 | 0.175 | **FAIL** |

**池子目標為什麼失敗（機制，不是運氣）**：它是**限價停利**——摸到才成交。
獲利被截斷在池子位置，沒摸到的虧損卻不受影響：**削掉右尾、留下左尾**。
而這條線的報酬靠正偏（中位 +0.11、最好 +2.25），右尾正是命脈。
ladder 略好於 target（−0.0010 vs −0.0020）也吻合此解釋（分批保留部分右尾）。
**V7 出場研究第三次得到同樣結論**：11 變體全敗給 3xATR，原因同樣是
「收緊會把 winner 提早掃掉」。→ 池子目標從候選名單移除，勿重跑。

**副產品**：通過的兩個都是**提早出場**，而 fail_fast 正是「用結構而非時鐘」
——與使用者的想法同源，差別在它用結構判**論點失效**而非**目標達成**。
這條線的結構訊號值錢在「認錯」那側，不在「停利」那側。

**紀律**：兩個候選都**不得現在替換** ——Gate F 跑在現行出場上，換出場 = 424 筆
重數。等 B 判決後才談，屆時需各自 forward 註冊。

### 0.45 策略 #3 變體 B（2026-07-29 預註冊 + 同日修訂）— 每小時 shadow 累積中
使用者批評「流動性只用位置定義、pivot=10 不夠明確」→ 當日測完落地：
- **pierce 濾網**（掃單穿越 ≤0.25 ATR）：三桶單調、core9/added20 分開成立、
  前後半分開成立、11/11 季正、門檻曲線平滑無懸崖、非停損距離/ATR regime 假象。
  33% 交易佔 84% 利潤，SR/筆 0.050→0.128，VIF 6.85→2.69
- **流動性來源擴為四種池子**（swing / session / PDH-PDL / PWH-PWL，全部測試
  全部報告未挑選）。PDH/PDL 比 swing 還好（+0.0368 vs +0.0320）。pierce 在
  四種上各自獨立翻倍 = 它是結構不是對 swing 擬合
- 合併+濾網：n=39801、+0.0673、VIF 7.70、1327 筆/月 → **CI 過零 ~1.9 個月**
- **並發已量測**：無上限最多 54 個同時持倉、最糟單日 −64.2R = 不可交易。
  加上限後 **edge 存活**（cap 3 仍有 +0.0508）→ 可交易區間 = **cap 5-10 ×
  每筆 0.15-0.25% 風險**（最糟單日 −4% ~ −8%，遠離 −20% kill switch）
- **撤回**：equal-levels（重疊 pivot 密度）是前視洩漏——非因果版 +0.1198，
  因果版單獨只有 +0.0292（低於基準）。已記入 mistake.md
- [x] shadow 每小時記錄四種池子（`SweepShadow` 排程）+ `--gate` 進度
- [x] **變體 D 註冊（2026-08-01）**：D = C ∧ 量能高（vshock ≥ 該幣自身此前
      獵取中位數，因果·零參數·≥5 先例）＝研究的訂單流組合配方（收回∧量能）
      正式進 shadow 並列追蹤。首讀 n=35 meanR −0.011（C +0.053）——量能條件
      早期反而扣分，樣本太小僅記錄。D⊂C⊂B⊂A
- [x] **變體 C 註冊（2026-07-31）**：C = 變體B ∧ 流確認（收回內側=1），無數值
      門檻（量能跨幣尺度不可比，原始值續記、十月用分位數框架驗）。觀察 cohort：
      同 gate 算術並列顯示，不改 A/B、不承諾升級——升格為可交易規則仍需自己的
      forward 證據。shadow log 前瞻記錄流特徵（收回/攻擊分鐘/量能/追價/吸收）
- [x] PortfolioClocks 週報已含變體 B 進度（否則 2 個月時鐘沒人看）
- [x] 等 forward：n≥1400 且 day-clustered CI 低緣>0（同 Gate F 算術，未放寬）→ **2026-09-02 判決 FAIL**（n=1428，CI 下緣 −0.094；§0.92）
- 紀律：**變體 A（凍結、swing、無濾網）仍是 Gate F 正式軌道，未被改寫**；
      #3 的搜尋今日到此為止，任何新想法先寫下來、等 forward 窗口跑完再測

### 0.484 地形總目錄預註冊（2026-08-02，同日修訂：使用者指正加入市場結構層）
D1-D3 已定案（獵取情境/前方距離/背後距離，最終關全過）。**S 層（市場結構，
使用者指正的上位分類軸）插在最前**，D7 併入 S3 取消。測序：
S1→S3→S2→D4→D6→D9→D5→D8→D10。預測凍結：
- [x] **S1 結構方向 ☠️ G1 陣亡**（2026-08-02）：順−逆 gap 全期 −1pp、
      兩半反向（+4/−6）——結構方向對 V7 訊號零資訊。教科書與反轉派雙輸，
      盤整桶 61% 亦在噪音內
- [x] **S3 折價/溢價 ⚠️ G3 邊緣未過**（2026-08-02）：G1 過（+8/+8/+12 兩半
      同向）、G2 過（4/5 桶同向，唯 ctx=none −4）、G3 置換 p=0.0215 ✓、
      逐季 0/3 逆風 ✓、**但 bootstrap CI 下緣 −0.0 恰好觸零 → 按協議 FAIL**。
      方向與凍結預測一致（折價勝）、H2 +24 增強中——**停在門口的候選**，
      forward 樣本長大後可復審一次（僅一次）
- [x] **S2 結構事件距離 ☠️ G1 三具名對比全滅**（2026-08-02）：P1 CHoCH順
      vs BOS順 +11 但 H1=0；P1b CHoCH順 vs CHoCH逆 −16（方向反且 H2 thin）；
      P2 假BOS順 vs 假BOS逆 −4 兩半反向。CHoCH逆 82%(n=34) 未具名不追。
      **→ 結構層（S1/S2/S3）蓋棺：0 席。V7 的地形是流動性，不是形態**
- [x] **D4 牆的等級 ☠️ G1 效果量不及格**（2026-08-02）：方向與預測一致
      （日週級牆更硬 54% vs 盤中級 57%）、兩半同向 +4.7/+3.7，但 pooled
      +3.61pp < 4pp 最小門檻。技術註記：同一價位常堆疊多種池身分，歸屬
      規則=取最高等級標籤（swing 高幾乎必兼時段高，tie-break 按確認時間
      會把 swing 歸零——第一版就踩了這個儀器坑，已修）
- [x] **D6 翻轉位 ☠️ G1 陣亡**（2026-08-02）：背後空樣本內 翻轉墊背 55%
      vs 真空 56%（−1pp、兩半反向）；教科書翻轉子集也只 54%。**重要負面
      資訊：D3 的墊背效果不是「價格記憶」（翻轉位也該有），是真實掛著的
      未掃流動性**
- [x] **D9 彈簧後開槍 ☠️ G1 陣亡**（2026-08-02）：fade 內 彈簧(深∧收回)
      61% vs 其他 60%（+1pp、兩半 +5/−4）。Wyckoff 彈簧形狀在 fade 情境
      之上零加值；「深∧未收回」64% 未具名不追
- [x] **D5 池子密度 ✅ 三關全過＋六項彈藥庫——本戰役唯一取得席位**
      （2026-08-02）：疏(≤1池) 62% vs 密(≥3池) 54%，兩半 +9/+10、G2 5/5
      桶同向（D2 兩桶內 +13/+4 → 非 D2 換皮）、G3 p=0.0225 · CI [+1.0,
      +15.6] · 逐季 0/3 逆風。彈藥庫：三分 +9/+12/+9、方向 UP+12/DOWN+5、
      **ATR 機械混淆直接駁回**（密佔比跨 vol 分位 42/38/37% 持平）、桶界
      不敏感（+8/+8/+11/+9）。註記兩條：中vol 格 −3pp（z<0.5，是零非反
      轉）；形狀是 ≥3 的**階梯**非遞增梯度。**整合遞延**：不動凍結的
      T0-T3/§0.483 扳機與地形戳記——D5 列為下次 policy 修訂（扳機結算後）
      的第 4 維候選
- [x] **D8 獵取風暴 ☠️ 註冊桶退化**（2026-08-02）：全池種計數下 24h 0 場
      只有 4/766 筆、3+ 場佔 93%——註冊時的 base-rate 直覺錯了，對比無法
      成立。不做事後重分桶（composite 教訓）；要重測須以新註冊（例如按
      池種加權計數）重來
- [x] **D10 牆齡 ☠️ G1 陣亡**（2026-08-02）：年輕牆 56% vs 老牆 57%
      （−1/+0/+0），與記錄傾向一致——池齡第二次死亡，完整性補完
- [x] **L1 清算價位（第五種池子）☠️ 未取得席位**（2026-08-02）：資料只有
      逐時清算金額（無未來熱圖），唯一因果構造＝**清算爆量那根的極值當
      「槓桿死亡現場」**（多爆→低點/空爆→高點，門檻=該側自身的因果擴張
      90 分位，無調參）。1565 個池、**1523 個已被掃（97%）**——清算現場
      壽命極短，這是後面樣本薄的結構原因。
      · **L-A 墊背側 FAIL**：標準池真空樣本內，清算墊背 57%(69) vs 真空
        55%(435)，gap +1.4pp 兩半反向 → **與 D6 同結論：被吃掉的流動性
        就是沒了**，清算現場不是支撐
      · **L-B 前牆側 G1 過但 G2 無法執行**：清算牆 57%(49) vs 無牆 62%(458)，
        gap −4.4/−3.0/−8.5 兩半同向、方向與 D2 一致；但 n=49 攤到 ctx 三桶
        後每桶 <20，**殘餘關只湊出 2 格（協議要求 ≥3）**。這是「樣本不足
        以驗證」≠「沒效果」——與 S3 同列**門口候選，一次復審權**（V7 訊號
        再長一批後重跑，不得改門檻/不得換分桶重試）
      · 覆蓋率現實：就算 L-B 是真的，也只碰到 49/507 的乾淨跑道訊號（全樣本
        ~6%），邊際價值小
- [x] **現行 regime 抽查（2026-08-01）**：發現 tracked_signals 的 Strong 月量
      在 2026-04 出現結構性斷點（114-175/月 → 12-28/月，時點對上 04-19 warmup
      buffer 修復——舊量是 fallback 閾值灌水，現在才是設計值）。因此地形 766
      筆樣本**只有 ~66 筆來自現行解碼 regime**。抽查（`terrain_recent_regime.py`，
      事前聲明只看方向翻不翻、n 不夠跑三關）：**四維方向全部一致**（D1 −26 /
      D2 −20 / D3 +31 / D5 +17，5/1 起的更嚴切點同號），逐日剔除 31 天無任何
      翻轉。**但 gap 變大一律當小樣本放大處理，不得用來上調任何結論**；扳機
      仍是唯一裁判。副作用：+60 筆 Strong 在現行速率下 ≈ 3 個月，不是幾週
- **戰役總結（2026-08-02）**：10 維測畢，1 席（D5）+ 2 門口候選（S3 統計
      差 0.0、L1-B 樣本不足），各持一次復審權。地形定案四維：**D1 情境
      (veto)、D2 前方牆、D3 背後支撐、D5 池子密度**——全部是流動性維度，
      結構/形態維度全軍覆沒。**兩次獨立證明（D6 翻轉位、L1-A 清算現場）：
      被消耗掉的流動性不留下任何效應**——系統吃的是還掛在那裡的單
協議（不臨場改）：單維兩半同向 → 已定案邊際桶內殘餘檢定（防換皮）→
最終統計關（置換+bootstrap+逐季）。766 筆同一樣本；S/D 全目錄 ~24 looks，
預期 1-2 個機率假陽性，協議就是為它們準備的。
**概念接點記錄**：市場結構的 BOS/假突破判定 ＝ 獵取線的 ¬R/R——兩研究線
在 swing 池上共用同一個物理事件的兩種語言

### 0.483b 扳機結算日決策框架（2026-08-20 預註冊，扳機到期之前凍結）

**為什麼現在寫**：扳機已走到 34/60、gap +21.9pp，照近期開火速率 ~9 月
到期——比快照估的 11 月早。到期那天「選哪一檔」如果當場才想，就是看著
結算數字挑檔位＝檔位版的挑選偏誤。所以決策規則現在凍結，屆時只准執行。

**結算日輸入（全部用已凍結的儀器，不新增統計量）**：
90d clock 的 gap 點估計、gap 的 bootstrap CI95（樣本＝kept+veto 兩組
90d 內已結算 Strong）、veto 率（被 veto 的 Strong 佔比）。

**凍結決策表**：
| 結算情形 | 動作 |
|---|---|
| gap ≥8pp ∧ CI 下緣 >0 ∧ veto 率 ≤40% | **採 T1 進 executor**（只上「追突破 veto」一維——最小可回退步幅；T2/T3 不得跳級） |
| gap ≥8pp 但 CI 含 0 | 維持 display-only，**延長 +30 筆 Strong 後復讀一次**（只准延一次；仍含 0 → 關扳機，地形永留顯示層） |
| gap <8pp | 扳機失敗，T0 收案 |
| 任一檔 veto 率 >50% | 該檔切太深，降一檔 |

**T1 上線的強制配套（沒有它就不准上）**：executor 對被 veto 的進場記
**反事實影子帳**（該筆若進場會怎樣——沿用既有 shadow 機制的語意）。
- **升檔 T1→T2**：T1 forward 影子帳 ≥30 筆後，「前方牆」維度在該窗口
  自己的 kept-vs-veto gap ≥8pp 才升；T2→T3 同理，且 T3 須先補過
  0.482 同款三關（菜單裡的既有註記）
- **自動回退**：T1 forward 影子帳 ≥40 筆時若 vetoed 組**優於** kept 組
  （gap ≤0）→ 退回 display-only，扳機重置。回退不是失敗，是濾網
  在 forward 上沒複製 in-sample 的證明——這正是影子帳存在的目的

**紀律**：結算日不得引入本表以外的任何檔位、門檻或「這次情況特殊」。
本表寫於 34/60（2026-08-20），結算數字尚未存在。

**2026-08-30 狀態**：53/60，gap **+3.0pp**（kept 56.5% n=69 / veto 53.6%
n=28）。軌跡 08-08 +23.0 → 08-17 +21.5 → 08-24 +13.6 → 08-26 +10.0 →
08-30 +3.0：90d 窗口把推導期樣本滾出去、前瞻樣本補進來，被 veto 組的
勝率從 38% 回到 54%。若結算日仍 <8pp → 依表「扳機失敗，T0 收案」。
使用者確認：**等到 60，照表執行，中途不動。** 平行的 Moderate 時鐘已
作廢（§0.491 判決）。Q3（地形 vs regime 同效應）若扳機失敗即無題可測。

### 0.483 結構濾網推進：檔位菜單＋地形標記上線＋上線扳機（2026-08-02）
使用者定調「往這塊前進，接受訊號減少」。三步落地：
- **檔位菜單**（`v7_filter_policy.py`，全部兩半勝率同向）：
  T0 現狀 91.5筆/月 60%/+0.20% → T1 +追突破veto 68/月 62% →
  T2 +前方牆veto 48.6/月 63% → **T3 +需背靠支撐 23.2/月 68%/+0.37%**
  （T3 兩半 65/73；注意 T3 此疊法未過 0.482 最終關，採用前要補跑同款三關）
- **地形標記已上 production 告警**（`indicator/terrain.py`，顯示層 only）：
  每個 Strong/Moderate 告警帶「🗺 地形: 追突破⚠️/前方牆或淨 x.xATR/背後支撐
  或空」——操作者即時看到濾網會怎麼判，訊號照發、交易邏輯零改動。
  模組永不拋錯（全滅路徑測試通過），失敗只是不顯示
- **上線扳機（凍結）**：自 2026-08-02 起累積 +60 筆新 Strong 後，若 90d
  clock 的 kept-vs-vetoed gap 仍 ≥8pp → 提交「T1 veto 進 executor」的
  informed 決策（選檔 T1/T2/T3 由使用者按菜單決定）；未達則繼續等。
  在那之前 production 進場規則不動

### 0.482 最終統計關（2026-08-02，`v7_structural_final.py`）——三存活者全過
置換 p（2000 洗牌）＋bootstrap 95% CI＋逐季正負號，三關全設 keep 門檻：
- **V 追突破 veto**：+9.8pp · p=0.008 · CI [+1.5,+17.9] · 逆風季 0/3 ✅
- **A1 前方有牆**：+8.6pp · p=0.028 · CI [+0.4,+16.8] · 逆風季 0/3 ✅
- **A2 背靠支撐**：+12.1pp · p=0.0005 · CI [+5.1,+19.3] · 逆風季 0/3 ✅（最硬）
- （複合格已於 0.481 擊落不再議；首跑 A2 顯示 −12 是驗證腳本 good/bad 槽位
  寫反，修正後與 0.481 各切面一致——驗證腳本自己也要被驗，mistake.md
  2026-07-29 診斷工具教訓的迷你重演）
- V7 結構濾網最終名單＝這三條，全部進十月議程；production 仍不動

### 0.481 位置宣稱再驗證（2026-08-02，`v7_price_location_verify.py`）
使用者要求「再次驗證是不是真的」——六關攻擊（三等分/方向/regime/波動控制/
veto情境內/置換檢定）：
- **A1 前方跑道乾淨：存活**——所有切面無反轉（方向+9/+10、波動 +2~+13、
  veto 桶內 +2~+14）；但效應隨時間衰減 T1+16→T3+6，要盯
- **A2 背靠支撐：存活最穩**——全切面正（T +8/+18/+13、DOWN +16、
  veto 桶內 +4~+20），三宣稱中最硬
- **A3 複合格：降級** ❌——T3 −1pp、置換檢定 p=0.084（未達 5%）、
  加值集中在無獵取情境（fade 桶 n=74 只 +1pp）。複合格是看過邊際後
  post-hoc 選的格子，證據不足；改用 A1、A2 各自當條件，複合版交給
  forward 驗證
- 教訓再確認：邊際效應各自為真 ≠ 交集更強；post-hoc 選格要打折

### 0.480 V7×真·價格位置（2026-08-02，`v7_price_location.py`）
使用者糾正定義：0.477/0.479 測的是「事件情境」（前 4h 有沒有掃池），非位置。
真位置＝開火時距未掃池的 ATR 距離（因果池生命週期重建，766 筆 live Strong）：
- **前方距離：阻力故事勝出**（磁鐵敗）——前方跑道乾淨（>2.85 ATR）65% vs
  近牆 57%；H1 單調 46/51/62、H2 同向 64/66/68
- **背靠支撐：兩半皆 +12pp**——後方近池（≤1.79 ATR）H1 62% vs 48%、
  H2 74% vs 62%
- **複合格「背靠支撐∧前方乾淨」：n=107、WR 66%/+0.30%**（基準 60%/+0.20%），
  兩半皆高於基準
- 為何特徵層死（06-01）訊號層活：ensemble 邊際 AUC 問「模型還沒吸收嗎」，
  訊號層條件問「開了槍之後哪些情境準」——不同問題
- 落地：與 0.479 veto 同族＝**V7 結構情境濾網候選組**（追突破 veto＋位置
  複合格），十月一起議；production 不動

### 0.479 V7 獵取逆向 veto——穩健性切片＋註冊為觀察規則（2026-08-02）
`v7_raid_veto.py`。規則（凍結）：Strong 訊號方向若順著前 4h 內任一池掃穿的
突破方向＝follow → veto 候選。切片結果：
- 窗口 2/4/6/8h 全部 follow 最差（52-55%）＝平滑非懸崖 ✓；regime 內部
  梯度成立（CHOPPY 91/83/70、UNKNOWN 61/57/50）＝非 regime 代理 ✓；
  方向分拆 DOWN 強（49%）UP 弱（56%）
- **誠實旗標①**：三等分 T1 反向（none 44% < follow 49%），T2/T3 大幅正向
  （+18/+22pp）——效應近期增強但最早期反向
- **誠實旗標②**：被否決桶的方向報酬仍為正（+0.11%/4h）——veto 是品質
  集中（62% vs 52%），不是砍掉負 EV
- 反事實：砍 26% 訊號量，保留桶 WR 59.5→62%、報酬 +0.20→+0.23%
- **落地：觀察規則＋每週 clock**（90d 保留vs否決 gap 已進 PortfolioClocks）。
  production 採用需：gap 在 forward 持續（≥8pp）＋跨過 T1 型反例的解釋
  ＋informed 決策記錄。不是 AUC 突破——是品質/頻率交換

### 0.478 組合篩選→凍結 watchlist→shadow 計分板（2026-08-02）
使用者管線落地：`sweep_raid_combo_screen.py` 全網格 287 格（深度×收回×量能×
攻速×BTC旗標）、事前提名規則（n≥80∧|t|≥2.5∧兩半同號∧雙幣同號→去重 cap 8）：
- **8 個提名凍結進 `combo_watchlist.py`**（R∧V / R∧Q / R∧V∧Q / R∧快 /
  R∧快∧Q / R / PA / V∧LIQ）——定義不可改，改=新註冊
- `shadow_engine.py --combos` = forward 計分板（同日聚類 CI 算術）；已接進
  每週一 PortfolioClocks 報告
- 首讀（僅 5 天，勿當證據）：R n=220 meanR +0.165 CI低緣 +0.034；
  R∧V n=63 +0.186 CI低緣 +0.034；BTC 系組合個位數 n 純記錄
- **上線判準（沿用兩段式）**：watchlist 條目要 forward n 累積 + CI 低緣>0
  + 十月預註冊蓋章，缺一不可；in-sample 篩選只是入場券

### 0.477 V7×獵取情境（2026-08-02，`v7_raid_context.py`）——pred_align 的鏡像
使用者觀察「V7 完全沒用價格行為/位置」。特徵方向 2026-06-01 已死（位置特徵
進 ensemble 零提升）；**事件方向活的**：766 筆 live Strong 訊號按獵取情境分桶——
- 無獵取 64% / fade順 60% / **follow逆 52%**（追著獵取突破方向開火＝最差桶，
  兩半一致：H1 46-54-55、H2 57-66-73 梯度同向）
- 機制：V7 是均值回歸專家，順著新鮮突破開火＝跟獵取反轉動力學對做
- 落地：**「獵取逆向 veto」進十月清單（V7 側濾網候選）**；不動 production
  （Gate A 紀律）。V7↔獵取雙向橋完成：pred_align（獵取問V7）＋本測（V7問獵取）
- cohort 原則重申：組合不各開累積線——欄位層前瞻記錄，配方十月同批資料計算

### 0.476 CVD 背離（2026-08-02，`sweep_raid_divergence.py`）——A-L 菜單收官
菜單第三版唯一新可測格。事件錨定：獵取棒創局部極值時 24h 累積 delta 有無同創。
- **品質面 FAIL 且方向相反**：有背離 netR +0.017 vs 無背離 +0.067——CVD 確認
  的獵取反而更肥（教科書「背離=反轉燃料」不成立；機制：攻擊本身就是順向
  delta，fade 賺的是結構錢不是背離錢）
- 解析面弱存活：背離→突破率減半（13%→7%，雙幣兩半一致）但幅度小、
  高機率是 收回/淺穿越 家族的共線影子（解析已由兩因子講完 AUC 0.844）
- **A-L 菜單至此 100% 有下落**；第三版新增的輔助條款全部對上既有判決
  （攻擊前 CVD 同向=precrowd FAIL；日內 CVD 位置=移植 NO-GO；攻擊前 OI
  大漲=三形式全死；K 回撤中 OI 回升=postderiv 已證實）

### 0.475 三段式序列假設檢驗（2026-08-02，`sweep_raid_sequence.py`·BTC）
使用者假設：反轉＝獵取時(OI↓+清算放大)→獵取後(CVD 翻轉+OI 回升)；延續相反。
- **決議軸（會不會回來）：證實且兩半穩** — R+∧P+ 突破 43% vs 基準 62%
  （H1 46/59、H2 41/65 皆低於基準）；延續格 R-∧追殺 71%（但 H1 54% 未分離、
  H2 86% 近期熱）
- **品質軸（賺多少）：pooled 方向對但 R+∧P+ 兩半翻轉**（−0.20→+0.41）——
  「方向可測、肥瘦不穩」第三次重現
- **機制精修**：最毒格不是延續假設而是 **R+∧P−（−0.199）＝假沖洗陷阱**——
  獵取當下像止損沖洗但事後未確認。P（後段）邊際 > R（當下）邊際 →
  正確紀律＝等後段確認再接，不要見 OI↓+清算就進
- 十月清單追加：序列確認（R+∧P+）為反轉配方候選之一

### 0.474 變體 E 註冊（2026-08-02）——操作者三面板讀法 cohort
E = BTC ∧ Q（OI↓∧順破）∧ 清算爆量≥因果中位。使用者盤感（清算尖峰+OI 沖洗
+CVD 讀法）的可追蹤版本；首讀 n=6 均 +0.43（純記錄，小樣本）。gate line
與覆盤圖 chips 並列顯示。階梯全景：A⊃B⊃C⊃D（層層濾網）+ E（獨立盤感線）

### 0.473 存活特徵前瞻記錄上線（2026-08-02）
使用者：「繼續研究並把這些特徵列入紀錄」——shadow log 新增 BTC 四欄
drv_q / drv_liqburst / drv_gap_oi / v7_align（全部有存活紀錄的特徵），
15/15 BTC forward 列已覆蓋；十月驗證從此拿到全前瞻樣本。
**順手修掉一個靜默失敗**：研究線 cg_*_1h parquet 自 07-28 停更（兩條平行
CG 資料線——DailyCollect 只餵 raw_data/coinglass/，研究線靠 ensure_fresh
偶發觸發；排程綠燈+資料死亡的老家族）→ `research/backfill_all_parquet.py`
已掛進 daily_collect.bat step 1.5，每日隨排程刷新

### 0.472 存活者堆疊評分 PASS（2026-08-02，`sweep_raid_survivor_stack.py`·BTC）
使用者 A-L 清單的「簡單評分測試」——但只堆疊有存活紀錄的 7 特徵（pierce/
att_min/收回/量能/Q/清算爆量/pred_align），5 折擴窗 OOS，預定判準通過：
- **4/5 折 top-20% 勝折基準；pooled top-20%（n=119）netR +0.110 / WR 63%
  vs 其餘 +0.022、基準 55%；bootstrap CI [+0.005, +0.214] 全正 → PASS**
- 誠實條款：BTC-only n=812（5.5 個月窗）；第一折很醜（訓練 ~240 筆不夠，
  AUC 0.46/top −0.43），折 AUC 隨資料量爬升 0.46→0.71——模型要幾百筆才醒
- 全池版（G）已 NO-GO——**「只堆存活者」是揀特徵紀律的直接回報**
- 落地：十月預註冊的評分候選（凍結特徵集+訓練窗規則後在新資料驗）；
  可選：shadow BTC 列前瞻記錄 score（需凍結模型 artifact，十月一併決定）

### 0.471 獵取後衍生品動態（2026-08-02，`sweep_raid_postderiv.py`·BTC）
0.469 議程收尾。單特徵＋OI×CVD 四象限，F1（gap≥1h 進場品質 n=328）/F2（+2h
倖存者決議 n=516）：
- **跨源重現（最硬產出）**：獵取後 CVD 繼續順破 → 突破 70%（兩半皆 70%，
  低 tercile 52/43）——與 flow_bars 版 pf_taker 同結論、獨立資料源;
  清算第二波 → 突破（高>低 兩半成立）。延續識別配方獲得第二源背書
- **F1 新否決候選（推翻具名預測）**：gap 期 OI 崩（低 tercile）→ fade 有毒
  netR 兩半 −0.51/−0.22、WR 12-31%。與獵取當下相反：**raid 小時 OI↓=止損
  沖洗（好）、獵取後 OI 繼續失血=去槓桿進行中（毒）**——時序決定意義
- **後窗口象限無增量**：F1 塌縮成 OI 邊際、F2 塌縮成 CVD 邊際——聯合狀態
  在「當下」有戲（Q）、在「事前/事後」都沒有。三窗口的象限故事完整
- 十月清單追加：否決「gap 期 OI 崩」＋延續識別第二源（pd_cvd/pd_liq2）

### 0.47 V7 特徵移植判決（2026-08-02，`sweep_raid_v7transfer*.py`）
- **276 特徵移植 NO-GO**：單變量存活 93+92 個（遠超機率＝狀態特徵確實與獵取
  結果相關），但 fold gauntlet（每折 train 內選特徵）增量歸零——解析 mean Δ
  +0.017、正折 2/5、CI 跨零；品質 mean −0.025。WQ101 劇本原樣重演：單切分
  的 +0.047 全靠第一折。大量相關 ≠ 邊際資訊，V7 狀態特徵是 keys 的重述
- **pred_align 存活（單一具名 look，無選擇偏誤）**：−side×V7 OOS pred_ret
  ＝「模型站不站在 fade 這邊」→ netR IC 逐折 4/5 正、mean +0.157、
  **bootstrap CI [+0.025,+0.305] 全正**；tercile 模型反對 −0.042 → 支持
  +0.095（WR 53→59%）。**十月預註冊品質濾網候選（BTC）**——production
  每小時本來就有 pred_ret，整合零成本
- [ ] 剩餘議程：獵取後的衍生品動態（OI 去化/第二波清算/資金費率翻轉）

### 0.469 明日議程（2026-08-02 預定）：衍生品流×獵取後 + V7 特徵移植
使用者直覺：衍生品流一定能分辨獵取後行情。兩條具名路線（先寫下防 dredging）：
- [ ] **獵取後的衍生品動態（真缺口）**：0.465 的 post-flow 只用了 flow_bars
      （taker/量能），**沒用 CG 衍生品**。未測：獵取後 OI 去化路徑（繼續降=
      去槓桿延續?）、後續小時的第二波清算、資金費率翻轉。BTC-only，
      對齊 postflow 的 F1/F2 框架（fill 前窗口/固定+2h 決策點）
- [ ] **V7 特徵移植測試**：136 個 direction 特徵（多窗 z-score、累積流、
      positioning）在獵取時刻的快照能否分劇本。紀律：先 screen→conditional
      （對 R+pierce+V 的殘差）→單調+兩半，不做 raw 撒網；另一條廉價路線：
      **V7 模型輸出 pred_ret 在獵取時刻的值**直接當單一特徵測（模型視角
      與獵取事件的交互，一個 look）
- 已知死路提醒（勿重測）：事前佈局(precrowd)、OI 環境分位、攻擊窗追價/吸收、
      期貨 CVD(兩半不穩)、小時級單獨 OI/funding/LS/DVOL(round 2)

### 0.468 真實衍生品流同場排名·BTC（2026-08-01，`sweep_raid_keydrivers_btc.py`）
OI/CVD/清算/資金費率 vs 價格行為，n=917 同方法同場競技：
- **價格行為仍然稱王**：解析 OOS AUC 0.795，permutation 前二仍是穿越+收回
  （+0.106/+0.072）；真流最高只有期貨 CVD +0.017
- **真流單變量席位**：清算爆量解析 AUC 0.731（兩半穩，最強真流特徵，但多變量
  歸零＝資訊被價格行為吸收）；OI 變化品質 −0.08（OI↓＝fade 較肥，Q 象限的
  連續版）；被獵側清算佔比品質 −0.11（邊緣）；資金費率品質 +0.11（邊緣）
- **期貨 CVD 兩半不穩（0.77→0.53）**：無席位
- 品質多變量 OOS 再次失敗（−0.074）——「肉多肥」只有單變量級，跨資料集重現
- 結論：衍生品層在小時尺度大多是價格行為的重述；真正沒測過的真流只剩
  **L2 掛單簿**（歷史不存在）→ 十月 depth_deltas checkpoint 的主菜

### 0.467 關鍵驅動因子定案（2026-08-01，`sweep_raid_keydrivers.py`）
單變量（半切穩定）+多變量（時間切分 OOS+permutation）一次定案，n=1842：
- **解析（會不會回踩）= 兩因子故事，OOS AUC 0.844**：收回內側（perm +0.148）
  ＋穿越深度（+0.118）；攻擊分鐘/吸收是共線影子（多變量歸零）
- **品質（回踩後多少肉）= 只有單變量級**：量能 +0.136 / 攻擊分鐘 −0.113 /
  收回 +0.086（聯合模型 OOS IC −0.097 = 無多變量結構）；量能 H2 遠強於 H1
- **正式退役**：追價佔比、吸收（兩目標皆無獨立訊息）
- 結論：劇本表/變體 D/十月清單用的 R+pierce+V 正是被確認的關鍵集；
  「走哪個劇本」高度可測、「肉有多肥」只弱可測——期望管理要照這個比例

### 0.466 深淺×流劇本表（2026-08-01，`sweep_raid_depthflow.py`）
獵殺深淺（價格行為）×訂單流（R 收回/V 量能）12 格全報告，3 具名假設全過四切面：
- **彈簧劇本（最肥格）**：深∧R（∧V 更肥 +0.245，反轉 72%）——打深卻當小時收回
  ＝深度陷阱。BTC/ETH/前後半全正
- **教科書獵殺**：淺∧R∧V +0.134/反轉 65%（D 配方老家，四切面正）
- **真突破**：深∧¬R∧V 突破 42%（基準 23% 兩倍，雙幣一致）＝延續識別
- **陰乾（最差）**：¬R∧¬V 任何深度 netR 全負——沒收回沒量＝別碰
- **L1 長相**：淺=2 分鐘快閃+高吸收 0.55；深=26 分鐘持久戰+低吸收 0.10（真空）
- **關鍵推翻**：V 的增益隨深度遞增（淺+0.11/中+0.22/深+0.19），而變體 D 綁在
  B(淺)上——**R∧V 的肥肉在中深穿越，D 的定義錯過了彈簧**。十月預註冊應驗
  「R∧V 全深度」而非只 B 子集；D 照跑不改（觀察 cohort 的意義就是被打臉）

### 0.465 獵取後訂單流（2026-08-01，`sweep_raid_postflow.py`）
「獵取後·回踩前」窗口首切（F1 進場品質 n=236 慢回踩／F2 +2h 倖存者 n=350）：
- **存活（四切面一致）**：慢回踩且 pf_taker 高（獵取後資金仍朝突破方向追）
  → netR −0.256 / WR 28%（雙幣、前後半全負）＝**否決訊號候選**
- 候選（方向一致未全單調）：F2 pf_volrate 高（量能不衰）→ 突破 69% vs 低 47%
- 結構事實：立即回踩 n=13,640 均 +0.064 vs 慢回踩 n=236 均 +0.007——edge
  集中在快回踩；慢回踩整體打平，其中「還在追」的子集有毒
- 落地：進十月預註冊清單（否決=慢回踩∧流仍追）；不加 shadow 欄（慢回踩僅
  1.7% 覆蓋率，等預註冊一次驗）

### 0.46 訂單流 × 掃單事件（2026-07-29 首測，樣本不足，等資料）
使用者提案「流動性獵取是特殊事件，資金一定有特別動作」。四種池子讓事件數
×4.5，BTC+ETH 100 天得 n=737（swing-only 時只有 ~165，才做得成）。
- 四個特徵只有 **vshock 單調**（低量 −0.1098 / 高量 +0.1128）——但**4 選 1
  單調正好是純機率期望值**，值得再看的是幅度與 t 跨度（−3.2/+2.8），不是
  單調性本身
- **關鍵子集（淺穿越內 n=273）方向一致但 t 全 <1.5 = 統計力不足** → 不改任何東西
- [ ] 等 flow_bars 長到 n≈2000（約 6 個月）重跑 `research/sweep_orderflow.py`；
      shadow log 有 fill_ts，訂單流特徵可對任何已記錄訊號事後補算
- 觀察：高量+淺穿越 = 量爆但價格沒走 = **吸收**，與撤單流「吸收」劇本同形，
      兩線在此交會但都還沒有足夠樣本

### 0.5 掃單失敗反轉（策略 #3）— Gate F forward 累積中（2026-07-28 凍結）
價格結構事件系，與 v7(ML)/擠壓(撤單) 正交。**2026-07-28 成本審計**：遷入版
滑價符號寫反 → README 頭條 t=8.27 實質零成本；修正後（逐幣真實 bps 費用）
目標執行情境 pool **+0.0255R / t=+3.35 / PF 1.11 / 9/9 正**、全 taker
t=+2.29（見 research/sweep_failure/README.md CORRECTION 塊 + mistake.md）。
edge 薄而依賴執行。同日頑健性量測（cluster_stats.py）：月度 18/31 正
（t=+2.03）、**日分群 bootstrap VIF=2.95 → 有效 t=+1.95**（9 幣連動灌水已
量化）、0.5%/筆組合 ~+40%/年 / MDD 22.8%。定位 = **2σ 候選（帶翻號折扣）**，
「幾個月自證」賣點不成立，改為零成本被動追蹤。
- [x] ~~每月 5 號手動跑~~ → **2026-07-29 已自動化**：Windows 排程任務
      `PortfolioClocks`（每週一 09:30，`research/portfolio_clocks.bat` →
      `portfolio_clocks.py`），四合一 TG 週報：① Gate B 計數（n≥30 推
      SHORT-tilt 解鎖告警）② 月度視窗（5-11 號）自動 fetch+sweep_forward
      （Gate F 進度，帶 STALE-DATA guard）+cross_asset_probe（跨資產
      forward cohort）③ depth_deltas 天數（≥90d 推 subhourly 復活告警）
      ④ 2026-08-10 起自動跑 cancel_lead_ic/cancel_shock_ic 判決並推結果。
      送出層 = send_critical 6×60s 重試 + plain fallback；失敗留痕
      portfolio_clocks.log。**週一沒收到報告本身就是告警**（heartbeat）
- [ ] Gate F（預註冊 2026-07-28；同日收緊為 **day-clustered CI**，資料未累積
      前收緊、之後不准鬆）：forward pooled n≥1400 且 clustered CI95 低緣>0
      且 ≥6/9 幣正 → 才談 tiny live（複用 OKX executor/kill switch/$100 劇本）；
      誠實 runway ~2 年除非 forward 跑得比歷史熱；中途 checkpoint 禁提前放行
      · **2026-09-02 進度 292/1400**，CI [−0.087, +0.175] 含零、5/9 幣正 → 累積中（§0.92）
- [ ] 若 3 個月後 quasi-forward（07-11 起）+ forward 合計 meanR 顯著為負 → 直接收攤，
      不等 n=1400（提前止損條款，同樣預先寫死）
      · **2026-09-02 檢查：未觸發**（quasi +0.030 / forward +0.047，兩者 CI 皆含零）
- 紀律：規則（PIVOT/W/HOLD/DIS）已凍結於 2026-07-28 commit；禁任何參數再調；
      成本模型（bps 情境 A/B）已定，禁看結果後改

### 1. 擠壓指標 × 訂單流系統結合（策略 #2 候選）★ 最優先
流動性真空假說：壓縮後價格往阻力小的一側走，撤單領先成交洩露方向。
工具鏈全部完成（已 merge 進 main）：`squeeze_events{,_cli}.py`、`squeeze_flow_join.py`、
`depth_delta_collector.py`。

執行進度（2026-07-09 session）：
- [x] Step 0-b 無條件基線：**122 事件**（sl_first 53.8%、r_scaleout +0.455、MFE/MAE 2.71/1.23R）
- [x] **Step 2 撤單收集器已上線**：`depth_delta_collector` 掛進 market_data service（commit 927b4b7 pushed），
      `depth_deltas_1m` 自 2026-07-09 起 24/7 累積（本地 smoke 通過，每分鐘 bid/ask add/cancel）。← **資料時鐘已啟動**
- [~] Step 1 flag 統計：`squeeze_flow_join` 兩個 bug 已修（`window_start` 欄名 + cp950 輸出），管線跑通；
      但 `orderbook_snapshots_1m` 深度僅回溯 2026-05-11 → 122 事件只有 **26 個**有覆蓋、全 cell n<100 → **無法下結論**（如預警）
- [ ] Step 0-a Pine 對帳（仍是正式放行門，需 TV 端比對，未做）
      （atr_bo 欄=band=ATR×0.9；丟棄前 3×max(period) 根暖機；TV filterMode=OFF, confirmOnClose=ON）

本 session 額外 powered 檢驗（taker/衍生品訂單流，非撤單）：
- horizon-IC：taker flow 無條件對 1-12h ≈ 0（1h 尺度訊號已衰減殆盡）；squeeze 條件 h2-4 微弱駝峰但 n=119 CI 全含 0
- hybrid 篩選（8 訂單流特徵 × 突破成敗）：廣撒混合體 **OOS NO-GO**（LOO-CV AUC 0.564 CI[0.457,0.671] 含 0.5）；
  唯一 univariate 冒頭 = 方向對齊 Coinbase 現貨溢價（IC +0.23，CI 邊緣離 0，多重比較存疑，可能已在 V7）；taker 系全死
- context 搜索（全 6290 bar，有統計力）：壓縮/Donchian突破/測試關卡/高波動 **無一** context 把 taker IC 拉離 0 →
  **powered NULL**，且戳破 squeeze 駝峰（很可能小樣本雜訊）
- **結論**：taker/衍生品訂單流 × 傳統指標，1h+ 視野 = 已飽和/擁擠（第 4 次驗證）。火花只可能在
  **(a) 差異化訊號 = 撤單（`depth_deltas_1m` 收集中）** 或 **(b) 次小時視野（訂單流還活著的尺度）**

下一步（取代原執行順序）：
- [ ] （被動）等 `depth_deltas_1m` 累積 3-6 個月 → 用今天的 `horizon_ic` / `context_ic` 腳本**原樣換撤單訊號**重跑（差異化訊號的第一次真正檢驗）
- [ ] Pine 對帳（Step 0-a），讓事件時間戳可信
- [ ] 把 scratchpad 的 `horizon_ic.py` / `context_ic.py` / `hybrid_screen.py` 整進 `research/` + 留「換撤單訊號」接口（目前僅在 scratchpad）
- [ ] （可選、便宜）單獨驗 spot-premium：walk-forward + conditional IC vs V7 residual，確認真火花 or 噪音/冗餘

預先登記假設（2026-07-07 定，撤單資料到位後仍用）：
H1 薄側一致 → sl_first 較低；H2 撤單側 → 突破同側 >55%（CI 下緣>50%）；H3 三旗共振 r_scaleout CI 下緣 >0。
紀律鎖：cell n<100 不下結論；前後半同向才算過；看資料後禁改 flag 定義。
樣本量預警：1h BTC 一年約 30-60 事件（含叢聚 122）→ 第一輪樣本不足，可擴 15m / ETH，不降標準。

### 2. OKX.AI Genesis 黑客松 — ⏸️ 擱置（2026-07-10 決定）
**使用者叫停原 ASP 方向**：賣訊號解讀 = 把系統 edge 分享出去（alpha 擁擠即衰退），
per-call 收費完全不對價。okx-asp repo 保留（含已完成的 Claude 解讀層 a95edae）但**不部署、不上架**。
7/17 截止前若要參賽只剩 pivot 到不洩訊號的題目；預設不參賽。
⚠️ 相關事實：主系統 `/json` endpoint 目前**完全公開**（admin guard 刻意豁免），任何人可抓
direction/tier/confidence——使用者知情後選擇**先留公開**（保留產品化彈性）；若立場改變，把 `/json`
加進 guard 前先查內部呼叫者（圖表/bot）。$80 Claude 促銷積分改留給自用（如 sentiment 異源原型）。

<details><summary>原參賽計畫（點開，僅存檔）</summary>
角色 = ASP（Agent Service Provider，賣方）：把量化系統包裝成可被呼叫/
付費的 AI Agent 服務並上架 okx.ai。核心要求「打造並成功上架一個有真實價值
的 ASP」；評審重視 Revenue Rocket（收入+聲譽累積）。
- [ ] okx.ai → Become ASP / Tutorial，今天先提交（審核需時間）
- [ ] 服務描述定稿（草稿見 Claude 對話 2026-07-07；賣「訊號+風控分析工具」
      不賣「保證獲利」，全文附 not-financial-advice 聲明）
- [ ] 上架審核通過 → 發 X 貼文 → 填 Google 表單
- [ ] （相關但獨立）Agent Trade Kit 交易賽季另計：okx.com/en-us/agent-tradekit/competition
- [ ] **隔離規則**：競賽用獨立子帳戶 + 獨立小額資金，與 Stage 3 主帳戶完全分離；
      競賽虧損上限先定死（如 $100，輸完即止），不影響主系統任何紀律
- [ ] **IP 保護**：Skill Square 提交用包裝版/簡化版，V7 真實 edge 不公開
- 加分動機：參賽過程 = EP 系列 + LinkedIn 素材 + 履歷素材（OKX 官方賽事排名可驗證）
- 風險認知：ROI 榜首多為高槓桿樂透倉，不以奪榜為目標，以「完賽 + 正收益 + 內容產出」為目標

</details>

### 3. 朋友跟單系統部署（Phase 0+1 程式碼已完成）
- [ ] Railway env：`OKX_CRED_MASTER_KEY`（Fernet key，存密碼管理器）+ `TG_ADMIN_CHAT_ID`
- [ ] 跑 `migrations/014_okx_accounts.sql`
- [ ] Merge branch → 部署兩個 service（requirements 已含 cryptography）
- [ ] Selftest：自己的第二組 API key 走 /okx_addacct → resume → 跨一筆 trade → delete
- [ ] 通過後才收朋友 key（只勾讀取+交易、不勾提幣；capital ≤ $200）
- 注意：pause 時未平倉位停止管理（OKX stop algo 仍在）——pause 前確認無持倉

### 4. 研究腳本待跑（皆需資料層）
- [ ] `python research/poc_sweep_study.py` — POC × 流動性獵取（手動交易輔助），
      跑完把 Section 1+2 給 Claude 判讀
- [x] `python research/exit_decomposition.py` — **2026-07-10 已跑**：opp_signal 出場是
      利潤引擎（86% WR / +152bps / 多抱反虧 60-75bps → 不可動）；trail 平均 regret≈0，
      只有最差 1/4 漏 70-130bps（其中 9/13 是 Moderate 進場——Strong-only live 大半不存在）。
      結論：出場規則不改；剩餘槓桿 = 條件性重進場（下行）
- [ ] **flow 重進場**（pre-registered 2026-07-10，`research/flow_reentry_bt.py`，規則已凍結）：
      trail 掃出後 60min 內 cancel_skew 與 imbalance 都仍偏原方向 → 下根 bar 重進場。
      停損完整不動，只決定「要不要買回票」。Gate：n≥30 + CI 下緣>0 + 前後半同號，
      預計 **~2026-10**（backtest 節奏 ~8-9 trail 出場/月）。跑前需刷新 klines parquet
      （事件驅動：daily_collect.bat 只管 coinglass/ 子夾；root parquet 靠訓練/月度復驗刷）

### 4.65 次小時（分鐘級）系統可行性（2026-07-18 Phase 0 已判決：PARK）
使用者假說：1h 取樣把訂單流資訊平均掉，分鐘取樣可在衰退前捕捉 → 值得整套
搬到分鐘級。**Phase 0 依凍結 pre-registration 執行**（`research/subhourly/PREREG.md`
先 commit 固定時間戳 dd0558e → 才跑 `minute_ic_scan.py`，結果
`research/results/subhourly_ic_scan.csv`）：
- **G1 資訊存在：PASS ✅（假說的「資訊」半邊被證實）**——ohlcv_1m 15 個月
  （2025-01→2026-03，654k 分鐘）+ flow_bars 90 天兩時代交叉：taker 失衡/淨流 z/
  短期動量在 h=15-30m 月均 IC −0.03~−0.04、**15/15 個月同號**、前後半同號。
  IC×horizon 衰退曲線形狀完全符合假說：峰在 15-30m、到 4h 衰成 −0.013
  （= V7 在 4h 看不到它的原因）。註：**符號是負的**——分鐘級是「反著做」
  （fade taker flow / 短期均值回歸），不是跟單流；vshock 方向 IC≈0（與 F2
  「強度預測幅度不預測方向」一致）；obi 是唯一順向訊號（h=5m +0.034，30m 前死）
- **G2 經濟可行：FAIL ❌（假說的「可交易」半邊被否決）**——top-5% 條件桶
  在 30/60m **全部 138 格淨值為負**（扣 8bps=2×maker）；最好的一格
  （Era B ret_60@60m，且是純價格反轉非訂單流）毛利僅 ~6.2bps < 8bps 門檻，
  還有 138 格多重比較加持。IC 0.03-0.04 的單筆可轉化毛利就是付不起費率——
  市場把分鐘級可預測性磨到剛好低於成本前緣
- **處置（照凍結規則，不凹）**：G2 FAIL → 不跑 G3、**不重寫系統**、計畫
  PARK。復活條件：`depth_deltas_1m` 累積 ≥3-6 個月後帶撤單特徵 **re-run
  一次**（同門檻）；另 8/10 F1 判決是撤單側的獨立分鐘級檢定。
  V7 全程零改動（Phase 0 從頭到尾沒碰生產）
- **07-18 補 2：V7 換 15m bar 的重做成本已判**——direction 模型 importance
  拆解：**67.7% 的權重在 1h+/日頻原生源**（Coinglass OI/LS/margin 家族，
  top-10 佔 8 席；15m 網格上只是 ffill 階梯函數且無 15m 歷史可 backfill），
  fast 半邊（32.3%）正是 Phase 0 判弱的家族 → re-base = 重做全部工程去
  搬進更弱的資訊 regime，維持 NO-GO；「15m 輕量實驗」= G3，已被 G2 FAIL 封
- **07-18 補：re-run 必要贏面已量化**（`required_bar.py` + PREREG 附錄 A，
  家族在看資料前宣告）：撤單特徵須在 30-60m top-5% 桶交出 **≥8bps 毛捕捉
  ≈ 條件勝率 57.5%+**（最佳 taker cell 55.8%、真流系 cell 只有 53%）。
  兩個結構洞見進 re-run 分析義務：①流極端發生在安靜盤（條件 |move| 反而
  縮小）②低量分鐘就算勝率高也付不起 8bps → 撤單唯一過關路徑 =「安靜分鐘
  訊號預測隨後擴張波」（squeeze 論的分鐘級版本）。re-run 最早 **2026-10-09**；
  家族=撤單 def v1 聚合+簿型既有欄（wall/spread/imb）+taker 對照，僅此一次

### 4.6 V7 多幣化可行性研究（2026-07-15 啟動，2026-07-23 提前推進——第 5 次 informed override，見 CLAUDE.md）
動機：核心瓶頸是樣本速度（~265 Strong/年、73% 被單槽擠掉），多幣化 = 同機制
樣本產出 ×N，非找新 alpha。純 research track（`research/multicoin/`），不碰生產。
- [x] **Step 1 資料審計完成**（見 `research/multicoin/audit_results.md`）：
      ETH 特徵覆蓋 ~97%（只缺 coinbase premium 家族 3 個 + etf_aum）、SOL ~92%
      （再缺 DVOL 家族）。資料層不是瓶頸。陷阱：`/coinbase-premium-index` 無視
      symbol 參數（三幣回同值）→ 新增幣種端點必跑值差異化檢查（`verify_value_diff.py`）
- [x] **Step 2 ETH 移植實驗完成（2026-07-23，NO-GO）**——見
      `research/multicoin/step2_eth_results.md`：backfill ETH 4000 根 1h bars
      +12 端點（排除 symbol-ignoring 的 coinbase_premium）→ `build_live_features()`
      逐字重用建 136/136 特徵 → 與 BTC 0.5412 完全同一套 harness
      （`feature_search_ab._per_fold_oos(leaky=False)`，purge+embargo，77 folds）。
      **ETH clean pooled sign-AUC = 0.5057**（vs BTC 0.5412），且 first/second
      half IC 由 +0.048 翻負至 -0.035（halves 不同號，訊號不穩定，非邊緣案例）。
      資料品質已排除（正確 ETH 價格區間、僅預期內的 cb_premium 全 NaN、
      oi_coin_margin 75% NaN 但僅 4/136 特徵不足以解釋落差）。
- [x] **Step 3 訊號重合率——跳過（Step 2 已 FAIL，AND gate 不需再測）**
- **Go/No-Go 判定（2026-07-23）**：ETH clean AUC 0.5057 < 門檻 0.54 → **NO-GO**。
  V7 用「搬 BTC 136 特徵+未調超參數」這條路對 ETH 不成立；SOL 分支（同路線）
  不建議投入。若未來想再試，需要 ETH 專屬特徵工程/超參數（等於重做 Step 4-5
  模型開發，非移植）。資源回異源資料線（撤單流），該線**不受影響**——撤單流
  ETH 多幣化是獨立 track，已上線資料時鐘（見下方「撤單流多幣化」條目），照舊
  累積到 10 月 re-run。
- [x] **Follow-up：超參數重調 + 淺覆蓋率欄位剔除（2026-07-23，仍 NO-GO）**——
      使用者質疑是否因未針對 ETH 重調 BTC 超參數才失敗。跑 3 個有先驗理由的
      變體（非網格搜尋——避免重演 2026-06-20 threshold-sweep 錯誤）：加強
      正則化、剔除 4 個 75% NaN 的 oi_coin_margin 欄、兩者合併。用跟 BTC
      特徵 A/B 完全同一套 4-condition sanity gate 判定，**三個變體全部
      no significant lift**，絕對 AUC 全部在 0.5034~0.5073 之間打轉（跟
      baseline 0.5057 本質同一數字，離 0.54 門檻一樣遠）。結論：不是超參數
      移植不公平，是 136 特徵機制本身在 ETH 上不帶訊號。詳見
      `research/multicoin/step2_eth_results.md` Follow-up 段落，可重跑腳本
      `research/multicoin/eth_retune_ab.py`。
- [x] **Follow-up 2：特徵刪減篩選（2026-07-23，仍 NO-GO）**——用嚴格
      univariate IC(|IC|≥0.05)+跨fold同號一致性(≥0.65) 篩 136 特徵（門檻
      刻意比 BTC 新特徵 A/B 篩選嚴，因 136 次篩選本身是多重比較），只用
      WF test-fold bars 算避免篩選洩漏。**5/136 通過**，但其中 2 個是未
      差分的原始水位值（非平穩序列易假性相關）、1 個只有 21/77 fold 有
      資料（呼應 oi_coin_margin 覆蓋率缺口）。拿 5 個重訓：pooled AUC
      0.5057→0.5267 看似進步，但 4-condition gate 判 **no significant
      lift**（frac_pos=0.53 未過 0.55、bootstrap p=0.358 遠不顯著）——跟
      2026-06-02 WQ101「aggregate 好看、per-fold 撐不住」同一種陷阱。詳見
      `research/multicoin/step2_eth_results.md`，腳本
      `research/multicoin/eth_feature_elimination.py`。
- **2026-07-23 override 收尾**：第 5 次 informed override 授權提前跑 Step 2/3
  （不等 BTC Gate A），Go/No-Go 判準本身未變——三輪測試（原始移植+重調+特徵
  刪減 follow-up）全部指向同一結論，沒有留下「也許沒調好/沒篩對」的疑點。
  **V7 多幣化正式收尾**；若要繼續唯一剩路是從頭替 ETH 設計新特徵（等同重啟
  Step 4-5），非本次範疇。詳細記錄見 CLAUDE.md「V7 多幣化提前啟動」。

### 4.5 基建完善（2026-07-10 審視——防「新基建靜默失敗」族）
- [x] **depth_delta_collector freshness 監控**（2026-07-10 完成，a23d174）：APScheduler
      每 30 分查 `depth_deltas_1m` 最新分鐘，落後 >120min → TG 告警（停更+恢復各一次）
- [x] **depth_deltas_1m 每日 parquet 匯出**（2026-07-10 完成）：export_depth_deltas.py
      掛進 daily_collect.bat（注意 bat 被 gitignore＝本機檔，改動已生效於明日 04:00）。
      首跑備份 1837 行，收集器 31h 近零斷點
- [ ] Drawdown governor（先 alert-only）：回撤加深自動縮 size 的觸發邏輯，
      對症 M2M MDD -21% 破 Stage3→4a 門檻
- 📅 研究判決日曆：8/5 月度復驗（自動）→ **8/10 撤單 bar 級領先性**（cancel_lead_ic,
      n≥40k）→ **8月中 shadow maker 裁決**（live n≥30）→ **~10月 flow 重進場 gate**（n≥30）
- [x] **perp 撤單收集器部署**（2026-07-15 完成，168e8b2 pushed）：depth_delta_collector
      參數化 + start_all.py 平行起 binance_perp 實例（fstream，同表 exchange 區分），
      本機 smoke 通過（perp 撤單量 ~8x 現貨）。現貨流不動（凍結檢定的序列），
      五支分析腳本已加 exchange='binance' 過濾防污染。DB 已確認 binance_perp
      自 2026-07-15 09:06 UTC 起累積、近零斷點 → **perp 40k 判決點 ≈ 2026-08-12**
- [x] **淨偏斜基線/假象檢查（2026-07-17 登記，同日判決：B 真結構偏差，F1 無污染）**：
      `research/netskew_baseline_check.py`（spot 8d n=11,739 分鐘 / perp 2d n=3,031）：
      ①小時 tercile 分組綠佔比 漲 54.7% / 橫盤 53.3% / 跌 54.1%——**無梯度、橫盤也綠**
      → 非價格遷移假象；分鐘級 spearman(net_raw, ret_1m)=−0.075（fills-as-cancels
      機制若主導，上漲吃 ask 應推「正」相關，實測反向且極弱=非主導）；
      ②同分鐘對照：perp 幾乎中性（綠 52.0%、均值 +0.0007）vs 現貨綠 55.4%、
      均值 +0.0331（~47x）——**同一套收集碼跑 perp 得中性 = 收集邏輯無方向性偏差**，
      綠基線是「現貨簿本身」的結構性質（B）。兩個前提修正：
      (a) 收集器用 diff 全簿流（depth_delta_collector.py），無「可見窗」可遷移——
      原 A 假設的窗口機制不存在；(b) watcher skew15/net15 **本來就做 trailing-60m
      去均值**（cancel_playbook_watcher.py:161,166），觸發門檻非原始值——原④條
      「觸發率天生不對稱」疑慮不成立（偏差在 60m 尺度穩定即被吸收）。
      **F1 8/10 判決可照常進行**（spearman 對常數位移不敏感、無窗口假相關向量）。
      殘留 UX 債（可選、低優先）：覆盤圖淨偏斜面板畫 raw，長期綠 = 現貨簿基線
      而非訊號——可加 60m 去均值序列或基線參考線輔助判讀
- [~] **撤單狀態機判斷器（2026-07-17 登記，依賴上一條先完成〔07-17 已判 B 解鎖〕）**：
      **A1 核心已完成（07-17）**：`classify_state()` 進 `cancel_playbook_watcher.py`
      ——凍結 v1 分類器的 1:1 重標籤（零新門檻；15 測試含 fuzz 驗證與
      classify_minute 零漂移），六態 + gate_only 殘餘誠實顯示為「爆量未定」⚪；
      TG `/cancelstate [mins]` 上線（狀態行+特徵值+回看窗分布+持續時間，
      走 `/research/cancel-analyze?mode=state` 同管線）。
      **A2 完成（07-18）**：①互動覆盤圖價格 pane 底部 6% overlay 狀態色格
      （blank=平靜/灰=換防·爆量·瀑布/🟢🔴=真空·吸收，色值單一來源
      `state_color()` in watcher）②獵取標記層（tv_alert_events+liquidity_events
      → ⚡ marker+被掃價位黃虛線，空表安全）③sweep 特寫圖：poller 對帶
      liquidity_side 的快訊渲染 plotly+kaleido 回看窗 K 線+狀態格+價位線
      PNG 作卡片附圖（deps 缺→優雅退文字卡；marketdata image 兩者都有）。
      **A3 完成（07-19）**：①四鍵按鈕即日誌——watcher 劇本告警 + TV 事件卡都帶
      `[🟢同意][🔴相反][⏸不確定][✗略過]`（callback `ceb|src|id|verdict` →
      Service 1 webhook → `cancel_eyeball_log`，INSERT IGNORE **首判鎖定不可
      事後改**、skip 不落表、60/120m 判定窗自動回填=人機同尺）②sweep 二段式：
      第 1 段掃穿瞬間只報事實+特寫圖無按鈕；強度回落（3-15min，15 強制）後
      第 2 段卡帶 **H-R 凍結雙旗標**（被掃側淨回填+對側淨撤離）結論+四鍵。
      32 測試綠+e2e 煙測（二段流程/首判鎖定/真實旗標卡）。
      eyeball_log.md 手填流程可退役——按鈕版取代。↓ 原 spec：把 shock/毛/淨/
      量比/taker 融合成單一「當前狀態」輸出——**分類狀態機不是加權分數**
      （CDP 墓碑：合成分數稀釋信號 + 權重可調 = overfit）。六態：平靜/換防警戒
      （毛高淨零）/瀑布中（強度尖峰）/向上真空/向下真空/吸收（量大守住）。
      門檻全部沿用 def v1 已凍結值，不新調參數。顯示三出口共用同一狀態函數：
      TG /cancelstate 一行 + 告警照舊 + **狀態直接畫在 K 線面板**（2026-07-17 使用者
      定版，取代獨立緞帶）——每根 K 腳下貼狀態色格，同座標系疊「流動性獵取標記」：
      讀 `liquidity_events`（read-only 跨表）畫被掃價位虛線 + 掃穿 K 標 ⚡ + 當分鐘
      狀態註記 → 「獵取當下撤單長什麼樣」一眼可讀。
      加碼（零件全現成，純組裝）：sweep 事件觸發時自動渲染 ±90min 放大圖
      （K線+狀態色+被掃價位線）推 TG——獵取事件的撤單特寫自動送達。
      判讀卡從三步簡化為「看 K 線腳下的顏色」
      **UX 定案（2026-07-17 使用者拍板）——「事件卡 + 按鈕即日誌」**：
      - 卡片格式：結論在標題（機器判讀+推理一行）→ 特寫圖佐證 → 四鍵
        `[🟢同意] [🔴相反] [⏸不確定] [✗略過不記]`——前三鍵判讀並自動落表
        （半自動化 eyeball log：人按的仍是人的判讀，落表動作自動化=強制當下記
        +天然前瞻+不可事後改，比手填 md 更符合凍結規則），略過鍵不佔樣本。
        inline button 複用 manual approval 基礎設施
      - **事件源 ×3**：watcher 劇本 / 主 bot sweep 偵測 / **TV 快訊 webhook**
        （使用者在 TV 畫關卡設快訊 → 主 bot 現成 webhook 端點收 → 只寫
        `tv_alert_events` 表（DB 當匯流排，share data not code，兩服務不互 import）
        → Service 2 輪詢 → 算狀態+渲染卡）。TV 源注入機器看不見的大級別位置感
      - **判讀窗明寫在卡上（時間區段是關鍵）**：回看窗（觸發前 90min 狀態序列，
        TV message JSON 可帶 {"window": N} 自訂）+ 判定窗（def v1 的 2h，到期
        自動回填）。TV 觸發事件與 watcher 事件共用同一回填管線 → 未來可直接
        統計「人圈的位置 vs 機器抓的位置」命中率差（= 人的位置感 alpha 檢定）
      - 顏色語言全系統唯一一套：🟢向上真空 / 🔴向下真空 / ⚪換防或瀑布（不判方向）
      - **sweep 事件卡改二段式（2026-07-17 使用者釐清：其 setup = 獵取後的假突破/
        假跌破反轉）**：掃穿瞬間 = 資訊黑洞（撤單全是保護性雜訊），第一段卡只報
        「⚡掃穿+瀑布中,判讀待塵埃落定」；強度回落後（3-15min）自動觸發第二段卡
        給結論（反轉條件成立/未現）+ 四鍵——使用者的決策時刻在第二段
- [x] **MCP agent 遠端化（2026-07-17 登記；07-18 全部完成並上線）**：
      `indicator/agent/http_server.py` streamable-HTTP + AGENT_MCP_TOKEN
      路徑認證（fail-closed：無 token 拒啟動、錯路徑 404、421 rebinding
      修正 708a7c2）。Railway `agent-mcp` service 用 CLI 全自動建立
      （Dockerfile.agent image、repo 綁定 main 自動部署、MYSQL 內部連線）：
      `https://agent-mcp-production-46d7.up.railway.app/<token>/mcp`
      公網 initialize 200 實測通過。**剩使用者一步**：claude.ai →
      Settings → Connectors → Add custom connector 貼上完整 URL。
      Railway CLI 已裝並授權（rfobelieve1@gmail.com）＝未來 Railway
      操作可全代管。原 spec：現況 stdio 版只有本機 Claude Desktop 能用，雲端
      Claude Code / 手機 app 都摸不到。改法：`indicator/agent/server.py` 的
      FastMCP 換 HTTP transport（framework 原生支援）+ token 認證（比照
      ADMIN_HEAL_TOKEN 模式，fail-closed）+ 掛 Railway（可與指標 service 同容器
      不同 port，或獨立 service）→ claude.ai Connectors 加入 → 所有 Claude
      介面可直接呼叫 cancel-flow 分析工具（fc44106 的 MCP 工具已存在，
      只差 transport）。邊界不變：agent-boundary.md 全部照舊（read-only、
      不碰 executor、AST 測試守著）。副作用：MCP agent 從 demo 變成
      「作者本人日用的生產工具」——面試/履歷素材直接升級。
      過渡期替代：TG `/cancelanalyze 90` 輸出貼給 Claude 人工判讀
- [ ] **H-R 獵取反轉濾網（2026-07-17 登記，使用者主 setup 的專屬檢定）**：
      ⚠️ **07-17 樣本審計：`liquidity_events` 與 `sweep_outcomes` 全表 0 筆**
      （事件管線從未產出資料——TV 快訊 → `/tv` webhook 這條源頭目前是死的）。
      H-R 不是「寫好 harness 等資料」而是**先復活事件源才會開始累積**；
      復活 = A3 的 TV webhook 事件源（使用者在 TV 畫關卡設快訊）——
      這使 A3 的 TV 源部分從「UX 加值」升級為「H-R 的硬前置」。
      ✅ **07-17 接收端已上線**：`/tv` 加寫 `tv_alert_events`（BTC+secret 驗證後、
      liquidity_side gate 前——純關卡快訊無 side 也收；`{"window":N}` 自訂回看）
      + Service 2 `tv_alert_poller`（觸發分鐘收盤後算狀態→推簡版事件卡→
      30/60/120m 判定窗回填，與機器事件同管線=人機命中率可比）。
      **剩使用者動作：在 TV 畫關卡設快訊指到 /tv**（帶 secret/price/event；
      含 liquidity_side buy/sell 的同時餵舊 liquidity_events 管線=H-R 時鐘）。
      原凍結假說↓
      基率警告：掃穿延續 ~2/3 → **盲目接反轉 = -EV**，edge 全在濾網。
      凍結假說：sweep 事件塵埃落定段（強度尖峰回落後 15min 內）雙旗標
      **(a) 被掃側淨回填**（cancel−add 轉負）**+ (b) 對側淨撤離**（偏斜翻轉到
      逆掃穿方向）同時成立的子集 → 60-120min 反轉率（收回被掃關卡）顯著高於
      無旗標 sweep 基準（~1/3）。兩個 categorical 旗標、不調參；
      事件源 `liquidity_events`/`sweep_outcomes` × `depth_deltas_1m` join；
      紀律鎖照舊：cell n<100 不下結論、前後半同向、看資料後禁改旗標定義。
      此檢定 = 事件卡第二段「反轉條件成立/未現」判讀的統計地基
- [ ] **F1b 條件版領先性（2026-07-17 登記，F1 過關後的第一個追問；
      2026-07-20 分析腳本補上）**：`research/cancel_lead_ic_tercile.py` 已寫
      （按已凍結 vshock 定義三分位分層跑 cancel_lead_ic，不新造任何門檻）。
      SMOKE 結果（n=15,823，遠低於 40,000 判決線，**不算數**）：三桶都沒有
      CI 乾淨脫離 0 的格子；low-vol h=30 IC+0.104 halves 同號但 CI 含 0；
      mid-vol 反而全部同號偏負（h=5~60 都約 -0.05~-0.12，halves 全部同號，
      唯獨 CI 都含 0）——**跟預先登記的「low-vol 有訊號、high-vol 趨零」
      形狀不符**，mid-vol 意外地方向最一致。n 太小，任何桶的形狀都可能是
      雜訊；不得以此調整假說或提前判決，等 40,000 分鐘（spot 現 15,995，
      ~8/10 前後）才是真正的判決點。動工緣由：使用者 07-20 指出「撤單流
      要有自己獨立於獵取事件的訊號判斷」——這正是 F1b 要回答的問題（v1
      六態全部需要 shock 或 skew/net 門檻，結構上排除了 F1b 假說要抓的
      安靜盤微弱漂移，見今早 07:44-07:57 案例：net15 峰值 0.178 未達真空
      門檻 0.30，零事件記錄）。若 F1b 通過，def v2 才有資格納入無鬧鐘門檻
      的「安靜盤傾向」狀態；在那之前，任何「撤單流自己判斷方向」的即時
      輸出都不存在、也不該存在。
      核心假說「撤單流的資訊價值與成交活動成反比」——市場兩條資訊通道，
      成交火熱時價格由吃單驅動（撤單=保護性雜訊+冗餘，例：07-17 三則真破告警
      量 30x/taker −50% 而淨偏斜≈0）；成交冷清時報價通道是唯一在動的東西
      （做市商挪動=先行洩露）。**預先登記的預測：撤單 IC 在低量分鐘顯著、
      高量分鐘趨近零**（按成交量 tercile 分層跑 cancel_lead_ic）。
      推論的應用排序（機制推理，非驗證結論）：①壓縮期方向洩露（策略 #2 主場，
      成交越安靜撤單越大聲）②獵取的逼近段+收尾段（避開掃穿瞬間——那一刻
      資訊權在量價，撤單退場）③瀑布當下禁用。若 F1b 成立，判斷器的狀態
      權重應在低量時段調高撤單維度、高量時段讓位給量價（分工寫進 def v2）
- [x] **事件卡按鈕改行動鍵 + 自動對答案（2026-07-20 完成）**：四鍵判讀退役
      ——使用者點破統計無效（卡片先給機器結論=錨定污染、樣本慢、選擇偏誤，
      量到的是服從率非 alpha）。改成：①按鈕=給工具 `[📊特寫圖][🔍90m深入]
      [⭐收藏][✗忽略]`（callback `cfa|src|id|action`，ceb| 處理器留給歷史卡）
      ②判定窗回填完成自動 reply 原卡「對答案」（60/120m 走勢+命中+劇本近
      30 筆滾動命中率；tv 事件報走勢）——告警流變自評分 feed，8/10 前就對
      「哪個劇本有料」長體感。「人的位置感」檢定不受影響：靠 TV 關卡被動
      回填（無按鈕、無錨定），照舊累積。star 落 cancel_eyeball_log
      verdict='star'（收藏語義，回填照舊，供未來覆盤包）
- [x] **撤單流指令全遷新 bot（2026-07-19 登記，同日完成 f85400e）**：
      新 bot /start 出選單（覆盤圖/狀態/摘要按鈕）+ 三指令帶參數，回覆全走
      CANCEL_API；主 bot 移除三指令/help 文字/按鈕/callback。頻道分流非
      系統分家：handler 共用、edge 證明後撤單流仍進 V7 判斷層（overlay）
- [x] **撤單流多幣化 ETH 資料時鐘啟動（2026-07-19 登記，2026-07-23 上線 1b2e2ad）**：
      與 V7 多幣化（4.6）同方向，第 5 次 informed override 的一部分。
      `depth_delta_collector.py` 的 `canonical_symbol` 從硬編碼模組常數改成
      建構子參數（BTC 預設值不變），`start_all.py` 新增 ETH spot/perp 兩條
      平行 daemon thread。Railway market_data 已重部署，live 驗證：
      `exchange=binance/canonical_symbol=ETH-USD` 與
      `exchange=binance_perp/canonical_symbol=ETH-USD` 各自開始累積、
      與 BTC 序列（20,409 spot / 11,556 perp 行）同分鐘同步、零污染。
      紀律：BTC 凍結序列不動，ETH 為新增平行序列——**已達成**。
      後續：watcher/圖表跟進非急迫（先讓資料鐘跑，10 月 re-run 前有 2.5 個月
      累積）；ETH 版 F1/F1b 判準與門檻另訂，不沿用 BTC 的 n≥40,000 時程。
- [x] **first-hit-wins 平行診斷（2026-07-21，FROZEN v1）**：使用者質疑
      固定時間點(60m/120m)判對錯會混淆「方向對但慢」跟「真的錯」→
      `first_hit_verdict()`：逐分鐘掃，先碰目標價(±0.5%沿用
      outcome_tracker.py既有門檻)算對、先碰反向價算錯，120m窗沿用既有
      窗口。與固定時間點指標並行顯示，不取代(F1/F1b判決仍用固定時間點，
      因為那才是防p-hacking的量法)。回填放寬 SELECT 讓舊事件自動追平。
      **實測結果推翻直覺**：21:47-21:53 那組 120m 翻正 +1.0~1.5% 的案例，
      用 first-hit-wins 重查後**除 21:53 外全部仍是 miss**——price 在反彈
      之前先碰到 -0.5% 反向門檻，若真設停損會先出場；120m 翻正是巧合
      時間點快照，不是訊號提早判對只是被切錯位置。58 測試綠。
- [x] **色格短窗誠實檢查（2026-07-20，探索性非家族）**：使用者主觀回報
      「看綠後面漲、看紅後面跌，感覺有利可圖」→ `research/color_state_forward_check.py`
      用既有凍結 `classify_state` 零新定義跑全歷史（n=15,979 分鐘）。結果與
      直覺**相反**：綠 h=1/3m 平均 **-6~-7bps**（方向錯）、紅 h=1~60m 全部
      方向錯或低於 8bps 成本線；沒有一格通過。且「綠」100% 是吸收態
      （vacuum 從未觸發過一次）。n=32/54 太小 + 12 組無多重比較校正，
      **不當結論用，不翻轉色格邏輯**——避免重演 2026-06-20 FOMO 的鏡像版
      （這次是「發現乾淨反訊號」而非「發現漂亮正訊號」，陷阱同大）。
      跟 subhourly Phase 0 G2 同一個死法：資訊或許存在，幅度到不了成本線。
- 撤單檢定家族現況（皆凍結）：F1 skew 水位（07-10 註冊）8/10 判決；
  F2 變化幅度 shock（07-15 註冊，smoke: intensity→|ret| 5m IC +0.113 CI 全正，待 powered）；
  F3 深帶翻轉事件（07-15 註冊，6 天 0 事件，需 ≥30/方向）；
  主觀判讀日誌 research/results/eyeball_log.md（30 筆判決，前瞻記錄才算）
- 凍結假說（2026-07-11 註冊，看 depth 資料前寫定；皆隨撤單檢驗點跑）：
  - **P-cascade**：撤單強度（total_cancel/total_add）top-decile 分鐘 → 之後 15min 內
    出現巨量（vol top-1%）機率顯著高於基準（CI 離 0 + 前後半同號）。
    背景：巨量分鐘 92% 含清算 / 重清算濃縮 35x = 被迫流實錘；但事後 fade 無 edge
    （60min 延續率 51/48% = 硬幣）→ 錢只可能在瀑布**之前**的撤單裡
  - **P-wall-pull**：逼近 24h 極值（±10bps）事件中，逼近前 15min 該側撤單不對稱
    （tercile 分組）預測 60min 內掃穿。背景：靜態牆厚已測死（薄/中/厚 = 65/66/62%,
    z=0.45）——可撤回的宣告無資訊、撤離動作才有；無條件掃穿基率 ~2/3
    （「止損區是磁鐵」成立但人人皆知，非 edge）

### 5. 內容線
- [ ] EP2 英文版發 LinkedIn；EP3 細修後發（Medium 英文版已備）
- [x] **EP3「你的回測正在騙你」中英文皆完成（2026-07-20）**：原稿太術語
      密（IC/fold/bootstrap/aggregate 堆疊），使用者反饋改成一般讀者能懂
      的白話 + 比喻版（老師發答案卷／量錯尺／班級平均被學霸拉高），砍掉
      4 條門檻條列式清單、拿掉結尾自介。三個陷阱骨架與 mistake 2026-04-13、
      2026-06-02 素材不變。中文
      `Desktop\linkedin_posts\quant_ep3_backtest-lying_v3_headings.docx`
      （帶分段粗體小標題），英文改成 Medium 格式（去掉 LinkedIn 專用的
      「Copy-paste ready post」框、hashtag 換成 Medium 站方標籤上限 5 個、
      加 Cover image 提示）：
      `Desktop\linkedin_posts\quant_ep3_backtest-lying_medium.docx`。
      下一步：使用者最終審閱後可發布
- [ ] EP12 素材「AI 當槓桿不當許願池」已入 roadmap（docs/linkedin_ep_series_roadmap.md）

### 6. ★ 徹底掌握自己的系統（面試就緒度 — 求職關鍵）
「系統會跑但講不清 why」是最大面試風險（很多是 AI 寫的）。面試官不問「還能做什麼」，
問「這個為什麼這樣做」。詳細手冊：docs/system_mastery.md（+ Claude 生成的
rfobot_系統精通手冊.docx 含完整標準答案）。逐層攻破，每層要能不看筆記回答灵魂拷問：
- [ ] 層 1 數據層（notional 換算 / 1e12 時間戳 / share-data-not-code）
- [ ] 層 2 特徵層（look-ahead 防範 / 加特徵流程 / 為何不用 MACD）
- [ ] 層 3 模型層（雙 regressor / walk-forward purge+embargo / IC 0.063 / in-sample 陷阱）
- [ ] 層 4 訊號層（rolling percentile / Strong-only / Gate A 算法）
- [ ] 層 5 執行層（5 種 reconcile / 狀態機四態 / 3 個 kill trigger / fail-closed / 不用 CCXT）
- [ ] 層 6 風控數學（2.0x 怎麼算 / 何時放寬 / hit kill 降階重驗）
- [ ] mistake.md 六個失敗故事能講成 30 秒
- [ ] 回 Claude 做面試官壓力測試（跨層追問）
自測進度 2026-07-07：層 1 Q1 60分（漏 notional）、Q2/Q3 知識債 → 已補入手冊

### 7. 產品化網站——3D/互動風格參考（2026-07-21 登記，同日補正）
使用者想把系統未來做成產品網站。原先參考的 IG reel 靜態截圖只看到模糊
漸層，經使用者上傳實機螢幕錄影（jerrythewebdev,「Websites after leaving
ChatGPT for Claude」）確認真正的視覺語言，跟第一版描述（互動漸層）差
很多，以錄影為準：
- **深色電影感底**，極簡放大無襯線標題字
- **玻璃質感 3D 浮動面板** + 全息/虹彩材質的有機造型 3D 物件飄浮在暗色空間
- 破碎鏡面/玻璃碎片拼接面板，文字嵌在碎片裡，搭配潑彩煙霧色塊（藍紫粉）
  點綴
- 捲動觸發場景轉場（技術棧常見 React + Tailwind + Three.js + GSAP +
  WebGL），配 hashtag #opus #fable5 #mythos（用 Claude 系列模型輔助製作）
- [ ] 定位：視覺風格參考（深色、玻璃質感 3D、捲動轉場），不是逐項規格；
      跟 `project_productization_goal`（長期商業化目標，現階段先顧穩定+
      訊號品質，不做過早的 multi-tenant/auth/billing 抽象）放一起看
- [ ] 待評估：這個風格值不值得投入（Three.js/WebGL 對求職 demo 或未來
      產品頁的 ROI），還是先用更輕量的方案（如既有 dashboard/Artifact）
      驗證內容本身，之後才考慮視覺升級
- [x] product-site 已上線精修：自架 email/password 登入（取代 Google OAuth，
      `agent_user_accounts` 表 PBKDF2 雜湊）+ 3D K 線改玻璃質感
      （RoundedBoxGeometry + meshPhysicalMaterial transmission/clearcoat/
      iridescence，桌面/手機分級）。修過一個坑：`<Environment preset="night">`
      抓遠端 CDN HDR，正式站首載卡 10+ 秒背景全空白——改用場景內
      Lightformer 產生反光貼圖，零網路依賴（commit 7c67273）
- [x] **全站 2D 卡片跟上玻璃質感（2026-07-22 完成，commit 9ecda70）**：
      Architecture/TrackRecord/EngineeringLog/Explore/LiveSignal/signals
      頁的卡片原本是扁的霧面 blur 盒子，加共用 `.glass-panel` CSS
      utility（漸層邊緣反光 + 角落柔光 + cyan/violet 環境光暈）疊上去；
      FAQ 原本是純底線分隔手風琴、完全沒玻璃處理，改成同款玻璃卡片。
      過程踩到一個雷：改完 CSS 在瀏覽器裡看不到效果，一度以為程式碼沒生效，
      實際是 dev server 重啟後瀏覽器仍吃舊 JS bundle（非 `.next` cache 問題，
      硬性重新整理即解）——下次「明明改了看起來沒變」先試 hard reload
      再懷疑程式碼
- [x] **中文切換上線（2026-07-22 完成，commit 4e475a7）**：next-intl 路由
      分語言，`/zh` 前綴、英文維持原本無前綴網址不變（localePrefix:
      as-needed，保住既有連結/SEO）。middleware 自動偵測瀏覽器語言；
      全站 app/ 路由搬進 app/[locale]/...；messages/en.json +
      messages/zh.json 兩份完整字典（177 key 逐一核對雙語對齊）；Nav
      右上角加 EN/中切換鈕，切換時停在同一頁換語不跳轉首頁。本機
      production build 驗證：兩語言全頁 SSG 成功、語言偵測+同頁切換+
      真實 API 資料頁（Track Record）+ 表單頁（Register）全過，正式站
      curl 7 條路由全 200。已知限制：Track Record/Signals 頁的
      note/caveat/disclaimer 是後端 agent API 直接回傳的英文，不是靜態
      UI 字串，這次沒雙語化
- [x] **UI 組件 MCP 授權完成（2026-07-21 登記 → 2026-07-22 確認已授權）**：
      `21st-dev`（https://21st.dev/mcp）與 `originkit`
      （https://mcp.originkit.dev/mcp）OAuth 登入已完成，兩邊工具
      （`mcp__21st-dev__*` / `mcp__originkit__*`）已出現在可用工具列表。
- [ ] 下一步：搜尋兩邊組件庫有沒有跟現有深色/玻璃美術風格搭的現成
      React/Tailwind 組件（表單、卡片、按鈕）可替換掉目前手刻的
      LoginForm/RegisterForm 等，非必然有收穫（要先看實際組件品質跟
      風格是否搭），此項只是「有了這兩個工具，值不值得換」的探索，
      不是既定要做的重構
- [x] **首頁螺旋入場動畫 + K 線比例修正（2026-07-22 完成，commit
      468629e/42efcbf）**：用 21st.dev 收藏的社群組件（xubohuah/
      spiral-animation）移植成 `components/intro/`，session 一次性
      （sessionStorage）。移植時修掉原始碼三個問題：星星陣列建兩次、
      正方形畫布硬拉伸進非正方形視窗、每幀算的星星大小只餵給從未被
      讀取的 lineWidth。K 線蕊寬度 0.26→0.17、圓角 0.16→0.06，影線
      圓角 0.4→0.06（原本貼著 RoundedBoxGeometry 0.5 上限被撐成
      膠囊）——使用者原本反應「像塑膠積木」，改窄改利落後待驗收。
- [x] **product-site 首次連 GitHub + 上線 Vercel（2026-07-22）**：
      repo `github.com/rfobelieve-crypto/product-site`（公開）。
      過程踩到一個雷：這個 Vercel project 掛在 team scope
      （`rfobelieve-cryptos-projects`），team 帳號**沒有個人 scope
      可切換**（`personal_scope_not_allowed`）；team 又是免費 Hobby
      方案，Hobby **不支援私有 repo 的協作部署**（commit author 對
      repo 沒 contributing access 就整個 block，跟登入哪個帳號無關）。
      連 GitHub 前若已知 Vercel project 是 team scope，應先查方案
      等級再決定 repo 公開/私有，省掉三輪盲猜重推——已改公開解決，
      正式站 `https://product-site-green.vercel.app`。
- [x] **系統靜態圖 + 撤單流指標圖上網站 Phase 1 完成（2026-07-22，
      使用者定調「這才是我系統的精髓」）**：product-site 新開獨立
      `/charts` 頁（使用者選定，不塞進 system 頁也不進首頁）。架構走
      **純代理，agent-mcp 不重新渲染**——`indicator/agent/server.py`
      新增 `/public/chart`（V7）+ `/public/cancel-flow-chart`（撤單流）
      兩個唯讀 route，直接轉發 `indicator` 服務已經算好的 PNG bytes
      （V7 5 分鐘快取、撤單流 2 分鐘快取——撤單流原始 route 是每次
      重跑 90 秒 subprocess，這層快取是防止公開端點變成任何人都能
      觸發重運算的節流閥，不只是效能優化）。完全不碰
      `agent-boundary.md` 白名單，`tests/test_agent_boundary.py` 5 過
      1 skip（HTTP 轉發不是 Python import）。V7 靜態圖原本白底
      （`chart_renderer.py` `facecolor="white"`）跟站上暗色玻璃風格
      衝突，加 `dark=` 參數（新配色貼齊撤單流圖表既有的 `#0e1116`），
      新增 `indicator/app.py` `/chart-dark` route，原本白底呼叫路徑
      （Telegram 推送）完全不動。過程中靠獨立單元測試抓到一個真的
      會炸的 bug：`token` 參數宣告 keyword-only，但
      `anyio.to_thread.run_sync` 只轉發位置參數不轉 kwargs——先測試
      再上線才抓到，5 個案例全過（成功抓取/token 轉發/TTL 快取命中/
      來源失敗回 503/來源失敗吐舊快取）。剩一個手動步驟：Railway
      `agent-mcp` 服務要設 `INDICATOR_ADMIN_TOKEN`（=`indicator` 服務
      現有的 `ADMIN_HEAL_TOKEN`），撤單流圖表才會通。Phase 2（互動圖表
      版）留待之後，範圍明顯更大（iframe 或整個用 React 重刻）



---

## 0.49 地形濾網「更快取證」（2026-08-01）——不降門檻的加速

**問題**：凍結扳機要 +60 筆新 Strong，現行速率（12-28/月）≈ 3 個月。

**先講一個算過的事實**：+60 筆按目前 kept:veto ≈ 37:18 的比例會分成
約 40/20，gap 的 95% CI 半寬 = **±26pp**（+100 筆也還有 ±21pp）。
也就是說**扳機本來就不是統計檢定**，它是「有沒有繼續 work」的持續性
檢查。真正的統計力不在那 60 筆，而在大母體。

**已跑（`research/terrain_fast_evidence.py`，事前註冊：同門檻、不調參、
四維全報）**：

- **測試 1 — Moderate tier（n=1356，CI 半寬 ±5.7pp）**：四維**全部同向、
  兩半穩定**。D1 −6（H1 −6/H2 −9）、**D2 −11**（比 Strong 的 −5 更強）、
  D3 +7、D5 +8（+12/+6）。地形是「價格位置」的性質，不是 Strong 專屬
  ——這是目前**統計力最強**的一份證據，且母體與推導母體不同
- **測試 2 — clean WF OOS（n=262）**：**低統計力、結果混雜**。D2 −2、
  D3 +10 同向；**D1 +7 翻轉**（追突破桶只有 n=36，兩半 +32/−3）、
  **D5 −1 翻轉**（密桶 n=47，兩半 +20/−20）。兩半劇烈擺盪＝雜訊特徵，
  但**照事前註冊如實記錄為翻轉**，不解釋掉。D1/D5 需要 forward 才能了結

**建議的加速做法（尚未執行，等使用者決定）**：
1. **不動凍結的 Strong 扳機**（它繼續跑）
2. **另註冊一條平行的 Moderate forward 時鐘**：同樣 +60 筆、同樣 gap
   ≥8pp、同樣四維定義。Moderate 每月 67-77 筆 → **約 3-4 週填滿**，
   而且 base WR 較低（53%）反而更容易看出濾網效果
3. 兩條時鐘任一填滿即可上桌討論；**Moderate 是間接證據**（executor 只
   交易 Strong），要在決策時如實標註這一點

**紀律註記**：這不是把門檻放低——樣本數（60）與 gap 門檻（8pp）都沒動，
換的是「哪個母體」，而那個母體剛剛獨立證實了四維全部成立。



---

## 0.492 策略 #2 出場研究（2026-08-01）——兩個倖存者，一個被儀器殺掉的假贏家

**方法**：進場規則完全不動，只換出場，所以是**配對比較**（同一批 fill，
去掉進場運氣、統計力大增）。變體全部是**分類式機制**、不是掃參數
（避免 mistake.md 2026-06-20 的 threshold-sweep 陷阱）。三關：
G1 池 dR>0 且兩半同向｜G2 ≥6/9 幣同向｜G3 配對 bootstrap CI 排除 0
且符號翻轉置換 p<0.05。母體：9 幣、約 11,400 筆進場。

### 結果（三次儀器修正之後）
| 變體 | 配對 dR | 兩半 | 幣別 | p | 判決 |
|---|---|---|---|---|---|
| hold_4（持有上限 8→4 根） | **+0.0091** | +.009/+.009 | 7/9 | 0.012 | **PASS** |
| fail_fast（收盤跌回價位另一側即走） | **+0.0097** | +.013/+.007 | 8/9 | 0.006 | **PASS** |
| trail_2atr | +0.0003 | −.001/+.001 | 6/9 | 0.818 | FAIL |
| trail_1atr | −0.0017 | — | 4/9 | 0.246 | FAIL |
| half_at_1r（+1R 出一半） | −0.0005 | — | 4/9 | 0.561 | FAIL |
| hold_12 | −0.0021 | — | 4/9 | 0.525 | FAIL |
| giveback（回吐 50% MFE 出場） | −0.0006 | — | 4/9 | 0.669 | FAIL |

### 三次儀器修正（過程本身就是結論）
1. **棒內順序偷看**：用同一根 K 的高點算出場價位、又用同一根的低點判定
   觸發＝假設「先到高點才回落」。改成用**前一根為止的峰值**算價位
2. **成交價現實化（決定性的一刀）**：價位型出場原本假設「觸價即以該價
   成交」。改成**跳空穿越時以開盤價成交**（baseline 的災難停損一併適用，
   對變體反而寬鬆）。**giveback 從 +0.047 → −0.0006、trailing 全歸零**
3. 效能：`run_exit` 每次重建 4 個上萬元素陣列（呼叫 83 萬次）→ 快取欄位，
   10 分鐘 → 6.8 秒

**這條研究最重要的產物不是那兩個 PASS，是第 2 點**：整個 trailing／回吐
家族的「優勢」100% 來自「回撤出場能剛好在觸發價成交」這個假設。**任何
價位型出場的回測，都必須先把跳空穿越的成交價改成開盤價再看數字。**
（與 V7 那次「11 個出場變體全輸 3xATR」相互印證。）

### 倖存者的共同結構（值得注意）
hold_4 與 fail_fast **都不是價位型出場**——它們在**K 棒收盤**執行，
所以完全不受成交假設影響。這不是巧合：能在這份資料上活下來的，
就是不依賴「觸價即成交」的規則。

### 但**現在不換**，理由三個
1. **Gate F 正在用現行出場累積 forward 樣本**（變體 B 161/1400），中途
   換出場＝污染整條 forward 證據鏈
2. 效果量小（+0.01R，baseline 均 +0.04R 級別），且 hold_4 與 fail_fast
   高度重疊（都在縮短持有），**尚未測兩者是不是同一個效果**
3. 任何替換都要走**自己的 forward 註冊**，不能拿歷史配對結果直接上線

### hold_4 vs fail_fast 是不是同一個效果？（2026-08-01 已測）
`research/sweep_failure/exit_overlap.py`：
- **機械重疊只有 6%**（兩者在同一根出場的比例，n=11,374）
- **統計重疊中等**：逐幣 dR 相關係數中位 **+0.58**（0.54~0.62）
- **殘餘增量（決定性的一項）**：
  · fail_fast 加在 hold_4 之上 **+0.0048R，CI [+0.0002,+0.0091]，p=0.032 → 顯著**
  · hold_4 加在 fail_fast 之上 **+0.0043R，CI [−0.0002,+0.0088]，p=0.061 → 不顯著**
- 合併變體 vs baseline +0.0140R（p=0.002）

**判決：一個效果為主（fail_fast），hold_4 只在特定情況補刀。**
所以 **合併的 +0.0140R 不可以當成「兩個 edge 疊加」來報**——它有一半是
同一個機制被算兩次。要註冊就註冊 **fail_fast 單獨**那個。

### 下一步（未執行，待決定）
- 若確定要換：**只註冊 fail_fast**，寫成新的凍結註冊、與現行出場**平行**
  記帳，用 forward 樣本決勝，不在 Gate F 累積期中途切換

---

## 0.491 【凍結註冊】Moderate 平行時鐘（2026-08-02 起算）

**這是一份凍結註冊，改動＝重新註冊、換新日期、樣本從零重數。**

**動機**：Strong 扳機 +60 筆在現行速率下需 ~3 個月，而且 60 筆的 gap
95% CI 半寬約 ±26pp（見 §0.49）——等三個月換不到統計力。Moderate 每月
67-77 筆、90 天窗口目前已有 n=208（Strong 只有 55）。

**已具備的前置證據**（§0.49 測試 1）：四維在 Moderate（n=1356，CI 半寬
±5.7pp）**全部同向、兩半穩定**，D2 甚至比 Strong 更強（−11 vs −5）。

### 凍結內容
- **母體**：`tracked_signals.strength='Moderate'`、`correct IS NOT NULL`
- **起算**：`signal_time >= 2026-08-02`（**與 Strong 同一瞬間起算**，
  兩條時鐘的 forward 窗口完全可比）
- **判準**：與 Strong 完全相同 → **+60 筆新訊號** **且** 90 天
  kept-vs-vetoed **gap ≥ 8pp**
- **維度定義**：一字不改沿用 D1 追突破 veto（look=4h）
- **實作**：`research/v7_raid_veto.py --clock` 同時輸出兩條，寫進
  `v7_veto_clock.json` 的 `clocks.{strong,moderate}`；週報兩行都印
- **註冊當下基準**：Moderate 90d kept 52.5%(n=139) / veto 43.5%(n=69)
  → gap **+9.0pp**；Strong 62.2%(37)/44.4%(18) → **+17.7pp**；兩者
  since_trigger 皆 0

### 講清楚它的極限（決策時必須一起念）
1. **這是間接證據**——executor 只交易 Strong。Moderate 過關證明的是
   「地形機制在這個模型上普遍成立」，不是「Strong 上的效果量是多少」
2. **不可用 Moderate 的 gap 數字去代表 Strong 的效果量**（base WR 差
   7pp，效果量本來就不同層級）
3. **兩條時鐘都不能單獨授權進 executor**——它們只是把「該不該上桌討論」
   這件事提早，最終仍要操作者選檔位（T0-T3）
4. 任一條時鐘若 gap 掉到 8pp 以下 → **該條時鐘作廢重數**，不是取平均、
   不是挑好看的那條講

### 判決（2026-08-30 補記；時鐘 08-26 已到期，當時漏記）

**作廢。** Moderate 時鐘 08-26 週報過 60 筆（61/60）時 gap **+0.5pp**，
08-30 為 67/60、gap **+2.2pp**（kept 46.3% n=136 / veto 44.2% n=77）——
遠低於 8pp 門檻，依上面第 4 條作廢。軌跡：08-01 +8.7 → 08-03 +7.9 →
08-08 +1.5 → 08-10 +0.1 → 08-17 −0.6 → 08-24 +0.6 → 08-26 +0.5 → 08-30 +2.2。
**註冊當下 +9.0pp 的優勢在第一週就消失，之後兩個月都貼零**——這是「forward
沒有複製推導期」的乾淨形狀。

讀法（依「極限」第 1、2 條）：這是**間接**證據，不能反推 Strong 的效果量；
但它與 Strong 時鐘同期的軌跡同向（Strong gap 08-08 +23.0 → 08-30 +3.0，
53/60），兩條獨立母體一起往零走。Strong 扳機照 §0.483b 凍結表在 60 筆
結算，**中途不動**（使用者 2026-08-30 確認）。不重數——重數的前提是
「還相信機制、只是這批運氣差」，兩條同時歸零時這個前提不成立；地形層
維持 display-only。

---

## 0.5 個人語音助理（JARVIS-like）— **調研完成，尚未動工**（2026-08-01 入列）

**狀態：待辦，零程式碼、未建 repo。** 使用者的決定是「先列入待辦事項」，
不是開始施工。第一用途已定：**用語音操作這套量化系統**。

### 2026-08-02 覆評：**維持不做**
評估結論：語音是**介面層**，這套系統的瓶頸在**證據累積速度**與**獨立資料源**，
不在操作便利。真正的槓桿排序是 (1) 十月真實掛單簿 depth_deltas（唯一新資料
源）(2) 組合風控框架（決定能否同時跑 N 條薄策略）(3) 執行品質。另有一個具體
風險：隨時能問的助理會讓人**看得更頻繁**，而這個專案最硬的資產是「扳機沒到
就不動」的紀律（見 mistake.md 2026-06-20 FOMO 事件、6 次 informed override）。
使用者聽完後決定**先放著**。

### 若之後想加 AI，這個比語音助理值錢：**研究前置檢查器**
把 `.claude/rules/mistake.md` 的教訓變成會跑的機械檢查，掛在研究腳本前面。
2026-08-01~02 單一 session 就製造了約 10 個「輸出看起來完全正常」的儀器 bug，
每一個都可機械化擋掉：
- 宣稱含成本的回測 → 自動跑 cost=0 對照（含成本 ≥ 零成本 = 成本模型壞了）
- 價位型出場 → 強制檢查跳空穿越的成交價（觸價即成交是最貴的假設）
- 任何分桶 → n 的分佈物理合理性（有桶為 0 / 佔比 >90% 直接擋）
- bar 標籤 join 牆鐘資料 → 強制價格對齊證明
- 新圖表 → 字型 fallback、單位一致性（%, 小數, 機率不可混軸）
- 任何 try/except → 禁止靜默吞錯（至少印一行 WARN）
理由：它針對的是這套系統**真正的失效模式**（儀器壞了但數字漂亮），
不是「操作不方便」。而且它跟 JARVIS 不衝突——是那個助理最該有的能力。

### 這條紀律先寫在最前面（未來 session 必讀）
語音助理要碰量化系統，**只能走 `indicator/agent/` 的唯讀路徑**，受
`.claude/rules/agent-boundary.md` 管：可以問訊號／績效／shadow 狀態，
**絕不可下單、改部位、碰 kill switch、寫任何非 `agent_*` 的表**。
「語音」不是新的權限來源——它只是 agent 既有唯讀能力的另一個前端。
真要有「動作」類指令（例如暫停 executor），必須走既有的 admin endpoint
＋ token ＋人工確認，不得由語音層自行實作捷徑。

### 已查證的事實（省下未來重查）
本機（實測）：RTX 3070 Laptop **8 GB VRAM**（Windows API 報 4 GB 是
AdapterRAM 溢位，以 nvidia-smi 為準）、i7-11370H 4C/8T、RAM 23.7 GB、
C 槽 131 GB、**桌面走 Intel Iris Xe 所以 8 GB 幾乎全可用**；已有
node／docker／git／WSL2，**缺 ollama、ffmpeg**；base Python 3.9.12
（jarvis_ai 需 3.11+，要開新環境）。

`eadmin2/jarvis_ai`（MIT、109 stars、7 commits）：
- **macOS-first**（launchd ＋ 全 `.sh`，只在 Apple Silicon 測過）
- **ElevenLabs 是硬編的**，不是 provider 抽象——「之後換本地 TTS」
  是改程式碼不是改設定；`config/server.yaml` 只能設 `voice_id`
- 需要 Hermes Agent 跑在 `localhost:8642`，靠 `~/.hermes/.env` 的
  `API_SERVER_ENABLED` / `API_SERVER_KEY` / `JARVIS_HUD_TOKEN`
- HUD 是**單檔 vanilla JS、無 build step** → 可以只留 HUD 換後端
- 瀏覽器麥克風強制 HTTPS，自簽憑證那步不能跳

Hermes Agent（Nous Research，MIT）：走 Ollama 的 OpenAI 相容端點
`http://localhost:11434/v1`，**要求 num_ctx >= 64K**（Ollama 預設 2048，
不改必爛），官方推薦 `gemma4:31b`（要 24 GB）並明說**小模型會忽略工具
呼叫、吐純文字**——這是 8 GB 純本地路線的真正瓶頸，不是速度。

### 三條路徑（使用者尚未選）
1. **30 分鐘驗證跑**（原推薦）：Ollama ＋ Qwen3 8B ＋ Open WebUI，先量
   延遲／中文辨識／工具呼叫成功率，數據好再投 HUD。零成本可逆——同
   「5 分鐘 sanity 擋掉兩週浪費」的紀律
2. **混合腦**：本地語音管線 ＋ `claude -p`（headless Claude Code，用
   現有訂閱、不開 API key）當代理大腦；本地小模型只做閒聊與路由
3. **照 jarvis_ai 原樣裝**：電影體驗優先，接受 macOS→Windows 移植工
   與 ElevenLabs 免費額度很快用完

### 8 GB 的模型選擇
**Qwen3 8B Q4_K_M（~5 GB）為首選**（中文優於 Llama 系；需配
`OLLAMA_KV_CACHE_TYPE=q8_0` 才塞得下 64K KV）；Qwen3.5 9B（6.6 GB）
次選但 context 要降到 16-32K；14B 以上必 offload 到 RAM，語音互動不可用。
**STT 先跑 CPU**（faster-whisper base/small），把 8 GB 全留給 LLM。
本地 TTS 首選 **Kokoro-82M**（CPU 可跑、延遲低），替代 Piper／Chatterbox。

### 踩坑清單（已預先蒐集）
num_ctx 沒改 → 多步就失憶｜Python 3.9 裝到一半炸｜`.sh` 要 Git Bash
或 WSL2｜**Hermes 是否支援 Windows 原生未經證實**（不行就整組進 WSL2、
Ollama 留 Windows）｜麥克風要 HTTPS ＋ 信任自簽憑證｜缺 ffmpeg｜
ElevenLabs 免費額度以字計費、用很快｜VRAM 爆掉時 Ollama 會靜默 offload
到 CPU（要看 nvidia-smi，不是等它報錯）

### 落地位置
**開在 `flow_system` 之外**（例如 `C:\Users\rfo\Desktop\jarvis`），不要污染量化 repo。
Hermes 的資料存 `~/.hermes/`（內含 token），要排除在任何同步／備份之外。

---

## 已歸檔（2026-04 舊計畫，多數已被後續工作取代）

<details>
<summary>2026-04 重訓計畫與特徵構想（點開）</summary>

### 數據累積（已完成）
- [x] 新特徵數據累積 (impact_asymmetry, post_absorb_breakout, bvol)
- [x] 14 個新 CG 端點數據累積

### Direction / Magnitude 重訓（已由後續多輪重訓取代）
- 原計畫：liq_frag + absorb 99 特徵、AUC>0.60 部署門檻
- 後續實際：見 mistake.md 2026-06-01/02 的 ensemble A/B 紀律與 4 條部署門檻

### 滾動重訓機制
- [ ] 每 2-4 週自動重訓 + 新舊 OOS 比較（仍有效，低優先）

### 新特徵構想（待回測驗證）
- [ ] 持倉痛苦累積：funding × 持續時間 × OI
- [ ] 參與者分歧度：CB 溢價 × BFX 保證金 × Binance taker 一致性
- [ ] Funding 結算前 2h 行為異常
- [ ] 多空比加速度（二階導數）

### 多時間框架 / Ensemble（長期）
- [ ] 1h 短線確認 + 4h 同向；3-5 seed ensemble

</details>

## 已完成（歷史）
- [x] impact_asymmetry 特徵 (IC=-0.071, 方向模型)
- [x] post_absorb_breakout 特徵 (mag IC=0.191, 兩個模型)
- [x] flow_trend_score 特徵 (mag IC=0.156)
- [x] Magnitude 模型重訓 v2 (ICIR 1.18→1.22, Top/Bot 3.03→3.16)
- [x] 績效追蹤系統 (Rolling IC, Strong signal tracking, SHAP)
- [x] 互動圖表 (/ichart, TradingView Lightweight Charts)
- [x] Order Flow Toxicity 特徵 (tox_pressure_zscore IC=+0.071)
- [x] 統一 backfill 腳本 (backfill_all_parquet.py)
- [x] Gate A 通過（2026-06-10：Strong n=739, WR 59.5%, CI [56.0, 63.2]）
- [x] 3-WR 透明化 /okx-perf + Gate B 進度卡
- [x] 多帳戶跟單 Phase 0+1 程式碼（accounts registry + executor fan-out）
- [x] admin/OKX routes token guard 上線（`ADMIN_HEAL_TOKEN` 兩 service 設定 + fail-closed guard，2026-07-09 pushed）
- [x] branch `claude/general-session-HEJed` merge 進 main + push（本 TODO 即出自此 merge）
- [x] 撤單收集器 `depth_delta_collector` 上線 Service 2（2026-07-09，`depth_deltas_1m` 開始累積）

## 回測失敗（已排除，勿重跑）
- [x] ~~加密橫斷面 relative value（long top-6 / short bottom-6）~~ —
      2026-07-29 篩選 NO-GO（`research/cross_sectional_crypto.py`）：
      29 幣 × 21,604 根 1H，3 個回看 × 3 個持有 = 9 格全報告。
      **符號不一致（4/9 為正）**、僅 1/9 的 CI 排除 0 而那格是**負的**
      （L24_H24 −16.7 bps）、換手成本 6-16 bps/次直接吃掉毛利。
      9 格本來就期望 ~0.45 個偽陽性 → 無證據。附帶偏誤：29 幣是**今日
      的流動幣種**，死掉的幣不在裡面，倖存者偏誤特別灌水動量方向。
      **這關掉了本專案唯一沒碰過的 edge 家族**（先前 19 個 NO-GO 全是
      單一資產時間序列方向預測）。
- [x] ~~BTC 資金費率 carry（delta中性收費）~~ — 2026-07-29 篩選：9.3 個月
      毛年化僅 +2.6%（median 0.003%/8h，遠低於 0.01% 經典基線）、28% 結算
      為負、2-4 月連三月負；扣雙腿成本+現貨腿佔倍資本後 ≈ 1%/yr 量級
      → 當前 regime 不值一個組合槽（research/funding_carry_screen.py）。
      **復活觸發：trailing 90d 年化 > 8-10% 時重看**（腳本留檔可重跑）
- [x] ~~流動性獵取反轉~~ — 4h 週期上 IC ≈ 0
- [x] ~~K 線 delta 背離~~ — IC = 0.01
- [x] ~~consolidation_score~~ — IC ≈ 0
- [x] ~~ChessDomination 4D (CDP)~~ — 合成乘法結構稀釋信號
- [x] ~~ML exit model~~ — oracle 天花板分析 NO-GO
- [x] ~~WQ101 alphas (6)~~ — aggregate lift 被 outlier fold 撐起，per-fold 負
- [x] ~~liquidity proxy features (21)~~ — univariate IC 高但 ensemble 零提升
- [x] ~~exit-variants sweep + 不對稱 cutoff Option C~~ — 雙 NO-GO（5d83da2）
- [x] ~~taker/衍生品訂單流 × 傳統指標 context~~ — powered NULL（2026-07-09；壓縮/突破/關卡/高波動 4 context 無一把 taker IC 拉離 0；1h+ 已飽和；火花改押撤單訊號 or 次小時視野）
- [x] ~~聯合進出場 offline RL (FQI)~~ — NO-GO（2026-07-24；4 折 walk-forward 全輸 baseline，per-fold mean −112bps、CI[-157,-70] 全負、0/4 折為正；根因是資料量（每折僅 1200-1900 in_oos bar）撐不起 9 維 state 的 Q function，非實作 bug；見 mistake.md）
