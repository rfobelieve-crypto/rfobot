# Secrets — never commit
.env
.env.*
!.env.example
config.json
config.*.json
secrets.yaml
secrets.yml
secrets.json
credentials.json
*.pem
*.key
railway-recovery-codes.txt
*recovery-codes*.txt
*.cert
*.crt
*.p12
*.pfx
api_keys.txt
.netrc
# Local-only setup scripts that may carry keys
scripts/local_*.sh
# Personal — resume / CV variants (template-only version is committed once;
# rename your filled-in copy to docs/resume_*.local.md or move outside repo)
docs/resume_*.local.md
docs/resume_*.docx
docs/resume_*.pdf
docs/cover_letter_*.md
scripts/setup_local_*.py

# Python
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Runtime state — never commit
indicator/state/

# Large data — never commit
market_data/raw_data/
research/ml_data/
research/eda_charts/
research/cache/
.cache/
.claude/worktrees/

# Local scripts with secrets
market_data/backfill/daily_collect.bat
market_data/backfill/setup_schedule.ps1

# Data cache and backups
indicator/model_artifacts/.data_cache/
indicator/model_artifacts/*_backup.parquet

# Local only
.claude/settings.local.json

# Remotion video output / deps / generated VO mp3s
promo_video/node_modules/
promo_video/out/
promo_video/.remotion/
promo_video/public/audio/*.mp3

# Course / marketing material — kept local only (slides, scripts, large mp4 videos)
量化課程/
外部技術借鑑/
research/results/portfolio_clocks_state.json
research/results/portfolio_clocks.log
research/sweep_failure/.cache/
research/results/sweep_shadow_run.log

# 看門狗每 5 分鐘一行，不進 git（2026-09-03）
research/results/arb_watchdog.log

# 原始 API 快取（2026-09-05）：只是為了避免重跑時重打 API，不是研究結果。
# perpdex_cache 一度到 55MB 被推上 GitHub，之後不再追蹤。
research/results/*_cache.json
research/results/funding_raw_cache.json
